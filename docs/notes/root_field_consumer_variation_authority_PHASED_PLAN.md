# Root-Field Consumer Variation Authority Proof

## Explicit User Asks

- Continue pressing the ExplainO pattern/root-field work toward system-wide applicability.
- Do not jump into a broad global rewrite before proving the next concrete seam.
- Preserve the completed Pattern Bank V1 behavior while making the same generated-root variation authority reach root-field consumer lanes.
- Prove the visible product path, not only helper functions, reacts to normal ExplainO variation controls when the active row points at the generated N-gon pattern.
- Document that root-field consumers and Color Pipeline rows are separate consumers: primary N-gon dynamics can remain active while a row samples secondary legacy roots.

## Current Phase

Phase 5 - hostile audit, validation, receipts, rearward review, push, and clean-tree closeout.

## Phase Checklist

- [x] Phase 0: create and validate this plan/contract, then lock the active slice.
- [x] Phase 1: add native renderer proof that a root-field consumer lane changes when generated regular-N root variation changes through normal ExplainO seed/phase/spread authority.
- [x] Phase 2: add no-mouse runtime proof for `explaino_magnet_root_well` regular-N variation through seed/phase/spread with active primary row routing.
- [x] Phase 3: add a clarity proof that secondary legacy rows remain separately reported while primary N-gon consumer variation can still affect the frame.
- [x] Phase 4: update report/proof docs and classify the broader global-extension follow-up.
- [ ] Phase 5: hostile audit, validation, receipts, rearward review, push, and clean-tree closeout.

## Scope

In scope:

- Root-field consumer lanes:
  - `explaino_mandelbrot_root_trap`
  - `explaino_magnet_root_well`
- Normal ExplainO root-layout variation controls already exposed for these lanes:
  - `explaino_generated_root_layout`
  - `explaino_generated_root_count`
  - `explaino_seed`
  - `explaino_phase`
  - `explaino_root_spread`
  - `explaino_root_field_pattern_ref`
- Programmable basin coloring path only where it currently drops `ViewState` or root-field consumer semantics.
- Focused native renderer and runtime no-mouse proof.
- Documentation of broader global root-pattern applicability as follow-up only.

Out of scope:

- New fractal lanes.
- New root-metric Color Pipeline source functions.
- `root_index` pattern refs or categorical routing changes.
- Unlimited pattern banks, graph editor, pattern algebra, or Pattern B custom roots.
- Escape-time N-root Newton dynamics.
- SDF field-generation optimization, SDF ops, or SDF-native lane work.
- Physical mouse automation.

## Phase 0 - Lock

- Contract validation and explicit plan sync passed.
- Active slice locked with `viewer_host_begin_work_slice.py` as `ck:cf40af71`.
- Discovery after lock found the previous identical Magnet example used a `secondary` legacy root source row, making the example ambiguous: the row sampled secondary roots while the root-field consumer itself still reported primary roots.

## Phase 1 - Product-Path Proof

Required proof:

- Native renderer proof that `explaino_magnet_root_well` with `regular_ngon_v1` changes pixels when normal ExplainO root variation changes.
- The proof must exercise `RenderFractalCUDA(...)` or an equivalent product renderer path, not only `TryResolveRootFieldConsumerDistance(...)`.
- The active Color Pipeline row must use `root_pattern_ref=primary` so generated N-gon variation is actually in the pixel path.

## Phase 2 - Runtime Primary Variation Proof

Implement:

- Published no-mouse proof that `explaino_magnet_root_well` changes frame/root hashes when primary regular-N seed/phase/spread changes and the active source row uses `primary`.
- Preserve the existing Pattern Bank V1 proof where primary and secondary rows intentionally produce different output.

## Phase 3 - Dual-Consumer Clarity Proof

Required proof:

- Document or test that `explaino_magnet_root_well` can report the root-field consumer on `primary` while a Color Pipeline source row samples `secondary`.
- Prove frame changes under primary generated-root variation are valid in that dual-consumer setup because the root-field consumer remains active.
- Classify the remaining UX concern as a future communication/globalization issue, not as a renderer defect.

## Phase 4 - Follow-Up Classification

Record:

- Whether this basin-color repair is enough to treat root-pattern authority as shared infrastructure for current root-field consumers.
- What remains before a truly global extension:
  - root-index categorical refs;
  - graph/pattern algebra;
  - additional Color Pipeline root-metric sources;
  - non-root field consumers;
  - unified source-signal descriptors for all color domains.

## Phase 5 - Hostile Audit And Close

Exit criteria:

- Hostile audit records at least one real finding or three clean passes after repair.
- Focused native rails pass.
- Runtime publish passes.
- Published no-mouse runtime proof passes.
- Contract validation, plan sync, code quality, hostile-audit validation, and diff check pass.
- Validation and contract proof receipts are written.
- Rearward review is `ok` on final committed head.
- Branch is pushed and tree is clean.
- Preplanned sliced work for this repair is exhausted; stop for replan before a broader global root-pattern campaign.

## Hostile Audit

- Status: complete
- Classification: real finding repaired by test/plan correction; no code defect found in current product path.

Audit questions:

- Did the product renderer path actually prove generated root variation through `RenderFractalCUDA(...)`?
- Did root-field consumer root-proximity/root-phase use consumer-aware pattern routing in the runtime report?
- Did normal ExplainO seed/phase/spread controls affect `explaino_magnet_root_well` with regular N roots when the active row uses primary roots?
- Did neutral collapse and existing Magnet/Mandelbrot defaults remain intact?
- Did Pattern Bank V1 primary/secondary routing remain intact?
- Did this slice avoid broad graph/pattern-algebra work?

## Audit Passes

- [x] Pass 1 - found the initial repair hypothesis was wrong: previous identical example used secondary legacy row routing and did not prove a renderer bug.
- [x] Pass 2 - re-read the repaired state after runtime proof found secondary rows and primary root-field consumers are separate active consumers; the plan was corrected from a false masking model to a dual-consumer model, and no additional real defect found in that corrected authority model.
- [x] Pass 3 - clean re-read after focused native and published runtime rails passed; no additional workflow mistake found and no additional V1 blocker found.

## Audit Findings

- [x] Finding 1: The first interpretation treated the identical Magnet example as a missing generated-root variation repair. Inspection showed the state used `root_pattern_ref=secondary` on the active root row while primary regular-N roots were changed, so the example was ambiguous. The slice now proves the dual-consumer model explicitly: primary N-gon variation changes the root-field consumer frame, while a root-aware row can still report/use secondary legacy roots.
- [x] Finding 2: The initial logged native run timed out during Visual Studio environment setup before the test body ran. Rerunning the same checked-in command with a longer timeout produced valid proof. This remains tooling friction, not product failure.

## Proof Ledger

| Item | Evidence |
| --- | --- |
| Branch opened | `git checkout -b codex/root-field-consumer-variation-authority` from clean `3b7bbaf`. |
| Initial contract validation | `py -3.14 tools/viewer_host_validate_slice_contract.py --contract docs/contracts/root_field_consumer_variation_authority.contract.json --out-json artifacts/validation/root_field_consumer_variation_authority_contract_initial.json` passed. |
| Active slice lock | `py -3.14 tools/viewer_host_begin_work_slice.py --intent "Root-field consumer generated-pattern variation authority" --profile runtime --plan docs/notes/root_field_consumer_variation_authority_PHASED_PLAN.md --contract docs/contracts/root_field_consumer_variation_authority.contract.json` opened `ck:cf40af71`. |
| Contract revisions | `py -3.14 tools/viewer_host_revise_contract.py --session-id global_active_contract --plan docs/notes/root_field_consumer_variation_authority_PHASED_PLAN.md --contract docs/contracts/root_field_consumer_variation_authority.contract.json` re-locked after correcting the plan from repair to proof/dual-consumer semantics. |
| Native product-path proof | `py -3.14 tools/viewer_host_run_logged_command.py --label root_field_consumer_variation_native --log artifacts/root_field_consumer_variation_authority/native.log -- cmd /c ui_app\build_tests_vsdevcmd.cmd test_fractal_renderer test_escape_time_coloring` passed; `test_fractal_renderer: passed=96 failed=0`, `test_escape_time_coloring: all passed`. |
| Runtime publish | `py -3.14 tools/viewer_host_run_logged_command.py --label root_field_consumer_variation_runtime_publish --log artifacts/root_field_consumer_variation_authority/runtime_publish.log -- cmd /c ui_app\build_vsdevcmd.cmd` passed and staged `D:\salt-fractal\cuda_newton_fractal_clone\runtime\fractal_ui.exe`. |
| Published runtime proof | `py -3.14 -m pytest tests/test_fractal_runtime_root_field_consumers.py -q --junitxml artifacts/pytest/root_field_consumer_variation_runtime.junit.xml` passed after runtime publish: `7 passed`. |
| Code quality | Pending. |
| Contract validation closeout | Pending. |
| Plan sync | Pending. |
| Receipts | Pending. |
| Rearward review | Pending. |
| Branch push | Pending. |
| Stop boundary | Pending. |
