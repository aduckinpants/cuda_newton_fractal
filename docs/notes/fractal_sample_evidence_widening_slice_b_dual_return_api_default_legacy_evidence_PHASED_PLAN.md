# Fractal Sample Evidence Widening Slice B - Dual Return API And Default Legacy Evidence

## Current Phase

Complete - slice B closed on its own head with one widened host API plus default legacy-evidence kernel wiring while `SampleFractalPoints(...)` remained the shipped legacy projection path. Later slice C on this branch landed the first paired-state/counterfactual widened consumer, so this plan is historical proof of the dual-return/API stop point rather than the live branch status surface.

## Phase Checklist

- [x] Phase 1 - open this checked-in slice-B plan/contract, repair the duplicated `ck:4b8ae91d` handoff closeout, reread launch-anchor and staging truth, and pin the current RED owner seams.
- [x] Phase 2 - land one widened host API plus default legacy-evidence kernel wiring while preserving `SampleFractalPoints(...)` as the shipped legacy projection path.
- [x] Phase 3 - add focused API or kernel projection proof, run the narrow validation ladder, complete hostile audit, checkpoint, write receipts, reread the committed head for stale closeout text, and leave a clean tree.

## Explicit User Asks

- [done] Front-load the small continuity cleanup by removing the duplicated `ck:4b8ae91d` closeout line in `HANDOFF_LOG.md`, then re-read the launch anchor and staging packet for live slice-A truth.
- [done] Add one widened host API beside `SampleFractalPoints(...)` while preserving `SampleFractalPoints(...)` as the shipped legacy projection path.
- [done] Emit default widened evidence that projects back to unchanged legacy semantics without widening into the first consumer.
- [done] Keep renderer replacement, whole-engine generic refactor, advanced-color reopen, Explaino deferred follow-ups, and slice-C consumer work out of scope.
- [done] Close only with commit, validation receipt, contract proof receipt, clean tree, and a committed-head stale-plan reread.

## Proof Ledger

- Bootstrap on 2026-05-15 proved branch=`feature/fractal-sample-evidence-widening`, `HEAD=a9a2a11`, a clean tree, and an active locked contract still pointing at the already closed slice-A contract `fractal_sample_evidence_widening_slice_a_contract_skeleton`.
- Minimal workflow preflight was required because the closed slice-A lock could not truthfully authorize successor slice-B file creation; the staging umbrella was re-locked only long enough to mint this slice-B plan/contract and then restored before the real slice-B breadcrumb was appended under `ck:796f46e9`.
- `HANDOFF_LOG.md` no longer carries the duplicated `ck:4b8ae91d` closeout line, so the restart-facing continuity defect was repaired before the feature mutation advanced.
- RED proof landed first in `ui_app/tests/test_fractal_sample_result.cpp`: a focused native compile failed on undeclared `SampleFractalEvidencePoints`, which proved the current head initially had no widened host API beside `SampleFractalPoints(...)`.
- The same RED reread proved `ui_app/src/fractal_sample_core.cu` still allocated, launched, and copied only `FractalSampleResult`, `ui_app/src/fractal_sample_device.inl` still terminated by returning one legacy `FractalSampleResult` payload per coordinate, and the sidecar/runtime-walk owners still consumed only `FractalSampleResult`, so the first widened consumer remained out of scope on the RED head.
- GREEN implementation now lives in `ui_app/src/fractal_types.h` plus `ui_app/src/fractal_sample_core.cu`: `SampleFractalEvidencePoints(...)` exists as the widened host API, the sample kernel now emits `FractalSampleEvidence` through `fractal_sample_evidence_device(...)`, and `SampleFractalPoints(...)` projects back through `BuildLegacySampleResult(...)` while keeping the legacy signature unchanged.
- Focused proof is green on the repaired state: `test_fractal_sample_result` now reports 40 passed, `test_fractal_sample_core` reports 17 passed, `test_fractal_sample_kernel` reports 136 passed, and `test_explaino_sidecar_cuda_sample_host` reports 14 passed.
- The launch anchor and staging packet required a truth repair after the GREEN implementation because the launch anchor still claimed the repo had no `SampleFractalEvidencePoints(...)`; those docs now reflect the live slice-B stop point while still deferring the first widened consumer.
- `ui_app/src/generic_sample_core.h` plus `ui_app/src/generic_sample_core.cu` still provide adjacent proof that a second bounded sample contract can exist without forcing a whole-engine abstraction rewrite.
- A first checkpoint commit landed as `dc9df82` and wrote validation plus contract-proof receipts, and the mandated stale-plan reread then exposed the lingering Phase 3 and closeout `todo` text that this final plan repair removes on the current head.

## Hostile Audit

- Status: complete
- Required posture: assume the slice only renames surfaces, accidentally replaces `SampleFractalPoints(...)`, silently widens into the first consumer, genericizes the engine by inertia, leaves the duplicate handoff line in place, or stops with stale closeout text until hostile proof disproves each failure mode.
- Current hostile result: three real defects were found and repaired during the slice, and the clean re-read of the repaired state did not find an additional real defect.

## Audit Passes

- [x] Pass 1 - re-prove the RED head had no widened host API beside `SampleFractalPoints(...)`, no widened default legacy-evidence emission in the sample kernel path, and no widened consumer.
- [x] Pass 2 - hostile review found and repaired the first real test defect: the initial widened-kernel proof patch wrote a broken string literal and would have failed for the wrong reason.
- [x] Pass 3 - hostile review found and repaired the second real doc-truth defect: the launch anchor still claimed the repo had no `SampleFractalEvidencePoints(...)` after the widened API landed.
- [x] Pass 4 - clean re-read of the repaired slice-B head confirmed `SampleFractalPoints(...)` still stays the shipped legacy projection path, widened evidence projects back truthfully, slice C was still deferred on that head, legacy callers still compile on the legacy surface, and no additional real defect was found.
- [x] Pass 5 - the mandatory stale-plan gate found and repaired lingering Phase 3 plus closeout `todo` text after the first receipt pass so the active plan now matches the actual closed state.

## Audit Findings

- [x] Real test defect fixed: the first widened-kernel witness patch accidentally broke its own `std::cerr << "\n"` string literal and would have produced a false compile failure until the witness was repaired.
- [x] Real doc-truth defect fixed: the launch anchor still claimed the repo had no checked-in `SampleFractalEvidencePoints(...)` after the GREEN implementation landed.
- [x] Real stale-closeout defect fixed: after the first receipt pass, this active slice-B plan still said Phase 3 was open and still carried a closeout `todo`, so the post-closeout stale-plan gate required one final truth repair.
- [x] Clean re-read evidence: `SampleFractalPoints(...)` still keeps the legacy signature and is now an explicit projection adapter over widened evidence, the default evidence kernel stays bounded to `sample_coord` plus nested `legacy_result`, the sidecar host still passes on the legacy API surface, and no additional real defect was found.

## Notes

- Current single-orbit authority for shipped callers still lives in `ui_app/src/fractal_sample_result.h`, `ui_app/src/fractal_types.h`, `ui_app/src/fractal_sample_core.cu`, `ui_app/src/fractal_renderer.cu`, `ui_app/src/explaino_sidecar_cuda_sample_host.cpp`, `ui_app/src/explaino_sidecar_measurement.cpp`, and `ui_app/src/runtime_walk_field_slime.cpp`.
- No current required caller already assumes richer evidence informally: sidecar measurement and runtime-walk aggregate only the legacy `FractalSampleResult` fields, the renderer still consumes the same legacy result directly for pixel coloring, and the sidecar host still passes against the unchanged `SampleFractalPoints(...)` surface.
- The widened authority for this slice now lives in `ui_app/src/fractal_sample_result.h`, `ui_app/src/fractal_types.h`, and `ui_app/src/fractal_sample_core.cu`: one explicit widened host API plus one explicit legacy projection helper and adapter path.
- Slice B stayed fully bounded without runtime publish or widened-consumer proof because no viewer-visible consumer behavior changed; slice C later became the first-consumer proof on this branch.
