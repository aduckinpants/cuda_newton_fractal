# Projection-and-Flow Fractal - Explaino-Pattern Variant

## Current Phase

Phase 4 in progress - hostile audit is complete on the repaired state and the slice is waiting on checkpoint, receipts, the committed-head stale-plan gate, and final clean-tree closure.

## Phase Checklist

- [x] Phase 1 - open this checked-in plan and contract, replace the closed Projection-and-Flow pressure-threshold repair lock, and prove the RED head still has no shipped Explaino-pattern Projection-and-Flow selector/runtime lane or published-runtime witness.
- [x] Phase 2 - land one bounded Explaino-pattern Projection-and-Flow runtime lane with explicit identity mapping, explicit shared-vs-Explaino control ownership, and preserved legacy sample surfaces.
- [x] Phase 3 - prove the lane through focused schema/unit/kernel/native rails plus focused runtime publish and published-runtime Explaino-variant proof.
- [ ] Phase 4 - run hostile audit, checkpoint, write receipts, clear stale closeout text, and leave the repo clean on the committed head.

## Explicit User Asks

- [x] Open and execute only one bounded Explaino-pattern Projection-and-Flow variant slice.
- [x] Layer the steerable standalone Projection-and-Flow object into the Explaino pattern as a real runtime-visible lane, not just a renamed selector.
- [x] Keep color-pipeline work, ExplainO-BalanceVoid, meta-basin, Operator-Itinerary, DSL/program-space, and broad engine refactor work out of scope.
- [x] Preserve `FractalSampleResult` and `SampleFractalPoints(...)` as the shipped legacy projection surfaces.
- [ ] Close only with focused native proof, runtime publish, published-runtime proof, hostile audit, checkpoint commit, machine receipts, and the committed-head stale-plan gate.

## Proof Ledger

- Bootstrap proved branch=`feature/fractal-sample-evidence-widening`, `HEAD=563639d`, clean tree, fresh session, and an active locked contract still pointing at the already closed Projection-and-Flow pressure-threshold repair contract `projection_and_flow_pressure_threshold_color_pipeline_regression_repair`.
- Minimal blocker-clearing preflight is required before feature edits because the current active contract does not permit creating this new Explaino Projection-and-Flow plan/contract pair; the preflight must stay limited to replacing that stale lock.
- Current shipped standalone Projection-and-Flow authority is explicit and bounded in `ui_app/src/fractal_types.h`, `ui_app/src/fractal_derived_fields.cpp`, `ui_app/src/fractal_sample_device.inl`, `ui_app/src/fractal_renderer.cu`, `ui_app/src/schema_binding.cpp`, `ui_app/src/diagnostics_capture.cpp`, `ui_app/src/diagnostics_state_io.cpp`, `ui_app/src/fractal_runtime_validation.h`, and `tests/test_fractal_runtime_projection_and_flow.py`.
- Current Explaino-pattern precedent is explicit and bounded: `explaino_counterfactual_pair` already proves the repo expects Explaino variants to carry explicit enum/runtime identity, explicit shared-vs-Explaino control ownership, and dedicated runtime proof instead of mere selector aliasing.
- RED-head inspection shows there is still no shipped `explaino_projection_and_flow` selector or runtime lane: no enum id, no schema selector, no Explaino selector descriptor, no diagnostics/runtime persistence seam, and no published-runtime test witness currently names or proves such a variant.
- Shared standalone Projection-and-Flow controls that this slice must either preserve or explicitly fence are `projection_and_flow_root_family`, `projection_and_flow_target_radius`, and `projection_and_flow_pressure_threshold`; RED still shows they are only public on standalone `projection_and_flow`.
- Initial Explaino-specific carrier hypothesis to prove or reject in code: the new lane should reuse the hardened Projection-and-Flow object while taking its distinct Explaino runtime identity from the existing Explaino carrier seams (`explaino_phase`, `explaino_seed`, `explaino_warp_strength`, `explaino_damping`, and `UpdateExplainoPolynomial(...)`) rather than reopening engine widening or color-pipeline work.
- Current widened payload appears sufficient without another engine pass: `SampleFractalPoints(...)`, `FractalSampleResult`, and the existing legacy sample adapters remain the shipped surfaces and must stay behaviorally untouched unless a tiny concrete blocker seam disproves this.
- Runtime-visible authority seams expected for this slice are `ui/fractal_binding_surface_v1.ui_schema.json`, `ui_app/src/fractal_types.h`, `ui_app/src/fractal_family_rules.h`, `ui_app/src/fractal_derived_fields.cpp`, `ui_app/src/schema_binding.cpp`, `ui_app/src/enum_id_utils.h`, `ui_app/src/safe_mode_schema.cpp`, `ui_app/src/fractal_runtime_validation.h`, `ui_app/src/fractal_sample_device.inl`, `ui_app/src/fractal_renderer.cu`, `ui_app/src/diagnostics_capture.cpp`, and `ui_app/src/diagnostics_state_io.cpp`.
- First RED target after the contract opens: prove the current repo has no shipped Explaino-pattern Projection-and-Flow runtime lane, no existing Explaino-family surface that already counts as it, and no need to mutate `SampleFractalPoints(...)` or color-pipeline seams to establish that absence.
- Landed runtime authority now exists at the intended seams without reopening engine widening: `FractalType::explaino_projection_and_flow` is explicit in schema/enum/family rules, the shared Projection-and-Flow controls stay public on the new carrier, the Explaino identity is explicit through Explaino presets plus runtime warp/damping controls, and `SampleFractalPoints(...)` / `FractalSampleResult` remain untouched.
- Focused proof is green on the repaired state:
  native helper rail `ui_app\\build_tests_vsdevcmd.cmd` via `artifacts/logs/projection_and_flow_explaino_variant_native_full_3.log`
  runtime publish `ui_app\\build_vsdevcmd.cmd` via `artifacts/logs/projection_and_flow_explaino_variant_runtime_build.log`
  published-runtime Explaino Projection-and-Flow witnesses `py -3.14 -m pytest tests/test_fractal_runtime_explaino_escape_variants.py -k projection_and_flow -q` via `artifacts/logs/projection_and_flow_explaino_variant_runtime_pytests_pf.log`
  published callable-surface witnesses `py -3.14 -m pytest tests/test_function_descriptor_cli.py tests/test_callable_engine_adversarial_cli.py -q` via `artifacts/logs/projection_and_flow_explaino_variant_runtime_pytests_callable.log`

## Hostile Audit

- Status: complete
- Required posture: assume the first implementation will only rename standalone Projection-and-Flow, leave the Explaino identity implicit, leak dead public controls, touch legacy sample surfaces, drift into color-pipeline work, or stop with stale closeout text unless focused proof disproves each failure mode.
- Hostile review questions:
  Did I actually implement an Explaino-pattern Projection-and-Flow lane, or only rename the standalone mode?
  Did I make the Explaino identity explicit, or leave it implicit and hand-wavy?
  Did I preserve `SampleFractalPoints(...)` as the shipped legacy projection path?
  Did I silently widen into color-pipeline work, ExplainO-BalanceVoid, meta-basin, or a generic engine rewrite?
  Did I stop with stale closeout text again?

## Audit Passes

- [x] Pass 1 - prove the RED head still has no shipped Explaino-pattern Projection-and-Flow selector/runtime lane or published-runtime witness.
- [x] Pass 2 - hostile reread the landed identity/control mapping for explicit Explaino ownership, shared-control truth, and preserved legacy sampling; the repaired state did not expose another scope leak or legacy-surface mutation.
- [x] Pass 3 - rerun focused native/runtime proof, perform a clean re-read of the repaired state, and confirm no additional real defect or workflow mistake remains after the probe/callable/runtime witness repairs.

## Audit Findings

- [x] Hostile pass found a stale probe-coverage lie: legacy Explaino single-axis / BalanceVoid selectors already canonicalize to `explaino_all` on the public runtime surface, so the broad native rail was asserting the wrong runtime id. The proof harness was repaired and extended to cover `explaino_projection_and_flow` explicitly.
- [x] Hostile pass found a real owner seam omission: `ui_app/src/fractal_probe_runner.cpp` had no Projection-and-Flow carrier path, so the new lane was not callable through `fractal.sample` even though the sample kernel and renderer were green. The probe runner now implements the shared Projection-and-Flow carrier seam with Explaino warp/damping identity on the new carrier.
- [x] Hostile pass found a callable-surface lie: the probe descriptor whitelist was advertising `explaino_counterfactual_pair` through a broad Explaino-selector filter even though `SamplePoint` does not implement it. The whitelist is now explicit, keeps the lie out of the published probe catalog, and intentionally includes `explaino_projection_and_flow`.
- [x] Hostile pass found a bad runtime witness expectation in this slice: generic `explaino_all` should reject a Projection-and-Flow payload rather than silently pretending to be the new lane. The published-runtime test now asserts that rejection as proof that the Explaino Projection-and-Flow identity stays explicit.

## Notes

- Shared control ownership to carry forward if the current engine seam proves sufficient: `projection_and_flow_root_family`, `projection_and_flow_target_radius`, and `projection_and_flow_pressure_threshold`.
- Explaino-specific identity to make explicit if the current engine seam proves sufficient: existing Explaino polynomial/root-pack carrier surfaces plus current Explaino runtime controls, not a color-pipeline expansion.
- Out-of-scope fences to re-prove after RED: color-pipeline expansion, ExplainO-BalanceVoid, meta-basin, Operator-Itinerary, DSL/program-space, generic engine rewrite, and changes to `SampleFractalPoints(...)` / `FractalSampleResult`.
