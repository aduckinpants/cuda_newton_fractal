# Advanced Color Library Foundation Phase 8R - Explaino Structural Root-Pack Classification And Owner Map

## Current Phase

Complete - the shared structural/root-pack classification slice closed on `70a323e`: it proved the bucket must split. On the current head, `phase8s` supersedes the provisional `phoenix_p_real` side of that answer with a dedicated phoenix-step carrier surface, while `explaino_cluster_radius` remains on the structural/root-pack carrier map.

## Phase Checklist

- [x] Phase 1 - replace the closed phase8q staging lock with a new bounded structural/root-pack classification contract, inspect the owner seams for `phoenix_p_real` and `explaino_cluster_radius`, and land RED witnesses that prove the repo still lacks one explicit fail-closed ownership answer for this bucket
- [x] Phase 2 - classify `phoenix_p_real` and `explaino_cluster_radius` explicitly through selector/runtime/default/state proof, make any required split explicit, and land only the minimal authority updates needed to keep that answer truthful
- [x] Phase 3 - rerun the narrowest truthful native plus published-runtime witnesses, hostile-audit the repaired state, checkpoint, write machine receipts, reread the committed head for stale closeout text, and keep slices 1-3 plus phase8n and phase8p intact

## Explicit User Asks

- [done] Open and execute one bounded structural/root-pack classification plus owner-seam map slice for `phoenix_p_real` and `explaino_cluster_radius` only.
- [done] Prove which current legacy selectors own `phoenix_p_real` and `explaino_cluster_radius`, whether each parameter has real runtime effect, and whether default or zero behavior is compatible with canonical Explaino behavior.
- [done] Do not force one shared ownership answer across unlike seams; make the immediate split explicit because the live runtime/default proof requires it.
- [done] Keep dual-seed / multi-seed structure, resolved couplings, generic advanced-color core work, manual archive archaeology, and historical archive compatibility out of scope.
- [done] Preserve slices 1-3, phase8n, and phase8p, and close only with hostile review, checkpoint commit, validation receipt, contract proof receipt, clean tree, and a committed-head stale-plan reread.

## Presumption Loop

The controlling risk for phase8r was false canonicalization by structural analogy. The final answer on this head is a split different-ownership outcome, not a structural promotion preview: `phoenix_p_real` remains a shared phoenix-step memory-term carrier, `explaino_cluster_radius` remains a separate root-pack carrier with divergent `explaino_mult` versus `explaino_rational` runtime roles, and zero/default does not collapse any of the active carriers back to canonical Explaino.

This slice closes the shared classification question. Any future follow-up must start from this checked-in answer and open separate later work for `phoenix_p_real` and/or `explaino_cluster_radius` instead of pretending the old shared staging bucket is still unresolved.

## Proof Ledger

- Bootstrap on 2026-05-15 proved branch=`feature/advanced-color-pipeline-draft-editor-reframe`, `HEAD=eafc3d4`, clean tree, and an active locked contract still pointing at the already-closed phase8q structural/root-pack staging slice.
- `py -3.14 tools/viewer_host_repo_status.py` proved `staged=none | unstaged=none | untracked=none`, so this classification slice started as a fresh session rather than dirty carryover.
- Mandatory authority reread re-proved that phase8q is docs-only staging, phase8p settled the dual-seed bucket separately, and phase8n settled the resolved couplings bucket separately.
- Current code search before implementation proved there was still no explicit structural/root-pack ownership registry parallel to `kExplainoAxisRegistry`, `kExplainoCouplingRegistry`, or `kExplainoDualSeedRegistry` in `ui_app/src/fractal_family_rules.h`.
- RED landed truthfully: `artifacts/phase8r_red_native.log` captured the focused compile failure on missing `ExplainoStructuralParamSlot`, `ExplainoStructuralCarrierDescriptor`, and `FindExplainoStructuralCarrierDescriptor`, proving the repo still lacked one fail-closed structural ownership surface before implementation.
- On the phase8r closeout head, `ui_app/src/fractal_family_rules.h` owned one structural parameter registry plus one carrier map. That proof forced the split immediately: `phoenix_p_real` carriers were the shared phoenix-step surfaces `phoenix`, `explaino_phoenix`, `explaino_joy`, `explaino_fold`, `explaino_bell`, `explaino_ripple`, `explaino_splice`, `explaino_vortex`, and `explaino_tension`, while `explaino_cluster_radius` stayed fenced to `explaino_mult` and `explaino_rational`. On the current head, `phase8s` lifts `phoenix_p_real` out of that structural map into a dedicated phoenix-step carrier surface.
- On the phase8r closeout head, `ui_app/src/fractal_derived_fields.cpp` routed `phoenix_p_real` and `explaino_cluster_radius` defaults through `ApplyExplainoStructuralRegistryDefaults(...)` instead of per-selector folklore. On the current head, `phase8s` narrows `phoenix_p_real` to `ApplyPhoenixStepCarrierDefaults(...)` / `ResetPhoenixStepCarrierValues(...)`, while `explaino_cluster_radius` remains on the structural registry path.
- The current head still proves through `ui_app/tests/test_ui_schema.cpp` that the main schema fences `phoenix_p_real` and `explaino_cluster_radius` to their exact non-canonical carrier maps and never exposes them under `explaino_all`.
- `ui_app/tests/test_fractal_derived_fields.cpp` now proves the semantics diverge in the host owner seam: changing `phoenix_p_real` leaves Explaino roots/coefficients unchanged, changing `explaino_cluster_radius` changes `explaino_mult` roots directly, and changing `explaino_cluster_radius` leaves `explaino_rational` roots/coefficients unchanged because that carrier owns the Laurent runtime term instead.
- Focused native witnesses are green from `D:/salt-fractal/cuda_newton_fractal_clone/build_tests`: `test_fractal_family_rules.exe`, `test_ui_schema.exe`, `test_fractal_derived_fields.exe`, `test_runtime_reset.exe`, `test_fractal_runtime_validation.exe`, `test_schema_binding.exe`, `test_diagnostics_state_io.exe`, and `test_fractal_probe.exe`.
- `py -3.14 tools/viewer_host_run_logged_command.py --label "phase8r-runtime-publish" --log artifacts/phase8r_runtime_publish.log -- ui_app/build_vsdevcmd.cmd` republished the current runtime successfully to `D:/salt-fractal/cuda_newton_fractal_clone/runtime/fractal_ui.exe`.
- `py -3.14 tools/viewer_host_run_logged_command.py --label "phase8r-runtime-pytest" --log artifacts/phase8r_runtime_pytest.log -- py -3.14 -m pytest tests/test_fractal_runtime_explaino_escape_variants.py -q -k "test_explaino_all_neutral_defaults_match_explaino_published_runtime or test_explaino_all_registry_axes_round_trip_and_change_published_runtime_frame or test_explaino_balance_void_neutral_defaults_match_explaino_published_runtime or test_explaino_dual_seed_runtime_behavior_stays_outside_canonical_explaino_all or test_explaino_legacy_couplings_round_trip_and_neutral_collapse or test_explaino_structural_root_pack_runtime_classification or test_explaino_all_ignores_deferred_structural_root_pack_params"` passed `12 passed, 30 deselected`, proving canonical Explaino, phase8n, and phase8p all stay intact while the new structural carrier answers remain outside canonical Explaino-all.

## Hostile Audit

- Status: complete
- Required posture: assume the bucket is being forced into one answer, that zero/default claims are being hand-waved, or that the work silently widened into promotion, dual-seed, or resolved-coupling follow-up until deterministic selector/runtime/default/state proof disproves that suspicion.
- Current hostile result: one real runtime-classification defect was found and repaired, and the repaired state survived a clean native plus published-runtime re-read without exposing another real issue.

## Audit Passes

- [x] Pass 1 - land REDs that prove the repo still lacks one fail-closed structural/root-pack ownership authority and that the live selector/runtime/default seams already diverge.
- [x] Pass 2 - hostile re-read the first GREEN for forced bucket unification, fake zero-collapse claims, or silent widening into promotion, dual-seed, or resolved couplings.
- [x] Pass 3 - clean re-read the repaired state with the final narrow native and published-runtime witnesses and confirm the committed head does not still contain stale closeout text.

## Audit Findings

- [x] Real missing authority fixed: the repo had canonical Explaino, deferred coupling, and dual-seed registries, but no parallel structural/root-pack ownership surface before this slice.
- [x] Real runtime-classification defect fixed: the first GREEN incorrectly marked `explaino_phoenix` and `explaino_rational` neutral structural states as canonical Explaino collapse. Published-runtime proof disproved that assumption, so the carrier map now records `zero_collapses_to_baseline=false` for both and the runtime witness expects those neutral states to remain outside canonical Explaino.
- [x] Clean re-read of the repaired state reran the focused native and published-runtime witnesses and did not expose another real issue after the zero-collapse correction.

## Notes

- Required owner seams touched on this head:
  - `docs/contracts/advanced_color_library_foundation_phase8r_explaino_structural_root_pack_classification_owner_map.contract.json`
  - `docs/notes/advanced_color_library_foundation_phase8r_explaino_structural_root_pack_classification_owner_map_PHASED_PLAN.md`
  - `ui/fractal_binding_surface_v1.ui_schema.json`
  - `ui_app/src/fractal_derived_fields.cpp`
  - `ui_app/src/fractal_family_rules.h`
  - `ui_app/src/fractal_runtime_validation.h`
  - `ui_app/src/runtime_reset.cpp`
  - `ui_app/tests/test_fractal_derived_fields.cpp`
  - `ui_app/tests/test_fractal_family_rules.cpp`
  - `ui_app/tests/test_fractal_runtime_validation.cpp`
  - `ui_app/tests/test_runtime_reset.cpp`
  - `ui_app/tests/test_ui_schema.cpp`
  - `tests/test_fractal_runtime_explaino_escape_variants.py`
- Final ownership answer on this head:
  - `phase8r` proved `phoenix_p_real` stays a different ownership model as a shared phoenix-step memory-term carrier. On the current head, `phase8s` now keeps that answer explicit through a dedicated phoenix-step carrier surface outside canonical `explaino_all`, and `0.0` remains only a neutral memory-term state on the carrier branch, not a canonical Explaino collapse.
  - `explaino_cluster_radius` stays a different ownership model as a root-pack carrier with two divergent runtime roles: root duplication/separation on `explaino_mult` and Laurent-term weighting on `explaino_rational`. It is runtime-real on both carriers, it remains fenced away from canonical `explaino_all`, and `0.0` is not a canonical Explaino collapse for either carrier.
  - The shared structural/root-pack bucket does not stay unified after classification. `phoenix_p_real` follow-up is now closed separately in `phase8s`, so any later structural/root-pack work should target `explaino_cluster_radius` or a new drift bug instead of reopening one shared slice.
- Validation note: `ui_app/build_tests_vsdevcmd.cmd` still ends through the pre-existing `The system cannot find the batch label specified - full_test_run` wrapper tail, so native proof for this slice relies on the rebuilt `D:/salt-fractal/cuda_newton_fractal_clone/build_tests/*.exe` witnesses rather than that wrapper’s final jump.

## Resume Point

Closed. Any later structural/root-pack work should start from this plan, [advanced_color_library_foundation_phase8s_explaino_phoenix_p_real_followup_ownership_fence_PHASED_PLAN.md](/C:/code/cuda_newton_fractal_clone/docs/notes/advanced_color_library_foundation_phase8s_explaino_phoenix_p_real_followup_ownership_fence_PHASED_PLAN.md), and [advanced_color_library_foundation_explaino_structural_root_pack_launch_anchor.md](/C:/code/cuda_newton_fractal_clone/docs/notes/advanced_color_library_foundation_explaino_structural_root_pack_launch_anchor.md), not from the old shared phase8q staging story alone, and it should treat `phoenix_p_real` as closed separate follow-up work on this head.

## Action Hostile Review

- Action ID: action-20260515-explaino-structural-root-pack-classification
- Suspected Failure Mode: the slice centralizes labels and defaults while hidden carrier-specific runtime behavior still owns the real structural answer, or it claims zero/default canonical collapse by analogy.
- Correct Owner/Action: classify `phoenix_p_real` and `explaino_cluster_radius` through one checked-in structural ownership surface plus carrier map, preserve canonical Explaino-all, and split future follow-up when the runtime roles diverge.
- Proof Surface: checked-in RED, focused native and published-runtime witnesses, phased-plan sync, hostile-audit validation, checkpoint commit, validation receipt, contract proof receipt, and committed-head stale-plan reread.
- Blocked Action: no promotion-by-analogy, no dual-seed reopening, no resolved-coupling reopening, no generic advanced-color core reopen, and no historical archive widening in this slice.
