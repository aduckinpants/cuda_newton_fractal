# Advanced Color Library Foundation Phase 8M - Explaino-all Enforcement Guardrails

## Current Phase

Complete - the phase8m enforcement guardrails are landed on this head, the focused native plus published-runtime viewer rails are green, and the earlier blank-capture live blocker was disproven on the same head as a transient live-session visibility/capture-surface failure rather than a stable Explaino product regression.

## Phase Checklist

- [x] Phase 1 - replace the closed phase8l lock with a new slice-3 plan and contract, inspect the owner seams plus duplicated Explaino selector and axis lists, and land RED witnesses that prove a new Explaino axis or supported legacy selector can still land without a fail-closed classification or coverage authority
- [x] Phase 2 - land the smallest truthful enforcement guardrails for the canonical seven-axis family and the supported legacy projection selectors only, keeping deferred parameter classes, cleanup-only work, and historical archive compatibility out of scope
- [x] Phase 3 - rerun the narrowest truthful native, runtime, and viewer witnesses, hostile-audit the repaired state, checkpoint, write machine receipts, reread the committed head for stale plan text, and keep umbrella authority truthful

## Explicit User Asks

- [done] Open and execute Explaino-all slice 3 only: enforcement guardrails for the canonical seven-axis family and its supported legacy projection selectors.
- [done] Prove a new Explaino family axis cannot land without canonical registry coverage.
- [done] Prove a new Explaino alias or preset cannot land without canonical projection coverage.
- [done] Keep canonical neutral and default invariants enforced.
- [done] Preserve slice-1 canonical identity and axis registry truth, slice-2 legacy projection truth, and phase8k sidecar support.
- [done] Keep deferred parameter-class promotion, cleanup-only work, generic advanced-color core reopen, historical archive compatibility, and unrelated tooling out of scope.
- [done] Close only with hostile review, checkpoint commit, validation receipt, contract proof receipt, clean tree, and a committed-head stale-plan reread.

## Presumption Loop

The controlling risk for phase8m was future Explaino-family drift, not reinvention of the already closed slice-1 or slice-2 behavior. The current code now centralizes selector classification and canonical-axis coverage for the bounded seven-axis family, and the repaired helper/test seams derive from that checked-in authority instead of parallel hand-maintained lists.

The final result is fail-closed coverage, not more label churn: selector classification, schema/control coverage, helper coverage, neutral/default invariants, runtime/state preservation, and the live viewer proof witnesses now all bind back to the same canonical authority on this head.

## Presumption Evidence

- `ui_app/src/fractal_family_rules.h` now owns both the canonical seven-axis registry and the selector-classification registry, including explicit roles for canonical public identity, supported single-axis projections, supported multi-axis projection, and legacy non-projection family selectors.
- `ui_app/src/explaino_variant_benchmark.cpp`, `ui_app/src/fractal_probe_runner.cpp`, `ui_app/src/function_descriptor.cpp`, and `ui_app/src/runtime_walk_bootstrap.cpp` now derive their Explaino subsets from that canonical selector and axis authority instead of separate hand-maintained lists.
- `ui_app/tests/test_fractal_family_rules.cpp`, `ui_app/tests/test_ui_schema.cpp`, `ui_app/tests/test_explaino_variant_benchmark.cpp`, `ui_app/tests/test_fractal_probe.cpp`, `ui_app/tests/test_runtime_walk_bootstrap.cpp`, and `ui_app/tests/test_explaino_zero_axis_equivalence.cu` now act as fail-closed guardrails for classifier parity, schema coverage, projection behavior, runtime-walk target coverage, and canonical zero-axis invariants.
- The deterministic published-runtime rails are green on the current head, including the Explaino CLI probe/catalog witnesses, the canonical runtime equivalence nodes, and the parameterized headless paced-loop stop-threshold witness for both `explaino` and `explaino_all`.
- The live viewer rail is green again on the same head: `tests/test_fractal_runtime_explaino_sidecar_live.py` and `tests/test_fractal_runtime_sweep_pause.py` both reran cleanly after the session returned to a discriminating foreground window state, and the runtime automation report/capture probes showed live control rectangles plus rendered foreground pixels without requiring any new product fix.

## Proof Ledger

- Bootstrap on 2026-05-14 proved branch=`feature/advanced-color-pipeline-draft-editor-reframe`, `HEAD=7568e45ae6d9460cdfef6933e72ced8a037aae2e`, clean tree, and an active locked contract on the already closed phase8l legacy-projection slice.
- `py -3.14 tools/viewer_host_repo_status.py` proved `staged=none | unstaged=none | untracked=none`, so this slice started fresh rather than as carryover.
- Mandatory authority reread re-proved that phase8j canonical identity plus axis registry, phase8k sidecar support, and phase8l legacy projection plus preset truth are all closed prior Explaino-all slices and must not become this session’s main work.
- `HANDOFF_LOG.md` entries for `ck:c4b13525`, `ck:835723a0`, and `ck:036b6e68` re-proved the closed checkpoint chain for slice 1, sidecar support, and slice 2.
- Minimal continuity preflight was required before mutation because the active phase8l contract was closed and did not authorize successor plan or contract creation. `py -3.14 tools/viewer_host_revise_contract.py --session-id global_active_contract --cwd . --plan docs/notes/advanced_color_library_foundation_phase8d_restart_authority_repair_PHASED_PLAN.md --contract docs/contracts/advanced_color_library_foundation_phase8d_restart_authority_repair.contract.json` temporarily re-opened the broader docs continuity scope only long enough to create this successor slice truthfully.
- `ui_app/src/fractal_family_rules.h` now owns a canonical Explaino selector registry with explicit roles, plus a derived single-axis projection descriptor surface that binds supported legacy carriers back to the canonical seven-axis authority.
- `ui_app/src/explaino_variant_benchmark.cpp`, `ui_app/src/fractal_probe_runner.cpp`, `ui_app/src/function_descriptor.cpp`, and `ui_app/src/runtime_walk_bootstrap.cpp` now derive their Explaino subset behavior from the canonical selector and axis surfaces instead of hand-maintained legacy lists.
- `ui_app/tests/test_fractal_family_rules.cpp`, `ui_app/tests/test_ui_schema.cpp`, `ui_app/tests/test_explaino_variant_benchmark.cpp`, `ui_app/tests/test_fractal_probe.cpp`, and `ui_app/tests/test_runtime_walk_bootstrap.cpp` now act as fail-closed guardrails for selector classification, single-axis projection coverage, schema visibility coverage, and runtime-walk target coverage.
- Focused compiled preservation witnesses are green on the slice head: `test_fractal_family_rules.exe`, `test_ui_schema.exe`, `test_explaino_variant_benchmark.exe`, `test_schema_binding.exe`, `test_runtime_reset.exe`, `test_diagnostics_state_io.exe`, `test_fractal_probe.exe`, `test_explaino_sidecar_measurement.exe`, `test_runtime_walk_bootstrap.exe`, and `test_explaino_zero_axis_equivalence.exe`.
- `cmd /c ui_app\build_vsdevcmd.cmd` passed on the current slice head and wrote the fresh publish log to `artifacts/validation/phase8m_runtime_publish.log`.
- The focused published-runtime proof bundle passed and wrote `artifacts/validation/phase8m_runtime_proof_bundle.log`. That bundle covered the probe/catalog witnesses, the canonical Explaino runtime equivalence nodes, the parameterized headless paced-loop stop-threshold witness for both `explaino` and `explaino_all`, the full live Explaino sidecar viewer witness file, and the live sweep viewer witness.
- The live sidecar and live sweep rails also passed individually and wrote `artifacts/validation/phase8m_live_sidecar_green.log` and `artifacts/validation/phase8m_live_sweep_green.log`.
- The speculative `ui_app/src/main.cpp` wall-clock fallback added during live-runtime debugging was removed after hostile review because it did not change the live witness outcome and was not truthful slice-3 guardrail evidence.
- The earlier blank/white live-capture blocker was disproven on the same head. `artifacts/validation/live_capture_probe/ui_automation_report.json` proved the Color Pipeline UI automation surface was initialized and emitting real control rectangles, `ui_automation_click_report.json` proved the runtime consumed an automation click, and `focus_probe_after_focus.png` plus `focus_probe_after_topmost.png` proved the current foreground runtime window was rendering real pixels again without any new phase8m product fix. The subsequent green live pytest reruns therefore proved the earlier `diff=0.000` blocker was a transient live-session visibility/capture-surface failure, not a stable Explaino regression in the landed code.

## Hostile Audit

- Status: complete
- Required posture: assume the landed guardrails are lying until the deterministic native/runtime rails and the live viewer rail agree that selector classification, canonical-axis coverage, neutral/default invariants, and preservation seams really fail closed instead of depending on one happy-path test or more hand-maintained lists.

## Audit Passes

- [x] Pass 1 - add RED witnesses that prove the current repo still lacks one fail-closed Explaino selector-classification authority and one fail-closed canonical-axis coverage authority across schema, helper, and runtime-preservation seams.
- [x] Pass 2 - hostile re-read found one real issue after the first GREEN: the live published-runtime proof surface temporarily collapsed into blank/white captures and `diff=0.000` motion witnesses, so the slice stayed open until the same head proved whether that was a product regression or a session-state proof-surface failure.
- [x] Pass 3 - re-read the repaired state after rerunning the focused native, runtime, and live viewer witnesses; no additional real defect found, and the repaired state proved cleanly on the current head.

## Audit Findings

- [closed] The Explaino selector-classification drift gap is repaired: one checked-in selector registry now classifies canonical public identity, supported single-axis projections, supported multi-axis legacy projection, and non-projection legacy family selectors, and the helper/test surfaces named in this slice derive from that authority.
- [closed] The canonical seven-axis coverage drift gap is repaired: schema, benchmark, probe, runtime-walk, neutral/default equivalence, and zero-axis runtime witnesses now fail closed against the canonical axis registry instead of parallel hand-maintained lists.
- [closed] Hostile audit found one real proof-surface issue after the first GREEN: the live published-runtime viewer witnesses temporarily produced blank/white captures and `diff=0.000` motion results. Same-head automation-report probes, foreground capture probes, and green live pytest reruns proved that this was a transient live-session visibility/capture-surface failure rather than a stable phase8m product regression.
- [closed] Clean re-read after the fresh publish, focused native/runtime bundle, and live viewer reruns found no additional real defect in the repaired slice.

## Notes

- Expected owner files for this bounded slice:
  - `docs/contracts/advanced_color_library_foundation_phase8m_explaino_all_enforcement_guardrails.contract.json`
  - `docs/notes/advanced_color_library_foundation_phase8m_explaino_all_enforcement_guardrails_PHASED_PLAN.md`
  - `docs/notes/advanced_color_feature_restart_inventory.md`
  - `docs/notes/advanced_color_library_foundation_PHASED_PLAN.md`
  - `docs/notes/advanced_color_library_foundation_CLOSURE_MATRIX.md`
  - `docs/notes/advanced_color_library_foundation_oracle_and_inventory.md`
  - `docs/notes/advanced_color_library_foundation_explaino_all_launch_anchor.md`
  - `docs/notes/advanced_color_library_foundation_phase8i_explaino_all_canonicalization_staging_PHASED_PLAN.md`
  - `docs/notes/advanced_color_library_foundation_phase8j_explaino_all_canonical_identity_axis_registry_PHASED_PLAN.md`
  - `docs/notes/advanced_color_library_foundation_phase8k_explaino_all_sidecar_support_PHASED_PLAN.md`
  - `docs/notes/advanced_color_library_foundation_phase8l_explaino_all_legacy_variant_projection_preset_truth_PHASED_PLAN.md`
  - `ui/fractal_binding_surface_v1.ui_schema.json`
  - `ui_app/src/fractal_family_rules.h`
  - `ui_app/src/fractal_probe_runner.cpp`
  - `ui_app/src/explaino_variant_benchmark.cpp`
  - the narrow preservation seams under `ui_app/src/schema_binding.cpp`, `ui_app/src/fractal_derived_fields.cpp`, `ui_app/src/runtime_reset.cpp`, `ui_app/src/diagnostics_capture.cpp`, `ui_app/src/diagnostics_state_io.cpp`, `ui_app/src/runtime_walk_headless.cpp`, `ui_app/src/explaino_sidecar_measurement.cpp`, and `ui_app/src/fractal_sample_device.inl` only if a guardrail repair genuinely has to touch them
  - `ui_app/tests/test_fractal_family_rules.cpp`
  - `ui_app/tests/test_ui_schema.cpp`
  - `ui_app/tests/test_explaino_variant_benchmark.cpp`
  - `ui_app/tests/test_explaino_zero_axis_equivalence.cu`
  - the narrow preservation witnesses under `ui_app/tests/test_schema_binding.cpp`, `ui_app/tests/test_runtime_reset.cpp`, `ui_app/tests/test_diagnostics_state_io.cpp`, `ui_app/tests/test_explaino_sidecar_measurement.cpp`, and `tests/test_fractal_runtime_explaino_escape_variants.py`
- Non-goals for this slice:
  - do not reopen slice 1 or slice 2 as the main work
  - do not promote deferred parameter classes into Explaino-all
  - do not make cleanup the main work
  - do not reopen generic advanced-color core work
  - do not reopen historical `234919_563__explaino_inertial` compatibility work
  - do not broaden into hook, crash-recovery, anti-lie tooling, or unrelated UI polish

## Resume Point

Closed slice. Re-open only if later evidence disproves the canonical selector/axis guardrails or the live viewer proof receipts on this committed head.

## Action Hostile Review

- Action ID: action-20260514-explaino-all-slice3-enforcement-guardrails
- Suspected Failure Mode: the slice adds another helper or another positive-only test while a new Explaino axis, selector, or `explaino_all` surface can still land without canonical registry coverage, explicit selector classification, or preservation proof.
- Correct Owner/Action: centralize Explaino selector classification and canonical-axis guardrails in checked-in authority, derive the narrow helper and witness subsets from that authority, and preserve slice-1, slice-2, and phase8k truth without widening into deferred parameter-class promotion or cleanup-only work.
- Proof Surface: checked-in REDs, focused native and runtime preservation witnesses, phased-plan sync, hostile-audit validation, checkpoint commit, validation receipt, contract proof receipt, and committed-head stale-plan reread.
- Blocked Action: no deferred parameter-class promotion, no slice-1 or slice-2 replay as the main work, no cleanup-only sweep, no generic advanced-color core reopen, and no historical archive or workflow-tooling widening in this slice.
