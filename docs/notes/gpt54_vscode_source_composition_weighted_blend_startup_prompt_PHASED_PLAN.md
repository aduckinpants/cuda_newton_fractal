# GPT-5.4 VS Code Source Composition Weighted-Blend Startup Prompt

## Current Phase

Phase 2 complete - the paste-ready startup prompt for generic Source composition via weighted blend is landed, and the stale restart/closure authority that still treated the historical manual archive as the active sprint blocker is repaired so the prompt launches into the real next core-feature lane.

## Phase Checklist

- [x] Phase 1 - create a docs-only slice for the GPT-5.4 VS Code Source composition startup prompt and restate the user ask as a checked-in deliverable
- [x] Phase 2 - write the final paste-ready prompt with explicit anti-lie rules, exact bootstrap reads/commands, weighted-blend scope lock, hostile-review requirements, and closure chain; then repair the stale restart/closure authority so the prompt does not boot into contradictory repo surfaces

## Explicit User Asks

- [done] Implement the next bounded work prompt instead of leaving it in chat.
- [done] Make the anti-lie rule explicit: any agent is assumed to be a liar until machine proof says otherwise.
- [done] Make hostile review explicitly assume the agent, its claims, its tests, and its summaries are guilty by default.
- [done] Target the next large useful core-feature lane: generic Source composition via weighted blend.
- [done] Keep root_index pair-only and keep Balance/Void, ExplainO-BalanceVoid, ExplainO-all, archive UX, and workflow churn out of this slice.
- [done] Repair the stale authority that still treated the historical manual archive as the active sprint blocker for future sessions.

## Presumption Loop

The controlling risk is not missing wording; it is launching the next GPT-5.4 session into stale product authority. A prompt that says "implement weighted blend" while the checked-in restart and closure surfaces still say the historical manual archive is the only active blocker would immediately produce contradiction, scope drift, and more wasted turns. The startup packet is only useful if it does two things at once: hard-lock the future session onto the generic Source composition weighted-blend lane, and remove the stale repo-authority claim that old historical archive compatibility outranks current core feature completion in this moving beta surface.

The cheapest disconfirming path is to reread the restart inventory, foundation plan, closure matrix, and closure-control plan against the user's reprioritization, then reread the final prompt as if a future GPT-5.4 session were trying to substitute archive archaeology, workflow churn, or planning theater for actual Source-composition implementation. If any checked-in authority still points the agent back at the archive lane or leaves weighted-blend semantics ambiguous, the startup packet is not done.

## Presumption Evidence

- `docs/notes/advanced_color_feature_restart_inventory.md` still named the manual `234919_563__explaino_inertial` archive as Priority 0 even after the user explicitly reprioritized the sprint back to core pipeline features.
- `docs/notes/advanced_color_library_foundation_PHASED_PLAN.md` and `docs/notes/advanced_color_library_foundation_CLOSURE_MATRIX.md` still described the historical manual archive as the active blocker, while generic Source composition remained only a deferred semantics lane.
- Live code still proves the real next missing core seam: `ui_app/src/escape_time_coloring.h` resolves exactly one Source scalar and `ui_app/src/color_pipeline_core.h` still exposes single-row Source lane helpers.
- The user explicitly chose weighted blend as the first generic Source mixer and explicitly rejected archive compatibility as the active sprint gate for this slice.

## Proof Ledger

- Authority reread: `docs/notes/advanced_color_feature_restart_inventory.md`, `docs/notes/advanced_color_feature_restart_inventory_PHASED_PLAN.md`, `docs/notes/advanced_color_library_foundation_PHASED_PLAN.md`, `docs/notes/advanced_color_library_foundation_CLOSURE_MATRIX.md`, and `docs/notes/advanced_color_library_foundation_closure_control_PHASED_PLAN.md` were re-read before rewriting the prompt and reprioritizing the restart surfaces.
- Deliverable target: `docs/notes/gpt54_vscode_source_composition_weighted_blend_startup_prompt.md` is the checked-in paste-ready startup prompt for future GPT-5.4 VS Code sessions on the new Source-composition lane.
- Authority repair: the restart inventory and foundation closure docs now describe the historical manual archive as a separate historical-compatibility issue rather than the active sprint-core blocker, and they promote weighted-blend generic Source composition to the next high-priority lane.

## Hostile Audit

- Status: done
- Required posture: assume the first prompt draft is still lying by omission until it proves the anti-lie standard is blunt enough, the Source-composition scope is locked tightly enough, and the repo-authority/bootstrap/closure chain no longer points at the stale archive lane.

## Audit Passes

- [done] Pass 1 - re-read the startup-prompt requirements against the restart inventory, foundation plan, closure matrix, and current code seams to pin the exact next core-feature lane and the exact weighted-blend scope.
- [done] Pass 2 - reread the finished prompt as if a future GPT-5.4 session were trying to reopen archive archaeology, broaden into Balance/Void or ExplainO-all, or replace proof with narrative.
- [done] Pass 3 - hostile-read the repaired restart/closure surfaces against the user's reprioritization; remove the stale archive-first wording and promote weighted-blend Source composition as the truthful next sprint-core lane.
- [done] Pass 4 - clean re-read the final packet as a future-session bootstrap surface; no additional scope leak or stale-authority contradiction remained after the final pass.

## Audit Findings

- [done] Real authority defect found: the checked-in restart and closure docs still told future sessions to treat the old historical manual archive as the active blocker even after the user explicitly reprioritized the sprint back to generic Source composition and remaining core pipeline work.
- [done] Real launch defect found: without a new prompt packet, the repo had no paste-ready GPT-5.4 startup surface for the weighted-blend Source-composition lane, which left the next session vulnerable to drifting back into archive archaeology or planning-only semantics work.
- [done] Clean re-read result: no additional contradiction or scope leak remained after the final pass; the repaired packet keeps the anti-lie rule blunt, treats every claim as guilty by default, promotes weighted-blend Source composition as the next core lane, and keeps archive compatibility, Balance/Void, ExplainO-BalanceVoid, and ExplainO-all out of this slice.

## Notes

- Expected owner files for this docs-only slice:
  - `docs/contracts/gpt54_vscode_source_composition_weighted_blend_startup_prompt.contract.json`
  - `docs/notes/gpt54_vscode_source_composition_weighted_blend_startup_prompt_PHASED_PLAN.md`
  - `docs/notes/gpt54_vscode_source_composition_weighted_blend_startup_prompt.md`
  - `docs/notes/advanced_color_feature_restart_inventory.md`
  - `docs/notes/advanced_color_feature_restart_inventory_PHASED_PLAN.md`
  - `docs/notes/advanced_color_library_foundation_PHASED_PLAN.md`
  - `docs/notes/advanced_color_library_foundation_CLOSURE_MATRIX.md`
  - `docs/notes/advanced_color_library_foundation_closure_control_PHASED_PLAN.md`
  - `HANDOFF_LOG.md`
- Non-goals for this slice:
  - do not edit runtime code
  - do not reopen the historical archive repair itself
  - do not implement weighted-blend Source composition in this docs-only slice
  - do not broaden the startup prompt into a generic multi-lane workflow template

## Resume Point

Validate the weighted-blend Source-composition startup packet, checkpoint it cleanly, and use that prompt directly for the next GPT-5.4 VS Code session on the core Source-composition lane instead of reopening the historical archive thread.
