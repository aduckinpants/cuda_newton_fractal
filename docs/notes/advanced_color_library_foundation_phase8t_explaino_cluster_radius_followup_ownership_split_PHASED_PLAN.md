# Advanced Color Library Foundation Phase 8T - Explaino Cluster Radius Follow-Up Ownership Split

## Current Phase

Complete - the bounded `explaino_cluster_radius` follow-up slice is closed on this head: `explaino_cluster_radius` now has one dedicated split-selector owner surface outside canonical `explaino_all`, the immediate split between `explaino_mult` root-shape and `explaino_rational` Laurent work is explicit, and no separate enforcement follow-up is warranted on this head.

## Phase Checklist

- [x] Phase 1 - replace the closed phase8s lock with a new bounded `explaino_cluster_radius` follow-up contract, inspect the owner seams, and land RED witnesses that prove the repo still lacks the final explicit follow-up answer for this lane and still leaves `explaino_mult` plus `explaino_rational` behind one generic structural surface
- [x] Phase 2 - settle one truthful `explaino_cluster_radius` follow-up answer, keep it outside canonical `explaino_all`, make any required immediate split between `explaino_mult` and `explaino_rational` explicit, and land only the minimal fence/default/reset/runtime/state updates needed to make that answer fail closed
- [x] Phase 3 - rerun the narrowest truthful native plus published-runtime witnesses, hostile-audit the repaired state, checkpoint, write machine receipts, reread the committed head for stale closeout text, and preserve slices 1-3, phase8n, phase8p, phase8r, and phase8s

## Explicit User Asks

- [done] Open and execute one bounded `explaino_cluster_radius` follow-up slice only.
- [done] Keep `phoenix_p_real`, dual-seed / multi-seed, resolved couplings, generic advanced-color core work, manual archive archaeology, and historical archive compatibility out of scope unless one becomes a real blocker.
- [done] Prove which legacy selectors still own `explaino_cluster_radius`, whether it still acts as two divergent runtime roles across `explaino_mult` and `explaino_rational`, and whether any selector still preserves hidden second authority after phase8r.
- [done] Prove whether current default/reset/state behavior is already truthful or still needs tighter fencing.
- [done] Prove whether `explaino_mult` and `explaino_rational` can still share one bounded follow-up answer or must split immediately.
- [done] Close only with hostile review, checkpoint commit, validation receipt, contract proof receipt, clean tree, and a committed-head stale-plan reread.

## Presumption Loop

The controlling risk for phase8t is fake completion by restating phase8r or by forcing `explaino_mult` and `explaino_rational` into one answer because they still share one parameter id. This slice only closes if current code and runtime proof settle the next truthful stop point for `explaino_cluster_radius` itself: a stronger explicit non-promotion fence, a narrower refined ownership surface that still stays outside canonical `explaino_all`, or an immediate split into separate later lanes if the live carrier seams do not truthfully collapse to one answer.

Until the code disproves it, assume the repo still leaves `explaino_cluster_radius` behind one generic structural/root-pack owner surface that is broader than the real `explaino_mult` versus `explaino_rational` answer.

## Proof Ledger

- Bootstrap on 2026-05-15 proved branch=`feature/advanced-color-pipeline-draft-editor-reframe`, `HEAD=87d9cc4`, clean tree, and an active locked contract still pointing at the already-closed phase8s `phoenix_p_real` follow-up slice.
- `py -3.14 tools/viewer_host_repo_status.py` proved `staged=none | unstaged=none | untracked=none`, so this follow-up started as a fresh session rather than dirty carryover.
- Mandatory authority reread re-proved that phase8q is closed staging, phase8r settled only the shared structural/root-pack classification question, phase8s closed `phoenix_p_real` separately, phase8p settled the dual-seed bucket separately, and phase8n settled the resolved couplings bucket separately.
- Current code search before mutation proved `explaino_cluster_radius` still lives only inside `kExplainoStructuralRegistry` and `kExplainoStructuralCarrierRegistry`, even though the live witnesses already diverge: `ui_app/src/fractal_derived_fields.cpp` feeds `explaino_mult` through `SetExplainoRootsForShape(...)`, `ui_app/src/fractal_probe_runner.cpp` and `ui_app/src/fractal_sample_device.inl` feed `explaino_rational` through the Laurent runtime term, and the current defaults stay split at `0.0` versus `0.1`.
- RED landed truthfully: `artifacts/phase8t_red_family_rules.log` captured the focused compile failure on missing `ExplainoClusterRadiusParamSlot`, `kExplainoClusterRadiusRegistry`, `FindExplainoClusterRadiusDescriptor(...)`, and `FindExplainoClusterRadiusSelectorDescriptor(...)`, proving the repo still lacked a dedicated split-selector `explaino_cluster_radius` follow-up surface before implementation.
- `ui_app/src/fractal_family_rules.h` now owns one dedicated `ExplainoClusterRadius` registry plus split selector map for `explaino_cluster_radius`, with `requires_immediate_lane_split=true` on the parameter surface and explicit `explaino_mult_root_shape` versus `explaino_rational_laurent` follow-up lanes on the selector surface.
- `ui_app/src/fractal_derived_fields.cpp`, `ui_app/src/runtime_reset.cpp`, and `ui_app/src/fractal_runtime_validation.h` now route `explaino_cluster_radius` defaults, reset truth, and runtime-domain validation through `ApplyExplainoClusterRadiusDefaults(...)`, `ResetExplainoClusterRadiusValues(...)`, and `FindExplainoClusterRadiusSelectorDescriptor(...)` instead of the old generic structural/root-pack carrier map.
- Focused native witnesses are green from `D:/salt-fractal/cuda_newton_fractal_clone/build_tests`: `test_fractal_family_rules.exe`, `test_ui_schema.exe`, `test_fractal_derived_fields.exe`, `test_runtime_reset.exe`, `test_fractal_runtime_validation.exe`, `test_schema_binding.exe`, and `test_diagnostics_state_io.exe`. The shared helper target `ui_app/build_tests_vsdevcmd.cmd advanced_color_grading_owner` is green, and the remaining cluster witnesses were rebuilt and rerun explicitly because the full helper script still ends through the pre-existing `The system cannot find the batch label specified - full_test_run` tail.
- `py -3.14 tools/viewer_host_run_logged_command.py --label phase8t-runtime-build --log artifacts/phase8t_runtime_build.log -- cmd /c ui_app\build_vsdevcmd.cmd` republished the current runtime successfully to `D:/salt-fractal/cuda_newton_fractal_clone/runtime/fractal_ui.exe`.
- `py -3.14 tools/viewer_host_run_logged_command.py --label phase8t-runtime-pytest --log artifacts/phase8t_runtime_pytest.log -- py -3.14 -m pytest tests/test_fractal_runtime_explaino_escape_variants.py -k "test_explaino_all_neutral_defaults_match_explaino_published_runtime or test_explaino_all_registry_axes_round_trip_and_change_published_runtime_frame or test_explaino_balance_void_neutral_defaults_match_explaino_published_runtime or test_explaino_dual_seed_runtime_behavior_stays_outside_canonical_explaino_all or test_phoenix_p_real_shared_carrier_runtime_classification or test_explaino_cluster_radius_runtime_classification or test_explaino_all_ignores_deferred_cluster_radius_param or test_explaino_legacy_couplings_round_trip_and_neutral_collapse"` passed `14 passed, 30 deselected`, proving slices 1-3, phase8n, phase8p, phase8r, and phase8s all stay intact while the new cluster-radius split answer remains outside canonical `explaino_all`.

## Hostile Audit

- Status: complete
- Required posture: assume the slice is just renaming phase8r, hand-waving default/reset truth, or forcing `explaino_mult` plus `explaino_rational` into one answer until deterministic selector/runtime/default/state proof disproves that suspicion.
- Current hostile result: one real stale-authority defect was found and repaired, and the repaired state survived a clean native plus published-runtime re-read without exposing another real issue.

## Audit Passes

- [x] Pass 1 - land REDs that prove phase8r classification is not yet the final `explaino_cluster_radius` follow-up answer and that at least one owner surface still mixes `explaino_mult` plus `explaino_rational` into one broader structural/root-pack authority.
- [x] Pass 2 - hostile re-read the first GREEN for fake canonical collapse, hidden second authority, or forced unification of the divergent carrier roles.
- [x] Pass 3 - clean re-read the repaired state with the final narrow native and published-runtime witnesses and confirm the committed head does not still contain stale closeout text.

## Audit Findings

- [x] Real missing authority fixed: the repo had only the generic structural/root-pack owner surface for `explaino_cluster_radius`, so the follow-up answer was not fail-closed before this slice.
- [x] Real stale restart-authority defect fixed: checked-in launch surfaces still routed future work to one remaining `explaino_cluster_radius` lane even after the code made the `explaino_mult` versus `explaino_rational` split explicit.
- [x] Clean re-read of the repaired state reran the focused native and published-runtime witnesses and did not expose another real issue after the split-selector repair and restart-authority sync.

## Notes

- Expected owner seams for this slice:
  - `docs/contracts/advanced_color_library_foundation_phase8t_explaino_cluster_radius_followup_ownership_split.contract.json`
  - `docs/notes/advanced_color_feature_restart_inventory.md`
  - `docs/notes/advanced_color_library_foundation_PHASED_PLAN.md`
  - `docs/notes/advanced_color_library_foundation_explaino_all_launch_anchor.md`
  - `docs/notes/advanced_color_library_foundation_explaino_structural_root_pack_launch_anchor.md`
  - `docs/notes/advanced_color_library_foundation_phase8r_explaino_structural_root_pack_classification_owner_map_PHASED_PLAN.md`
  - `docs/notes/advanced_color_library_foundation_phase8s_explaino_phoenix_p_real_followup_ownership_fence_PHASED_PLAN.md`
  - `docs/notes/advanced_color_library_foundation_phase8t_explaino_cluster_radius_followup_ownership_split_PHASED_PLAN.md`
  - `ui/fractal_binding_surface_v1.ui_schema.json`
  - `ui_app/src/diagnostics_capture.cpp`
  - `ui_app/src/diagnostics_state_io.cpp`
  - `ui_app/src/fractal_derived_fields.cpp`
  - `ui_app/src/fractal_family_rules.h`
  - `ui_app/src/fractal_probe_runner.cpp`
  - `ui_app/src/fractal_runtime_validation.h`
  - `ui_app/src/fractal_sample_device.inl`
  - `ui_app/src/fractal_types.h`
  - `ui_app/src/runtime_reset.cpp`
  - `ui_app/src/runtime_walk_headless.cpp`
  - `ui_app/src/schema_binding.cpp`
  - `ui_app/tests/test_diagnostics_state_io.cpp`
  - `ui_app/tests/test_fractal_derived_fields.cpp`
  - `ui_app/tests/test_fractal_family_rules.cpp`
  - `ui_app/tests/test_fractal_probe.cpp`
  - `ui_app/tests/test_fractal_runtime_validation.cpp`
  - `ui_app/tests/test_runtime_reset.cpp`
  - `ui_app/tests/test_schema_binding.cpp`
  - `ui_app/tests/test_ui_schema.cpp`
  - `tests/test_fractal_runtime_explaino_escape_variants.py`
  - `tests/test_fractal_runtime_explaino_runtime_walk.py`

- Final ownership answer on this head:
  - `explaino_cluster_radius` stays outside canonical `explaino_all` as one dedicated split-selector owner surface with one parameter registry and two explicit selector lanes.
  - `explaino_mult` owns the `explaino_mult_root_shape` lane, where `explaino_cluster_radius` remains runtime-real through root duplication/separation and resets to the carrier default `0.0` without collapsing to canonical Explaino.
  - `explaino_rational` owns the `explaino_rational_laurent` lane, where `explaino_cluster_radius` remains runtime-real through Laurent-term weighting and resets to the carrier default `0.1` without collapsing to canonical Explaino.
  - Any future work must split immediately between those two later lanes instead of reopening one unified `explaino_cluster_radius` packet.
- Validation note: `ui_app/build_tests_vsdevcmd.cmd` still ends through the pre-existing `The system cannot find the batch label specified - full_test_run` wrapper tail, so native proof for this slice relies on the focused `advanced_color_grading_owner` helper target plus rebuilt `D:/salt-fractal/cuda_newton_fractal_clone/build_tests/*.exe` witnesses rather than that wrapper's final jump.

## Closeout State

Any later structural/root-pack work on this head must choose one explicit `explaino_mult` root-shape or `explaino_rational` Laurent lane instead of reopening `phoenix_p_real`, dual-seed, resolved couplings, or a unified `explaino_cluster_radius` slice. No separate enforcement follow-up is warranted on this head.

## Action Hostile Review

- Action ID: action-20260515-explaino-cluster-radius-followup-ownership-split
- Suspected Failure Mode: the slice keeps `explaino_cluster_radius` behind a generic structural/root-pack owner surface, forces `explaino_mult` plus `explaino_rational` into one answer by analogy, or claims `0.0` is canonical Explaino collapse without carrier proof.
- Correct Owner/Action: prove one explicit truthful `explaino_cluster_radius` follow-up answer, keep it outside canonical `explaino_all`, and either refine or split the owner surface exactly where the `explaino_mult` versus `explaino_rational` runtime/default seams demand it.
- Proof Surface: checked-in RED, focused native and published-runtime witnesses, phased-plan sync, hostile-audit validation, checkpoint commit, validation receipt, contract proof receipt, and committed-head stale-plan reread.
- Blocked Action: no `phoenix_p_real` main work, no dual-seed reopening, no resolved-coupling reopening, no generic advanced-color core reopen, and no historical archive widening in this slice.
