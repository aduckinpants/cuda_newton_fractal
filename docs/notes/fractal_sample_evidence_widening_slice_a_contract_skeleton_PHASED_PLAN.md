# Fractal Sample Evidence Widening Slice A - Contract Skeleton

## Current Phase

Complete - slice A closed on its own head with one bounded `FractalSampleEvidence` payload plus one explicit `BuildLegacySampleResult(...)` helper in `ui_app/src/fractal_sample_result.h` while legacy caller behavior stayed unchanged. Later slice B and slice C on this branch added the widened host API/default evidence path and the first paired-state/counterfactual consumer, so this plan is historical proof of the contract-skeleton stop point rather than the live branch status surface.

## Phase Checklist

- [x] Phase 1 - replace the stale active lock with this slice-A plan/contract, re-prove the RED head lacked `FractalSampleEvidence`, `BuildLegacySampleResult(...)`, and any widened sample API beside `SampleFractalPoints(...)`, and pin the bounded owner seams.
- [x] Phase 2 - land one bounded widened evidence payload plus one explicit legacy projection helper while preserving `FractalSampleResult` and `SampleFractalPoints(...)` as the shipped legacy surfaces.
- [x] Phase 3 - repair umbrella sample-evidence doc truth, rerun the narrow validation ladder, complete hostile audit, checkpoint, write receipts, reread the committed head for stale closeout text, and keep the repo clean.

## Explicit User Asks

- [done] Open and execute sample-evidence widening slice A only: contract skeleton plus legacy projection helper.
- [done] Preserve `FractalSampleResult` as the shipped legacy result surface and keep `SampleFractalPoints(...)` truthful and unchanged for current callers.
- [done] Do not ship dual-return API wiring, widened-consumer work, renderer replacement, whole-engine generic refactor, meta-basin, operator-itinerary, DSL/program-space, generic advanced-color reopen, or historical `234919_563__explaino_inertial` archaeology.
- [done] Close only with commit, validation receipt, contract proof receipt, clean tree, and a committed-head stale-plan reread.

## Proof Ledger

- Bootstrap on 2026-05-15 re-proved branch=`feature/fractal-sample-evidence-widening`, `HEAD=09c233d`, clean tree, and an active locked contract on the already closed unrelated `advanced_color_library_foundation_phase8t_explaino_cluster_radius_followup_ownership_split` slice.
- Relevant authority reread re-proved `ck:4b8ae91d` staging is closed repo doctrine only and must not become this session's main mutation surface.
- Current live seam proof: `ui_app/src/fractal_sample_result.h` still defines the only checked-in fractal sample payload, `ui_app/src/fractal_sample_core.cu` still returns and copies only `FractalSampleResult`, `ui_app/src/fractal_types.h` still exposes only `SampleFractalPoints(..., FractalSampleResult*, ...)`, and the renderer, sidecar-measurement, and runtime-walk callers still consume `FractalSampleResult` directly.
- Minimal continuity preflight was required because the stale active lock did not truthfully authorize successor sample-evidence plan/contract creation; the broader docs continuity contract was re-locked only long enough to open this slice-A surface.
- Checked-in RED landed in `artifacts/validation/fractal_sample_evidence_slice_a_red_build.log`: the helper build first failed at `ui_app/tests/test_fractal_sample_result.cpp` because `FractalSampleEvidence` and `BuildLegacySampleResult(...)` did not exist on the RED head.
- GREEN implementation now lives in `ui_app/src/fractal_sample_result.h`: `FractalSampleEvidence` adds one bounded `sample_coord` field beside nested `legacy_result`, and `BuildLegacySampleResult(...)` projects straight back to the unchanged legacy result surface.
- `artifacts/validation/fractal_sample_evidence_slice_a_green_build.log` proves the focused contract helper test is green on the repaired state: `test_fractal_sample_result: 39 passed, 0 failed`, including the default-value witness, the projection round-trip witness, and the `SampleFractalPoints(...)` legacy-signature witness.
- The same green build log also proves the slice stayed bounded: the widened evidence work passed before the broader helper lane later failed at unrelated `test_safe_mode_schema` coverage (`TestSafeModeSchemaKeepsGroupedDefaults_DefaultExplaino`), so slice-A closure stays on the narrower contract/unit seam instead of pretending the whole helper suite is green.
- A direct rerun of `D:\salt-fractal\cuda_newton_fractal_clone\build_tests\test_fractal_sample_result.exe` stayed green after the umbrella-doc repair, confirming the repaired state still has 39 passing checks on the bounded sample-result seam.

## Hostile Audit

- Status: complete
- Required posture: assume the slice only renames the legacy result, accidentally widens into slice B or slice C, smuggles in a generic-engine rewrite, or leaves stale closeout text until hostile proof disproves each failure mode.
- Current hostile result: two real defects were found and repaired during the slice, and the clean re-read of the repaired state did not find any additional real defect.

## Audit Passes

- [x] Pass 1 - RED build proof confirmed the current head initially lacked `FractalSampleEvidence` and `BuildLegacySampleResult(...)`, while the live sample API and current callers still stayed on `FractalSampleResult`.
- [x] Pass 2 - hostile review found and repaired the first real workflow defect: the initial slice-A contract named a non-existent future header path and therefore could not lock truthfully under contract validation.
- [x] Pass 3 - clean re-read of the repaired state confirmed `FractalSampleResult` still remains the shipped legacy surface, `SampleFractalPoints(...)` still keeps the legacy signature, slice B/C work did not land by inertia, and no additional real defect was found.

## Audit Findings

- [x] Real workflow defect fixed: the first slice-A contract draft scoped a non-existent `ui_app/src/fractal_sample_evidence.h` path and therefore failed contract validation; the widened authority was re-bounded to the existing `ui_app/src/fractal_sample_result.h` seam before implementation continued.
- [x] Real doc-truth defect fixed: the launch anchor and staging packet still claimed the current repo had no `FractalSampleEvidence` or `BuildLegacySampleResult(...)` after the GREEN implementation landed.
- [x] Clean re-read evidence: the repaired state still keeps `SampleFractalPoints(...)` as the only shipped sample API, still keeps the widened payload bounded to `sample_coord` plus nested `legacy_result`, and no additional real defect was found.

## Notes

- Current single-orbit authority stays in `ui_app/src/fractal_sample_result.h`, `ui_app/src/fractal_sample_core.cu`, `ui_app/src/fractal_types.h`, `ui_app/src/fractal_renderer.cu`, `ui_app/src/explaino_sidecar_cuda_sample_host.cpp`, `ui_app/src/explaino_sidecar_measurement.cpp`, and `ui_app/src/runtime_walk_field_slime.cpp` until a later slice proves a widened API or non-legacy consumer.
- No current required caller already assumes richer evidence informally: sidecar measurement and runtime-walk aggregate only the current `FractalSampleResult` fields, and the renderer consumes the same legacy result immediately for pixel mapping.
- The widened-seam authority for this slice stays bounded to `ui_app/src/fractal_sample_result.h` with one `FractalSampleEvidence` type plus one `BuildLegacySampleResult(...)` helper that projects back to `FractalSampleResult`.
- Exit criteria satisfied on the bounded seam: one widened evidence type exists beside `FractalSampleResult`, one explicit legacy projection helper exists, current legacy result semantics stay intact, and slice B/C remain out of scope.
- Validation ladder used for closeout: `ui_app/build_tests_vsdevcmd.cmd` (focused seam observed green before an unrelated later-suite failure), `D:\salt-fractal\cuda_newton_fractal_clone\build_tests\test_fractal_sample_result.exe`, `py -3.14 tools/viewer_host_validate_slice_contract.py --contract docs/contracts/fractal_sample_evidence_widening_slice_a_contract_skeleton.contract.json --out-json artifacts/validation/fractal_sample_evidence_widening_slice_a_contract_skeleton_contract.json`, `py -3.14 tools/viewer_host_assert_phased_plan_sync.py`, and `py -3.14 tools/viewer_host_validate_hostile_audit.py --plan docs/notes/fractal_sample_evidence_widening_slice_a_contract_skeleton_PHASED_PLAN.md --out-json artifacts/validation/fractal_sample_evidence_widening_slice_a_contract_skeleton_hostile_audit.json`.
