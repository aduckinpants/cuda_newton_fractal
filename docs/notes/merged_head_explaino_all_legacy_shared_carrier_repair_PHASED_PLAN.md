# Merged-Head Explaino-All Legacy Shared-Carrier Repair

## Current Phase

Closed - the repaired shared-carrier head is validated, checkpointed, pushed, and stale-plan clean.

## Phase Checklist

- [x] Phase 1 - prove the current merged-head `explaino_all` runtime/schema/test model contradicts the pre-`4fc5aed` shared-carrier product contract, open focused REDs for that gap, and lock this new repair slice.
- [x] Phase 2 - restore the `explaino_all` shared seven-axis runtime composition and additive shared control surface without undoing explicit selector identity or deferred non-axis exclusions.
- [x] Phase 3 - repair the focused native, schema, sidecar/probe, runtime, and smooth-escape fast-path proofs so they enforce the restored merged contract instead of the broken neutral-umbrella rewrite.
- [x] Phase 4 - hostile-audit the repaired head, rerun the focused native/runtime proof rails, checkpoint, write receipts, push `master`, and clear stale-plan text.

## Explicit User Asks

- [closed] Restore `explaino_all` to the pre-`4fc5aed` seven-axis shared-carrier product contract while preserving the later explicit selector identity repair.
- [closed] Keep the seven legacy Explaino axis controls visible on both `explaino_all` and their explicit owner lanes instead of leaving `explaino_all` inert or hiding the shared surface.
- [closed] Preserve the bounded smooth-escape `tier_auto` usability/perf fix on the repaired `explaino_all` lane as well as the explicit owner lanes.
- [closed] Keep deferred non-axis Explaino surfaces such as dual-seed, cluster-radius, phoenix-step carriers, and couplings outside canonical `explaino_all` ownership unless separate historical proof says otherwise.
- [closed] Close only with focused native proof, runtime publish, published-runtime proof, hostile audit, checkpoint commit, receipts, push, clean tree, and stale-plan cleanup.

## Proof Ledger

- Bootstrap on 2026-05-18 proved branch=`master`, `HEAD=1b3ad3d`, clean tree, `origin/master...HEAD = 0 0`, and the active locked contract still points at the already-closed `merged_head_explaino_control_surface_continuation_repair` slice.
- Current merged head still hard-codes the broken neutral-umbrella rewrite in `ui_app/src/fractal_family_rules.h`: `ResolveExplainoRuntimeFractalType(explaino_all, ...)` returns baseline `explaino` regardless of the seven shared legacy axis params, while explicit selectors remain explicit in `ResolveExplainoPublicFractalType(...)`.
- The current shipped schema keeps the seven legacy axis controls only on the explicit owner lanes, while the pre-`4fc5aed` schema showed those same controls on `explaino_all` plus their owner lane.
- The current runtime/product tests still encode the broken neutral-umbrella model: `tests/test_fractal_runtime_explaino_escape_variants.py` asserts `explaino_all` registry-axis edits must leave the published runtime frame unchanged, while collateral rails in family-rules, sidecar measurement, zero-axis equivalence, probe, and UI schema also assert that `explaino_all` does not own the seven-axis carrier surface.
- Pre-`4fc5aed` repo authority proves the intended shared-carrier behavior: `ResolveExplainoRuntimeFractalType(explaino_all, ...)` routed nonzero shared axes into real legacy runtime carriers, `ui/fractal_binding_surface_v1.ui_schema.json` surfaced those seven controls on `explaino_all`, and the published-runtime tests required `explaino_all` axis edits to change the frame.
- Current selector-identity and reset/load seams no longer canonicalize explicit owner lanes back to `explaino_all`; those repairs must stay in place while the shared `explaino_all` carrier is restored.
- Current sample-tier protection already preserves the smooth-escape `tier_auto` float32 path for explicit owner-active legacy Explaino projection selectors; the repaired slice must extend that same fast-path proof to `explaino_all` when its active shared axis state resolves onto the same runtime seam.
- Focused RED proof landed before the fix: `artifacts/logs/explaino_all_legacy_shared_carrier_red_native.log` caught the broken `ResolveExplainoRuntimeFractalType(explaino_all, ...)` precedence seam, and `artifacts/logs/explaino_all_legacy_shared_carrier_red_runtime_pytest.log` proved the published runtime schema plus frame ownership still encoded the neutral-umbrella lie.
- The landed repair restores the shared contract in the narrow seams only: `ui_app/src/fractal_family_rules.h` routes `explaino_all` onto the legacy single-axis carrier first, then `explaino_balance_void`, then baseline `explaino`; `ui_app/src/ui_schema.cpp` plus `ui/fractal_binding_surface_v1.ui_schema.json` restore additive `visible_if/op=in` shared-axis visibility on `explaino_all` plus the owner lane; and `ui_app/src/sample_tier_resolver.cpp` preserves the `smooth_escape` float32 fast path when canonical `explaino_all` resolves onto a legacy projection runtime seam.
- Collateral/native contract rails were repaired to encode truthful shared ownership instead of the broken neutral-umbrella rewrite: UI schema, family rules, function-descriptor unit/CLI, sidecar measurement, zero-axis equivalence, sample-tier resolver, and published-runtime explaino tests now prove shared-axis ownership on `explaino_all`, preserve explicit selector identity, preserve hidden-axis isolation on explicit lanes, and keep deferred non-axis families outside canonical ownership.
- Focused rebuilt native proof is green in `artifacts/logs/explaino_all_legacy_shared_carrier_targeted_native.log`: `test_fractal_family_rules`, `test_ui_schema`, `test_runtime_reset`, `test_function_descriptor`, `test_explaino_sidecar_measurement`, `test_sample_tier_resolver`, and `test_explaino_zero_axis_equivalence` all passed after rebuilding the touched executables with the repo script toolchain flags.
- The broad `ui_app/build_tests_vsdevcmd.cmd` batch still overruns the interactive timeout on this machine, so the truthful native proof for this slice is the exact touched helper rail rebuild plus the full-run partial log in `artifacts/logs/explaino_all_legacy_shared_carrier_native_postfix.log`, not a false claim that the whole batch completed.
- Runtime publish is green in `artifacts/logs/explaino_all_legacy_shared_carrier_runtime_build.log`; the staged runtime activated `D:\salt-fractal\cuda_newton_fractal_clone\runtime\fractal_ui.exe` with the repaired schema copy beside it.
- Focused published-runtime proof is green in `artifacts/logs/explaino_all_legacy_shared_carrier_runtime_pytest.log`: `19 passed in 9.85s` across shared-axis schema visibility, `explaino_all` frame ownership, deferred non-axis exclusions, descriptor truth, callable-engine adversarial smoke, and the repaired smooth-escape backend/usability witnesses.
- Concrete smooth-escape usability witness is recorded in `artifacts/logs/explaino_all_legacy_shared_carrier_perf_witness.log`: median wall times were approximately `explaino=0.1999s`, `explaino_all=0.2300s`, `explaino_ripple=0.2189s`, `explaino_splice=0.2132s`, `explaino_tension=0.2206s`, and `explaino_vortex=0.1956s`, which stays within the bounded runtime envelope enforced by the published-runtime test.
- `SampleFractalPoints(...)` behavior stayed untouched; this slice only repaired selector/control routing, schema visibility, sample-tier routing, and proof surfaces around the existing runtime entrypoint.

## Hostile Audit

- Status: complete
- Required posture: assume `explaino_all` runtime ownership, shared control visibility, selector identity, smooth-escape perf preservation, deferred non-axis exclusions, tests, plan text, and my own summary are wrong until machine proof says otherwise.
- Hostile review questions:
  Did I actually restore the pre-`4fc5aed` shared `explaino_all` runtime carrier instead of only moving UI visibility around?
  Did I actually preserve explicit selector identity while restoring the shared carrier?
  Did I actually restore the seven-axis controls additively on `explaino_all` plus owner lanes, not just on one side?
  Did I actually keep deferred non-axis Explaino families out of canonical `explaino_all` ownership?
  Did I actually preserve the smooth-escape `tier_auto` fast path on the repaired `explaino_all` lane?
  Did I actually keep `SampleFractalPoints(...)` unchanged and avoid unrelated engine drift?
  Did I actually stop with truthful closeout text and a stale-plan-clean final head?

## Audit Passes

- [closed] Pass 1 - add/restore focused REDs for `explaino_all` shared runtime routing, additive shared control visibility, and `tier_auto` perf behavior.
- [closed] Pass 2 - clean re-read the repaired native/runtime proof rails after the zero-axis-equivalence fix; no additional real defect found in the touched shared-carrier seams.
- [closed] Pass 3 - clean re-read the repaired runtime/schema/collateral/closeout surfaces and confirmed the repaired state without exposing another real issue.

## Audit Findings

- [closed] Hostile audit caught one real remaining lie after the first repair pass: `ui_app/tests/test_explaino_zero_axis_equivalence.cu` had been over-corrected to require false explicit-lane parity for `explaino_all`, and it also incorrectly expected BalanceVoid perturbations to change runtime even when a single-axis carrier was already active. The targeted rebuilt native rail disproved both assumptions; the test now proves the truthful contract instead: shared-axis `explaino_all` differs from its own neutral baseline, explicit lanes still ignore hidden foreign axes, and single-axis precedence still blocks hidden BalanceVoid from retaking runtime ownership.

## Notes

- Exit criteria: restore the pre-`4fc5aed` seven-axis `explaino_all` shared-carrier behavior without undoing explicit selector identity, keep deferred non-axis ownership unchanged, preserve the smooth-escape fast path, and close with receipts on a clean pushed head.
- Validation target: focused native helper rails plus runtime publish and focused published-runtime pytest on the repaired `explaino_all` shared lane.
