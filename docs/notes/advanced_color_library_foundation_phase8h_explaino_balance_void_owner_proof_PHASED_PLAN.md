# Advanced Color Library Foundation Phase 8H - ExplainO-BalanceVoid Owner Proof

## Current Phase

Closed - ExplainO-BalanceVoid owner proof is implemented on this head as one bounded family-track identity with dedicated family axes, neutral Explaino collapse, persistence/reset truth, and published-runtime proof while the shipped generic `balance_void_grade` row remains unchanged as generic core.

## Phase Checklist

- [x] Phase 1 - create and lock the ExplainO-BalanceVoid plan/contract, inspect the owner seams, and add REDs proving the separate family identity, neutral-default collapse, persistence/reset truth, and generic-core regression boundary are still unproven on current HEAD
- [x] Phase 2 - land the smallest truthful ExplainO-BalanceVoid family-track owner path with dedicated family controls, runtime-real math, schema/runtime visibility, diagnostics persistence, and reset/default behavior while keeping `balance_void_grade` generic
- [x] Phase 3 - validate through the phase8c ladder where it still discriminates the shipped generic grading boundary, run the targeted family-track native/runtime witnesses, hostile-audit the repaired state, checkpoint, write receipts, and clear the stale-plan gate on the committed head

## Explicit User Asks

- [closed] Ship one bounded ExplainO-BalanceVoid family track as the first later family slice after the shipped generic core.
- [closed] Keep `balance_void_grade` inside the already shipped generic grading boundary; do not reopen generic color-pipeline debt.
- [closed] Keep `Explaino-all` out of this session.
- [closed] Keep historical `234919_563__explaino_inertial` archaeology and old archive compatibility out of this slice.
- [closed] Use one meta-family identity with neutral-default axes `balance_void`, `symmetry_tension`, and `field_curvature` instead of widening into multiple public fractal identities.
- [closed] Prove neutral defaults collapse exactly or near-exactly back to original ExplainO with machine evidence.
- [closed] Prove persistence/save-load, reset/default behavior, runtime identity, and focused published-runtime behavior all agree before closure.
- [closed] Keep the shipped generic grading rows and baseline ExplainO behavior regression-free.

## Presumption Loop

The controlling risk was family-track fraud: landing a label or editor path without a runtime-real family identity, neutral-collapse proof, or truthful persistence/reset ownership. This slice closes only because the repo now proves a separate ExplainO-BalanceVoid runtime path with dedicated family controls, while the shipped generic grading operator remains untouched as generic core.

## Presumption Evidence

- `docs/notes/advanced_color_library_foundation_oracle_and_inventory.md` separates the generic `balance_void_grade` operator from the later ExplainO-BalanceVoid family track and recommends the family controls `balance_void`, `symmetry_tension`, and `field_curvature`.
- `docs/notes/advanced_color_feature_restart_inventory.md`, `docs/notes/advanced_color_library_foundation_PHASED_PLAN.md`, and `docs/notes/advanced_color_library_foundation_CLOSURE_MATRIX.md` all say the generic grading core already shipped through `balance_void_grade`; this slice does not relitigate that boundary.
- Current code now carries a separate `FractalType::explaino_balance_void`, dedicated family-axis params, a dedicated sample/probe runtime branch, dedicated schema/runtime visibility, diagnostics save/load coverage, reset/default coverage, neutral-collapse proof, and published-runtime proof.
- The shipped generic grading owners `color_balance_void`, `color_chroma_tension`, and `color_accent_bias` remain the balance-void grading row seam; they were not repurposed as family controls.

## Proof Ledger

- Bootstrap on 2026-05-14 reported `branch=feature/advanced-color-pipeline-draft-editor-reframe`, `HEAD=cd0f248`, a clean worktree, and an active lock on the already closed `advanced_color_pipeline_followup_ux_truth_repair` contract.
- `py -3.14 tools/viewer_host_repo_status.py` reported `staged=none | unstaged=none | untracked=none`, so this session started fresh rather than as carryover.
- Minimal preflight cleared the closed follow-up contract lock, then `py -3.14 tools/viewer_host_begin_work_slice.py --intent "ExplainO-BalanceVoid owner proof" --profile checkpoint --plan docs/notes/advanced_color_library_foundation_phase8h_explaino_balance_void_owner_proof_PHASED_PLAN.md --contract docs/contracts/advanced_color_library_foundation_phase8h_explaino_balance_void_owner_proof.contract.json` opened the bounded slice with checkpoint token `ck:e0b9be75`.
- Focused owner-seam REDs were added first across family rules, schema binding, diagnostics state I/O, runtime reset, zero-axis equivalence, and the runtime pytest witness surface.
- `cmd /c ui_app\build_tests_vsdevcmd.cmd advanced_color_grading_red` is green on the repaired head, covering the family-id/schema seam without reopening generic grading debt.
- `cmd /c ui_app\build_tests_vsdevcmd.cmd advanced_color_grading_owner` is green on the repaired head, covering schema, diagnostics state I/O, finding archive actions, and runtime reset for the new family axes.
- A direct zero-axis witness compiled and ran the checked-in `test_explaino_zero_axis_equivalence.cu` command bundle because the broad full-test batch surface reused a stale binary and then tripped a pre-existing control-flow issue before reaching a truthful current-head witness. Current result: `test_explaino_zero_axis_equivalence: 123 passed, 0 failed`, including exact zero-axis collapse for `explaino_balance_void` and nonzero-axis perturbation (`balance_void_axes flag=0 basin=6745 iter=55699 final_z=6745`).
- `cmd /c ui_app\build_vsdevcmd.cmd` published the current runtime successfully; active runtime is `D:\salt-fractal\cuda_newton_fractal_clone\runtime\fractal_ui.exe`.
- `py -3.14 tools/viewer_host_runtime_pytest_lane.py tests/test_fractal_runtime_explaino_escape_variants.py -k explaino_balance_void --junitxml artifacts/pytest/test_fractal_runtime_explaino_balance_void.junit.xml` passed with `2 passed, 28 deselected`, proving exact neutral collapse back to published `explaino` and a measurable published-frame delta for nonzero family axes.

## Hostile Audit

- Status: complete
- Result: no post-fix product regression was found after re-reading the family identity, runtime math, defaults, persistence/reset ownership, runtime witness, and closeout surfaces under a distrust-first posture.

## Audit Passes

- [x] Pass 1 - add and run REDs that fail because ExplainO-BalanceVoid is still planning-only on current HEAD.
- [x] Pass 2 - after the first green pass, re-read the touched seams as if the work only added labels or editor controls without real runtime behavior.
- [x] Pass 3 - rerun the generic grading regression ladder plus the targeted family-track native/runtime witnesses and look for neutral-collapse lies, persistence drift, or accidental widening into `Explaino-all`.
- [x] Pass 4 - re-read the committed-plan text shape ahead of checkpoint, remove stale closeout language, and keep the committed head free of stale pre-closeout checkpoint language.

## Audit Findings

- [closed] Primary product gap fixed: ExplainO-BalanceVoid previously existed only as planning intent; the repo now carries a separate runtime-real family identity with dedicated axes, neutral collapse, persistence/reset coverage, and published-runtime proof.
- [closed] Clean re-read of the repaired state found no additional real defect after the runtime, persistence, reset, and published-runtime proofs were rerun.
- [observed] Pre-existing workflow defect outside this slice: the broad `ui_app/build_tests_vsdevcmd.cmd` full-run surface did not provide a trustworthy current-head zero-axis witness in this session, so the slice used the exact checked-in zero-axis NVCC command bundle directly instead of broadening into build-tooling repair.

## Notes

- Owner split kept truthful in this slice:
  - family-track identity and controls: `FractalType::explaino_balance_void`, `balance_void`, `symmetry_tension`, `field_curvature`
  - generic grading identity and controls: `balance_void_grade`, `color_balance_void`, `color_chroma_tension`, `color_accent_bias`
- Runtime math seam added only where the family identity is sampled/probed:
  - `ui_app/src/fractal_sample_device.inl`
  - `ui_app/src/fractal_probe_runner.cpp`
- Persistence/reset/runtime proof seams updated for the family track:
  - `ui_app/src/schema_binding.cpp`
  - `ui_app/src/diagnostics_capture.cpp`
  - `ui_app/src/diagnostics_state_io.cpp`
  - `ui_app/src/runtime_reset.cpp`
  - `ui_app/tests/test_schema_binding.cpp`
  - `ui_app/tests/test_diagnostics_state_io.cpp`
  - `ui_app/tests/test_runtime_reset.cpp`
  - `ui_app/tests/test_explaino_zero_axis_equivalence.cu`
  - `tests/test_fractal_runtime_explaino_escape_variants.py`
- Non-goals remained locked:
  - no `Explaino-all`
  - no historical archive compatibility reopening
  - no generic grading/core reopen
  - no hook, crash-recovery, or unrelated UI-polish work

## Re-entry Note

Closed on this slice contract. If a future session reopens ExplainO family work, start from the checkpoint/handoff entry for `ck:e0b9be75`, the receipts written for the committed head, and the current owner split between the family axes and the shipped generic grading row.

## Action Hostile Review

- Action ID: action-20260514-explaino-balance-void-owner-proof-1
- Suspected Failure Mode: the slice lands only a new label or a reuse of generic grading controls, while runtime math, neutral-collapse proof, persistence, or reset truth remain missing.
- Correct Owner/Action: keep the family identity on its dedicated runtime/persistence/reset seams and keep the grading row on the generic color-pipeline owners.
- Proof Surface: focused native tests, targeted ExplainO family runtime witnesses, runtime publish, contract validation, phased-plan sync, hostile-audit validation, checkpoint commit, validation receipt, contract proof receipt, and the post-closeout stale-plan reread.
- Blocked Action: no `Explaino-all`, no archive-compatibility reopening, and no generic color-pipeline reopen in this slice.
