# Explaino Dual-Seed / Multi-Seed Launch Anchor

Use this file as the checked-in authority surface for the closed dual-seed / multi-seed deferred-class packet covering `explaino_seed_b` and `explaino_mix`.

Purpose:

- keep future prompts short without making them underspecified
- force kickoff through checked-in repo authority instead of chat reconstruction
- preserve the bounded dual-seed / multi-seed scope fence even if the exact kickoff wording changes later
- preserve the closed dual-seed ownership answer without leaving this packet misadvertised as the next default lane after phase8p

## Non-Negotiable Identity Rules

- This lane is not a generic `Explaino-all` cleanup pass. It is one bounded deferred-class classification/promotion lane for `explaino_seed_b` and `explaino_mix` only.
- The current canonical seven-axis `Explaino-all` registry remains shipped and intact unless this lane proves a truthful extension path.
- Secondary-seed and blend semantics are not neutral-at-zero by assumption. They must be proven.
- If dual-seed semantics cannot truthfully project through canonical `Explaino-all` state, they must remain legacy-only or move to a different ownership model.
- Do not force dual-seed / multi-seed semantics into the existing seven-axis model by analogy.

## Scope Fence

Future dual-seed / multi-seed slices may:

- classify `explaino_seed_b` and `explaino_mix`
- prove whether either or both can project through canonical `Explaino-all` ownership
- preserve legacy-only or different-ownership answers when the proof does not support promotion
- add bounded enforcement follow-up only if a truthful promotion lands first

Future dual-seed / multi-seed slices must not:

- reopen shipped generic advanced-color core work
- reopen historical `234919_563__explaino_inertial` compatibility work unless explicitly reclassified
- auto-promote structural / root-pack modifiers
- auto-reopen the resolved couplings bucket
- treat this lane as a reason to skip proof, stale-plan gates, or slice contracts

## Staged Slice Order

Follow this order unless live repo evidence proves a narrower truthful split is better:

1. classification + ownership answer
2. bounded promotion only if proof supports it
3. enforcement follow-up only if a promotion actually lands
4. optional cleanup only if the first two slices leave truthful residual debt

Do not start with enforcement before ownership/classification truth exists.
Do not start with cleanup before a promoted path or explicit legacy fence exists.

## Group Under Review

The bounded deferred bucket for this packet is only:

- `explaino_seed_b`
- `explaino_mix`

The key questions this lane must answer are:

- do these parameters have real runtime effect
- are they neutral-at-zero in a way compatible with canonical Explaino behavior
- do they compose as canonical `Explaino-all` axes on one public family identity
- or do they remain legacy-only / different-ownership because second-seed semantics are intrinsically separate

## Explicit Out-Of-Scope Buckets

Do not widen this lane into:

- structural / root-pack modifiers
  - `phoenix_p_real`
  - `explaino_cluster_radius`
- resolved legacy coupling ownership
  - `momentum_beta`
  - `joy_coupling`
  - `fold_coupling`
  - `bell_coupling`

If one of those groups proves to be a real blocker, stop and report it instead of smuggling it into this lane.

## Required Owner Seams

Every future dual-seed / multi-seed kickoff must inspect and name the current ownership seams before mutation:

- `ui/fractal_binding_surface_v1.ui_schema.json`
- `ui_app/src/fractal_types.h`
- `ui_app/src/fractal_derived_fields.cpp`
- `ui_app/src/schema_binding.cpp`
- `ui_app/src/enum_id_utils.h`
- `ui_app/src/runtime_reset.cpp`
- `ui_app/src/diagnostics_capture.cpp`
- `ui_app/src/diagnostics_state_io.cpp`
- `ui_app/src/fractal_family_rules.h`
- `ui_app/src/fractal_sample_device.inl`
- `ui_app/src/fractal_probe_runner.cpp`
- the current narrowest truthful Explaino runtime/viewer witnesses under `ui_app/tests/` and `tests/`

Kickoff must explicitly report:

- which current legacy selectors own `explaino_seed_b` and `explaino_mix`
- whether each parameter already has real runtime effect
- whether zero/default behavior collapses to canonical Explaino
- whether the lane can stay bounded without widening into structural modifiers
- what the provisional ownership models are before RED turns into proof

## Mandatory Kickoff Floor

Every future dual-seed / multi-seed prompt should still require, at minimum:

- mandatory bootstrap reads of the current restart authority and most recent closed Explaino-family slices through `phase8n`
- `py -3.14 tools/viewer_host_session_bootstrap.py --audit --tail-handoff 8`
- `py -3.14 tools/viewer_host_repo_status.py`
- a fresh slice plan/contract before mutation
- an explicit first RED/proving step
- a hostile review posture
- the post-closeout stale-plan gate on the committed head

The prompt may stay short as long as it points here and the main Explaino-all launch anchor while still forcing the session to report repo state, active lock, chosen slice, first RED, and expected owner seams.

## Per-Slice Success Rules

### Slice A - Classification + Ownership Answer

Must prove:

- `explaino_seed_b` and `explaino_mix` have explicit ownership answers
- the answer is based on runtime/default/state proof, not analogy
- any required split between the two parameters is explicit

### Slice B - Promotion Only If Supported

Must prove:

- any promoted parameter is runtime-real end to end
- canonical neutral/default behavior remains truthful
- legacy selectors do not preserve a second hidden implementation authority

### Slice C - Enforcement Only If Needed

Must prove:

- a future dual-seed / multi-seed parameter cannot drift past the chosen ownership model without fail-closed test coverage
- canonical or fenced legacy behavior remains enforced after promotion or explicit non-promotion

## Post-Closeout Gate

Every future dual-seed / multi-seed slice must re-read its active phased plan on the committed head and fail closed if the plan still contains stale pre-closeout language or untruthful open asks.

## Start From

Use this packet when dual-seed / multi-seed follow-up is explicitly reopened or when the closed phase8p answer needs audit context. It is not the default next Explaino-family lane after phase8p; that routing now belongs to the structural / root-pack packet.

- [advanced_color_library_foundation_explaino_all_launch_anchor.md](/C:/code/cuda_newton_fractal_clone/docs/notes/advanced_color_library_foundation_explaino_all_launch_anchor.md)
- [advanced_color_library_foundation_phase8p_explaino_dual_seed_multiseed_classification_ownership_PHASED_PLAN.md](/C:/code/cuda_newton_fractal_clone/docs/notes/advanced_color_library_foundation_phase8p_explaino_dual_seed_multiseed_classification_ownership_PHASED_PLAN.md)
- [advanced_color_library_foundation_oracle_and_inventory.md](/C:/code/cuda_newton_fractal_clone/docs/notes/advanced_color_library_foundation_oracle_and_inventory.md)
- [advanced_color_feature_restart_inventory.md](/C:/code/cuda_newton_fractal_clone/docs/notes/advanced_color_feature_restart_inventory.md)
- [advanced_color_library_foundation_PHASED_PLAN.md](/C:/code/cuda_newton_fractal_clone/docs/notes/advanced_color_library_foundation_PHASED_PLAN.md)
