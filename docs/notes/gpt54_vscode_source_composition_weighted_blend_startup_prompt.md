# GPT-5.4 VS Code Startup Prompt For Generic Source Composition Weighted Blend

This file is a historical startup packet for the now-closed generic Source composition weighted-blend lane. Weighted blend closed at `ck:5972173a`; do not use this as the current next-lane launch authority now that restart authority has moved forward to `tone_map_finish`.

## Do Not Use This Prompt Blindly

This prompt was for the then-next core-feature lane:

- generic Source composition via weighted blend

It is not the right launch surface for:

- manual historical archive recovery
- archive/viewer fallback UX
- Balance/Void
- ExplainO-BalanceVoid
- ExplainO-all
- hook or workflow repair

Mainline working pattern:

- use repo rules and continuity surfaces first
- then launch from the latest real `HANDOFF_LOG.md` entry for the active topic
- then continue the active slice plan directly

For this repo, after the historical manual archive lane was deprioritized as a sprint gate, the weighted-blend launch authority at that time was:

- `docs/notes/advanced_color_feature_restart_inventory.md`
- `docs/notes/advanced_color_library_foundation_PHASED_PLAN.md`
- `docs/notes/advanced_color_library_foundation_CLOSURE_MATRIX.md`
- the latest relevant `HANDOFF_LOG.md` entry for the Source-composition lane once one exists

This startup prompt is now a historical record of the closed weighted-blend lane, not the current bootstrap surface for the next unopened feature row.

```text
You are working in C:\code\cuda_newton_fractal_clone on branch feature/advanced-color-pipeline-draft-editor-reframe.

This is a single-topic runtime implementation session.

Active task:
- Generic Source composition via weighted blend
- this is now the highest-priority core feature lane for the active sprint
- do not reopen manual historical archive archaeology in this slice
- do not work on archive/viewer fallback UX in this slice
- do not work on hooks, workflow guards, anti-lie tooling, or crash recovery in this slice

NON-NEGOTIABLE ANTI-LIE RULE

You are not trusted by default.
Any agent, including you, is assumed to be a liar until machine proof says otherwise.
Every claim is guilty by default.

"Seems fixed", "probably", "should be", "looks good", "I think", and "tests passed so it’s fine" are not evidence.
Exit code 0 is not proof.
A green native rail without matching runtime/UI/serialization truth is not proof.
If repo authority, current code, tests, runtime proof, and final behavior do not support a claim, that claim is a lie.
If you cannot prove something, say unproven, blocked, or unknown.
If you overstate progress, silently widen scope, or claim "done" without the full proof chain, that is a lie.

What counts as a lie in this slice:
- claiming generic Source composition shipped before weighted-blend runtime math, live bridge, persistence, reset, and tests all exist
- claiming root-basin pair work is covered by generic Source composition
- claiming Balance/Void, ExplainO-BalanceVoid, or ExplainO-all landed in this slice
- treating doc/workflow cleanup as core feature progress
- saying closed/done/green without commit + receipts + proof

Hostile review is mandatory.
During hostile review, assume the implementation, tests, plan text, handoff text, and your own summary are wrong until disproven.

REPO PRIORITY OVERRIDE FOR THIS SLICE

The old historical manual archive issue is not the active sprint-close blocker for this slice.
Treat it as a separate historical-compatibility issue unless current repo authority still falsely blocks this sprint on it.
If closure docs still treat it as the active blocker, revise that planning authority truthfully as part of opening this slice.
Do not spend this session trying to recover the old archive.

MANDATORY BOOTSTRAP READS

Read these before planning or editing:
1. AGENTS.md
2. AGENT_WORKING_PROTOCOL.md
3. AGENT_TERMINAL_PROTOCOL.md
4. docs/notes/advanced_color_feature_restart_inventory.md
5. docs/notes/advanced_color_library_foundation_PHASED_PLAN.md
6. docs/notes/advanced_color_library_foundation_CLOSURE_MATRIX.md
7. docs/notes/advanced_color_library_foundation_oracle_and_inventory.md
8. the relevant HANDOFF_LOG.md entries for ck:14222ffc, ck:679b2bc3, ck:44faf037, and ck:presetpreserve1

MANDATORY BOOTSTRAP COMMAND PASS

Run these before planning or editing:
- py -3.14 tools/viewer_host_session_bootstrap.py --audit --tail-handoff 8
- py -3.14 tools/viewer_host_repo_status.py

Then report:
- current branch
- current HEAD
- active locked contract
- clean/dirty state
- whether this is a fresh session or carryover
- whether current repo authority still incorrectly points at the manual archive lane instead of the new composition lane

BOOTSTRAP EXIT RULE

Bootstrap is not the task. Bootstrap is only the precondition for the task.

Immediately after the bootstrap read/command pass, you must do exactly one of these two things:
1. begin concrete repo-grounded execution on the Source-composition lane
2. declare one specific blocker that makes concrete execution impossible right now

Anything else is noncompliance.

Explicitly forbidden immediately after bootstrap:
- reviewing this startup prompt instead of doing the task
- "verifying whether the prompt needs corrections"
- writing a plan-only response
- writing status-only notes
- creating memory files or handoff notes before real task work
- turning the request into documentation review
- asking whether to preserve or refine the prompt

If you catch yourself evaluating this prompt instead of executing the Source-composition lane, stop and return to the Source-composition lane immediately.

The first substantive action after bootstrap must be one of:
- inspect the current one-Source runtime owner seam in detail
- inspect the current Source import/apply bridge in detail
- inspect the current Source persistence/reset seams in detail
- add the first RED for one-row-only Source runtime behavior
- explicitly re-lock and continue the new Source-composition slice if the repo state requires that before mutation

SOURCE-OF-TRUTH RULE

Chat history is not authority.
Only these are authority for this session:
- docs/notes/advanced_color_library_foundation_PHASED_PLAN.md
- docs/notes/advanced_color_library_foundation_CLOSURE_MATRIX.md
- docs/notes/advanced_color_library_foundation_oracle_and_inventory.md
- docs/notes/advanced_color_feature_restart_inventory.md
- current code
- current tests
- current repo state
- current runtime proof surfaces

Reject any earlier narrative that conflicts with those surfaces.

SCOPE LOCK

This session may do only this:
- open and execute a bounded generic Source composition slice using weighted blend semantics

This session must not:
- work on manual historical archive recovery
- work on archive/viewer fallback UX
- implement remaining deferred Grading rows
- implement Balance/Void
- implement ExplainO-BalanceVoid geometry/family work
- implement ExplainO-all
- do hook/workflow hardening
- do opportunistic UI polish
- bundle unrelated cleanup

EXACT PRODUCT INTENT

The goal is to finish the general composition pattern for Source, not just write a semantics note.
This must be real shipped runtime-backed core feature work.

For this first Source-composition slice:
- the mixer rule is weighted blend
- the slice must ship generic Source composition for the current shipped non-pair Source family
- root_index remains pair-only and must stay in the bounded root-basin family, not the generic Source stack
- remaining deferred Grading rows stay deferred
- Balance/Void and ExplainO-all stay out of scope

REQUIRED SEMANTIC RULES TO IMPLEMENT

Lock these rules in code/tests/docs for the first shipped Source-composition model:
- Source rows emit scalar signals independently from the same fractal sample/runtime state
- the running composed Source value is the weighted blend of enabled Source rows
- the first implementation is for the generic Source lane, not the bounded root-basin pair lane
- root_index is forbidden in generic Source composition for this slice
- root-basin pair behavior remains separate and existing
- root_proximity may participate only where existing family/runtime rules already allow it
- Shape still operates after Source composition on the composed scalar
- Palette still maps the final scalar after Shape
- Grading still applies after Palette as today

If current code shape makes one of those impossible without a cleaner equivalent implementation detail, you may adjust the internal representation but not the user-visible semantics above.

REQUIRED OWNER SEAMS TO INSPECT BEFORE EDITING

Inspect and name the concrete owner seams first:
- ui_app/src/color_pipeline_core.h
- ui_app/src/color_pipeline_window.h
- ui_app/src/fractal_types.h
- ui_app/src/escape_time_coloring.h
- ui_app/src/diagnostics_capture.cpp
- ui_app/src/diagnostics_state_io.cpp
- ui_app/src/runtime_reset.cpp
- ui_app/tests/test_schema_binding.cpp
- ui_app/tests/test_escape_time_coloring.cpp
- ui_app/tests/test_diagnostics_state_io.cpp
- ui_app/tests/test_finding_archive_actions.cpp
- ui_app/tests/test_runtime_reset.cpp

You must explicitly call out:
- where single-row Source assumptions still live
- where the new Source stack owner belongs
- where legacy flat signal fields still need compatibility mirroring or compatibility consumption
- how the bounded root-basin pair family remains separate

FIRST MUTATION TASKS

Before feature edits:
1. open a new checked-in plan/contract for generic Source composition weighted blend
2. re-lock the active slice to that new plan/contract
3. revise stale closure/restart authority only as needed to make this lane the active sprint-core feature lane and remove false active-blocker wording from the manual archive topic

Then execute forward TDD:
1. add REDs proving current Source lane still collapses to one row / one signal
2. add REDs for weighted multi-Source apply/import/runtime behavior
3. add REDs for diagnostics persistence, archive persistence, and reset/default behavior
4. only then implement the smallest owner changes that make the REDs pass

REQUIRED FEATURE BOUNDARY FOR THIS SLICE

The implementation must cover:
- generic Source lane model for weighted blend
- runtime evaluation of multiple enabled Source rows
- live editor import/apply behavior for multiple Source rows
- serialization round-trip for the Source stack
- capture/finding persistence for the Source stack
- reset/default handling for the Source stack
- proof that the weighted blend path is runtime-real and not preview-only

The implementation must not cover:
- root_index generic composition
- generic Palette chaining changes
- Grading owner-proof expansion
- Balance/Void
- ExplainO-BalanceVoid
- ExplainO-all
- archive UX work

EXPECTED SHIPPED SOURCE SET FOR THIS SLICE

Treat these as the candidate generic Source rows for the first weighted-blend implementation:
- smooth_escape_ramp
- phase_orbit
- banded_signal
- escape_magnitude
- orbit_stripe
- root_proximity only where already runtime-valid for the active family rules

Treat this as excluded from generic Source composition in this slice:
- root_index

TEST AND PROOF REQUIREMENTS

You must add or update tests that prove:
- multi-row Source draft/apply/import works through the live bridge
- weighted blend math is correct in runtime evaluation
- single-row behavior still matches existing shipped behavior when only one Source row is enabled
- root_index is rejected or excluded from generic Source composition in this slice
- root-basin pair behavior remains intact and separate
- diagnostics save/load preserves the Source stack
- finding archive persistence preserves the Source stack
- runtime reset/default clears or restores the Source stack correctly
- no false single-row fallback survives

At closeout, require:
- native tests for the touched owner seams
- runtime publish
- published runtime proof or closest existing runtime proof rail that truthfully exercises the new Source-composition path
- contract validation
- phased-plan sync
- hostile-audit validation
- checkpoint commit
- validation receipt
- contract proof receipt

HOSTILE REVIEW REQUIREMENTS

Hostile review must assume you are lying.
It must explicitly ask:
- Did I actually ship generic Source composition, or only the editor draft model?
- Did I leave runtime on a hidden single-row fallback?
- Did I silently fold root_index into generic composition even though it is pair-only?
- Did I break root-basin pair behavior while adding generic Source composition?
- Did I claim more shipped Source rows than the runtime actually supports?
- Did I widen into Grading, Balance/Void, or ExplainO-all without proof?
- Did I summarize beyond the proof?

SUCCESS CRITERIA

Only this counts as success:
1. generic Source composition with weighted blend is real in runtime, bridge, persistence, archive, and reset surfaces for the bounded shipped Source set
2. root_index remains separate in the bounded root-basin pair family
3. repo authority and handoff surfaces truthfully say this is the new active core-feature lane
4. the slice closes with commit + receipts + clean tree

This does not count as success:
- a semantics note with no shipped implementation
- editor-only multi-row draft behavior
- runtime math without persistence
- persistence without runtime math
- "mostly works"
- Balance/Void hitching a ride
- ExplainO-all hitching a ride

FIRST RESPONSE RULE

Your first response must contain:
- repo state
- active contract
- whether stale manual-archive authority must be revised for this slice
- exact first RED/proving step
- the concrete owner seams you expect to touch

Do not review this prompt.
Do not write a plan-only response after bootstrap.
Do not create memory notes or handoff notes before real work starts.
Do not broaden scope.
```

## Usage Notes

- This prompt is intentionally blunt.
- It is not a generic workflow bootstrap.
- It is only for the next core-feature lane:
  - generic Source composition via weighted blend
- Once a newer real handoff exists for the same topic, stop using this note as the launch authority and switch to the current slice plan plus the latest relevant `HANDOFF_LOG.md` entry.
- If the repo already has a newer checked-in Source-composition implementation handoff, do not waste a session re-deriving the lane from this prompt.
- If the session broadens beyond weighted-blend Source composition without first stopping and naming the blocker, the session is already drifting.
