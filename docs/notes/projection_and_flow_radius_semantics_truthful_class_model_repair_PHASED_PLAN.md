# Projection-and-Flow Fractal - Radius Semantics Truthful Class-Model Repair

## Current Phase

Phase 4 complete - the Projection-and-Flow public class model now uses `root sector x transient-pressure band`, the focused kernel/renderer/schema/runtime witnesses are green, and this plan now reflects the closed bounded stop-point without stale in-progress text.

## Phase Checklist

- [x] Phase 1 - open this checked-in follow-up plan and contract, replace the closed non-unit-radius stopgap lock, and prove the current runtime still makes `projection_and_flow_target_radius` effectively meaningless away from `1.0`.
- [x] Phase 2 - repair the bounded Projection-and-Flow public class semantics so the current runtime exposes truthful radius-sensitive structure without changing `SampleFractalPoints(...)`, `FractalSampleResult`, Explaino lanes, or the color pipeline.
- [x] Phase 3 - prove the repaired state through focused kernel, renderer, schema/help, and published-runtime Projection-and-Flow witnesses.
- [x] Phase 4 - run hostile audit, checkpoint, write receipts, clear stale closeout text, and leave the repo clean on the committed head.

## Explicit User Asks

- [resolved] Fix the real Projection-and-Flow defect where radius `1.0` is still the only meaningful value and non-unit radii still render as trivial large sectors.
- [resolved] Stop wasting time on fake closures and pay attention to the actual runtime-visible task.
- [resolved] Keep this bounded to Projection-and-Flow semantics/class ownership rather than widening into Explaino work, color-pipeline work, or generic engine refactor.
- [resolved] Preserve `SampleFractalPoints(...)` and `FractalSampleResult` as the shipped legacy sampling surfaces.
- [resolved] Close only with focused native proof, runtime publish, published-runtime proof, hostile audit, checkpoint commit, machine receipts, clean tree, and the committed-head stale-plan gate.

## Proof Ledger

- Repo state at slice open is clean on branch `feature/fractal-sample-evidence-widening` at committed head `e6e5681`; the active locked contract still points at the already closed `projection_and_flow_nonunit_radius_runtime_truth_fix` slice and must be replaced before mutation.
- Current published runtime proof on the committed head still confirms the user's complaint: radius `1.0` renders `6` colors, while radii `0.25`, `0.5`, `0.75`, `1.25`, `1.75`, `2.5`, and `4.0` each render only `3` colors.
- The bounded cause is now clearer than the previous stopgap summary: even after preserving non-unit stable classes, the shipped public class model for Projection-and-Flow is still only `root sector x one pressure bucket`, so whenever the pressure bit collapses the runtime can show at most the three cubic sectors or four quartic sectors.
- Hidden runtime evidence shows the current full-projection orbit still carries more structure than the public class model exposes: non-unit radii vary in transient projection-pressure excess even when their projected root sector stabilizes, so the truthful owner seam is the public class model rather than `SampleFractalPoints(...)` or the renderer contract.
- The bounded owner seams for this repair are expected to be `ui_app/src/fractal_sample_device.inl`, `ui_app/src/fractal_renderer.cu`, `ui_app/src/safe_mode_schema.cpp`, `ui/fractal_binding_surface_v1.ui_schema.json`, `ui_app/tests/test_fractal_sample_kernel.cu`, `ui_app/tests/test_fractal_renderer.cu`, `ui_app/tests/test_safe_mode_schema.cpp`, `ui_app/tests/test_ui_schema.cpp`, and `tests/test_fractal_runtime_projection_and_flow.py`.
- GREEN class-model repair: Projection-and-Flow now classifies by `root sector x transient-pressure band`, where transient pressure is the projected orbit's peak radial correction minus its settled correction, split into four public bands by the existing `projection_and_flow_pressure_threshold` control.
- Focused native proof is green through `artifacts/projection_flow_radius_semantics_green.log`: `test_safe_mode_schema.exe`, `test_ui_schema.exe`, `test_fractal_renderer.exe`, and `test_fractal_sample_kernel.exe` all passed on the rebuilt bounded seams.
- Focused published-runtime proof is green through `artifacts/projection_flow_radius_semantics_runtime_green.log`: `pytest tests/test_fractal_runtime_projection_and_flow.py -q` passed `6/6` after runtime publish, and direct capture receipts now show radii `0.25`, `0.5`, `0.75`, `1.0`, `1.25`, `1.75`, `2.5`, and `4.0` each rendering `8` colors with distinct non-unit frame hashes instead of the old three-color collapse.
- Hostile visual audit on `artifacts/projection_flow_radius_1_75_postfix.bmp` confirms the `1.75` runtime no longer shows the old flat three-sector map; it now exposes internal transient-pressure band structure around the projected sectors while preserving the bounded Projection-and-Flow identity.

## Hostile Audit

- Status: complete
- Required posture: assume the next fix will only repaint the same trivial sectors, smuggle in arbitrary coloring disguised as class semantics, or broaden into unrelated engine work unless focused proof disproves each failure mode.
- Hostile review questions:
  Did I actually make non-unit radius values meaningful, or only increase the color count on the same trivial map?
  Did I make the public class model explicit and truthful, or hide another sampler-only heuristic behind a new name?
  Did I preserve `SampleFractalPoints(...)` and `FractalSampleResult` as the shipped legacy surfaces?
  Did I silently widen into Explaino work, color-pipeline work, or a generic engine rewrite?
  Did I stop with stale closeout text again?

## Audit Passes

- [x] Pass 1 - prove the current runtime still reduces non-unit radii to a trivial three-sector public class map.
- [x] Pass 2 - clean re-read the repaired Projection-and-Flow class semantics for truthfulness, bounded scope, and legacy-surface preservation.
- [x] Pass 3 - rerun the repaired kernel/renderer/schema/runtime proofs, then re-read the repaired state and confirm a real slice-owned defect was fixed before closure or that no further slice-owned defect remains.

## Audit Findings

- [x] Projection-and-Flow radius semantics were still broken after the prior stopgap: focused runtime proof showed non-unit radii still rendered only three public colors, and radii `1.25` and `1.75` even shared the same published frame hash under the old class model.
- [x] The repaired public class model is still bounded and truthful: only the Projection-and-Flow sampler, renderer class-count seam, schema help text, and focused witnesses changed; `SampleFractalPoints(...)`, `FractalSampleResult`, Explaino lanes, and the color pipeline stayed untouched.
- [x] No further slice-owned defect was found after the hostile reread, focused native/runtime reruns, quartic sanity check, and direct visual pass on the published `1.75` capture; the final bounded stop-point is the repaired transient-pressure band class model with the stale closeout text removed.

## Action Hostile Review

- Action ID: projection-flow-radius-class-model-truth-repair
- Suspected Failure Mode: I could make the output look busier without giving radius a truthful public semantics, keep the same trivial sector map under a different bucket story, or widen into generic rendering/color work.
- Correct Owner/Action: Change only the bounded Projection-and-Flow public class semantics and the focused proofs needed to show radius-sensitive structure on the current runtime while preserving the shipped sampling seam.
- Proof Surface: GREEN evidence must come from focused Projection-and-Flow kernel, renderer, schema/help, and published-runtime witnesses on the current committed head.
- Blocked Action: Mutate unrelated fractal families, Explaino surfaces, broad color-pipeline behavior, or generic engine abstractions outside the bounded owner seams above.

## Notes

- Expected REDs to lock before implementation:
  1. non-unit radii still render as only three colors under the shipped cubic Projection-and-Flow defaults
  2. the current kernel/renderer public class model still decodes to only `root sector x one pressure bucket`
  3. the current safe-mode/schema help text still publicly describes Projection-and-Flow as `root sector x pressure bucket`
  4. `SampleFractalPoints(...)` and legacy callers remain on their shipped semantics
