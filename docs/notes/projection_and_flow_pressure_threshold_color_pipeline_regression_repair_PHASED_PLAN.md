# Projection-and-Flow Pressure-Threshold Programmable Color Regression Repair

## Current Phase

Complete - the bounded Projection-and-Flow programmable pressure-threshold repair is committed, receipted, pushed on `feature/fractal-sample-evidence-widening`, and the committed-head stale-plan reread is clean.

## Phase Checklist

- [x] Phase 1 - replace the closed prior lock, prove the programmable pressure-threshold regression on the current head, and name the exact owner seam that dropped pressure-threshold authority from the public color path.
- [x] Phase 2 - land one bounded Projection-and-Flow programmable color authority repair without reopening generic color-pipeline work, Explaino work, renderer replacement, or `SampleFractalPoints(...)` / `FractalSampleResult` changes.
- [x] Phase 3 - prove the repaired state through focused native color/renderer proof, focused runtime publish, and published-runtime Projection-and-Flow threshold witnesses while preserving the shipped legacy sample surface.
- [x] Phase 4 - run hostile audit, checkpoint, write receipts, push `feature/fractal-sample-evidence-widening`, clear stale closeout text, restore any temporary preflight scope widening, and leave the repo clean on the committed head.

## Explicit User Asks

- [closed] Repair the real Projection-and-Flow regression where `projection_and_flow_pressure_threshold` now does nothing in the public programmable or smooth-escape color path.
- [closed] Do not accept "one tiny slice works while other areas break" as a truthful stop-point; the public Projection-and-Flow product surface must stay coherent.
- [closed] Keep this bounded to Projection-and-Flow programmable color authority rather than widening into generic color-pipeline expansion, Explaino integration, renderer replacement, or a broad engine refactor.
- [closed] Preserve `FractalSampleResult` and `SampleFractalPoints(...)` as the shipped legacy sampling surfaces.
- [closed] Close only with focused native proof, runtime publish, published-runtime Projection-and-Flow proof, hostile audit, checkpoint commit, machine receipts, push, clean tree, and the committed-head stale-plan gate.

## Proof Ledger

- Repo state at slice open is clean on branch `feature/fractal-sample-evidence-widening` at pushed head `658adee`; this is a fresh follow-up regression repair, not carryover debt.
- The active smooth-escape/perf repair contract is already closed and must not become the main work surface for this regression; a fresh bounded follow-up plan and contract are required before product mutation.
- Current runtime RED is explicit on the current published runtime: with `projection_and_flow_target_radius=1.75`, the legacy `root_basin` path changes frame hash across `projection_and_flow_pressure_threshold` values `0.25`, `0.5`, `1.0`, and `2.0`, but the public programmable `smooth_escape` tuple (`coloring_mode=smooth_escape`, `color_signal=smooth_escape`, `color_palette=cyclic_escape`, `color_grading=escape_default`) stays hash-identical across those same thresholds.
- The bounded owner hypothesis is split across two closely related programmable seams in `ui_app/src/escape_time_coloring.h`: Projection-and-Flow smooth-escape currently samples the raw transient-pressure residual without normalizing it against the public threshold, and the generic color-pipeline root-sample helper still resolves Projection-and-Flow against the physical polynomial root count rather than the synthetic projected class count.
- Existing sampler and renderer proof already says the public class model itself is alive: the radius/class-model repair plan and focused tests still show `projection_and_flow_pressure_threshold` affects the explicit `root sector x transient-pressure band` public class model in the legacy root-basin branch, so the current defect is isolated to the programmable color path rather than `SampleFractalPoints(...)`.
- Focused RED is locked at `artifacts/projection_flow_threshold_red_build_tests.log` and `artifacts/projection_flow_threshold_runtime_red.log`: the new native helper witness failed with `Projection-and-Flow smooth_escape programmable coloring should react to pressure-threshold changes`, and the published-runtime witness failed because the `0.25` and `2.0` smooth-escape captures shared the same frame hash `475c1e9048a3a4077b6a3782324d0b362728f01fb7c66b942bb68055c8cc6864`.
- Landed programmable smooth-escape repair: `ui_app/src/escape_time_coloring.h` now normalizes Projection-and-Flow smooth-escape residual sampling against `projection_and_flow_pressure_threshold`, so the programmable basin signal follows the same public pressure metric family that the class model already exposes.
- Landed programmable root-index repair: `TryResolveColorPipelineRootSample(...)` now treats Projection-and-Flow as a synthetic-class lane and resolves programmable root samples against the projected class count (`root sectors * 4 pressure bands + unstable`) rather than collapsing them back to the physical polynomial root count.
- Focused native proof is green on the repaired head: `artifacts/projection_flow_threshold_test_escape_time_coloring_after_root_index_fix.log` passes the smooth-escape threshold witness plus the adjacent synthetic-root-index witness, `artifacts/projection_flow_threshold_test_fractal_renderer_after_root_index_fix.log` passes the same-lane render differential, and `artifacts/projection_flow_threshold_test_fractal_sample_kernel_postfix.log` keeps the shipped legacy sample surface green (`715 passed, 0 failed`).
- Focused runtime publish and published-runtime proof are green on the repaired head: `artifacts/projection_flow_threshold_runtime_build_final.log` stages the active runtime, and `artifacts/projection_flow_threshold_runtime_green_final.log` passes `8/8` including the new threshold-differential runtime test.
- Concrete repaired runtime witness now differs on the same public lane: at `projection_and_flow_target_radius=1.75` and the public smooth-escape tuple, threshold `0.25` hashes to `d68f582d4cdd7f508a1c068eea6e67e4df1d4655bc69033e9ffc7abd5ffa5871` while threshold `2.0` hashes to `8a338ce0cbfe6731697b6fdc551091e1397e0ab146fb71b431b24b669e43ee1d`.
- The temporary preflight widening on the closed smooth-escape/perf contract has already been removed again; final closeout only needs the new slice surfaces plus the repaired code/test seams.
- Closeout discipline is complete on the final committed head: the closing checkpoint chain carries fresh validation and contract-proof receipts, the branch is pushed, and this plan no longer leaves stale closeout chores behind.

## Hostile Audit

- Status: complete
- Required posture: assume the first fix will only repaint smooth_escape enough to move one hash, leave other programmable threshold paths dead, or silently widen into generic color work unless focused proof disproves each failure mode.
- Hostile review questions:
  Did I actually restore pressure-threshold authority in the public programmable Projection-and-Flow path, or only tweak one happy-path tuple?
  Did I preserve the shipped `SampleFractalPoints(...)` and `FractalSampleResult` surfaces?
  Did I keep the repair bounded to Projection-and-Flow programmable color authority instead of widening into a generic color-pipeline rewrite?
  Did I restore any temporary contract-scope preflight before closeout and stop with truthful pushed clean state?

## Audit Passes

- [x] Pass 1 - lock the RED with focused native and published-runtime witnesses showing the programmable path ignores `projection_and_flow_pressure_threshold` on the current head.
- [x] Pass 2 - re-read the repaired programmable owner seam for boundedness, legacy-surface preservation, and threshold authority coverage beyond one happy-path tuple.
- [x] Pass 3 - rerun the repaired native/runtime proofs, then re-read the repaired state and confirm no additional slice-owned programmable defect remains before closure.

## Audit Findings

- [x] Real programmable regression found: Projection-and-Flow `smooth_escape` on the published runtime stayed hash-identical across materially different `projection_and_flow_pressure_threshold` values even though the legacy root-basin/class-model path still reacted, proving the public threshold had dropped out of the programmable basin signal.
- [x] Second real programmable defect found during hostile reread: `TryResolveColorPipelineRootSample(...)` still resolved Projection-and-Flow against the physical polynomial root count, so adjacent synthetic class roots in the programmable `root_index` path could collapse back together even after the smooth-escape threshold repair.
- [x] No additional slice-owned defect found after the repaired native/runtime reruns, legacy sample-kernel rerun, and final programmable-seam reread on the current head.

## Action Hostile Review

- Action ID: projection-flow-pressure-threshold-programmable-color-repair
- Suspected Failure Mode: I could move one smooth_escape output hash while leaving Projection-and-Flow threshold semantics dead in the broader programmable path, or I could "fix" the symptom by widening into generic color-pipeline churn instead of repairing the bounded owner seam.
- Correct Owner/Action: Repair only the Projection-and-Flow programmable color authority seam that dropped public pressure-threshold influence, then prove the bounded programmable path reacts while the shipped legacy sample surface stays intact.
- Proof Surface: GREEN evidence must come from focused Projection-and-Flow native color/renderer witnesses, focused runtime publish, and published-runtime threshold-differential captures on the current branch.
- Blocked Action: generic color-pipeline expansion, Explaino integration, renderer replacement, `SampleFractalPoints(...)` replacement, or unrelated workflow/tooling work.

## Notes

- Expected owner seams for this slice:
  - `ui_app/src/escape_time_coloring.h`
  - `ui_app/src/fractal_renderer.cu`
  - `ui_app/tests/test_escape_time_coloring.cpp`
  - `ui_app/tests/test_fractal_renderer.cu`
  - `tests/test_fractal_runtime_projection_and_flow.py`
  - `docs/contracts/projection_and_flow_pressure_threshold_color_pipeline_regression_repair.contract.json`
  - `docs/notes/projection_and_flow_pressure_threshold_color_pipeline_regression_repair_PHASED_PLAN.md`
  - `docs/contracts/projection_and_flow_smooth_escape_perf_regression_repair.contract.json` only to undo the temporary preflight widening before final closeout
- Focused acceptance for this slice is the repaired Projection-and-Flow native/runtime seam set above rather than waiting indefinitely on the full `ui_app/build_tests_vsdevcmd.cmd` farm. The broad test build wrapper timed out twice after advancing well beyond the slice-owned seams; the targeted rebuilt executables and published-runtime proof are the truthful acceptance surface on this head.
- Closeout discipline is complete on the final committed head; no further closeout chores remain in this plan.
- Expected first REDs:
  1. the programmable `smooth_escape` Projection-and-Flow runtime stays hash-identical across materially different pressure-threshold values on the same radius lane
  2. the focused renderer seam reproduces the same threshold-dead programmable output
  3. the legacy `root_basin` path still reacts, proving the bug is programmable-path authority loss rather than a dead state/control surface
