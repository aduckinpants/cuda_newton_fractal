# Explaino-All Registry-Driven No-Mouse Live Viewer Repair

## Current Phase

Phase 5 - closed on repaired head after focused native proof, runtime publish, no-mouse published-runtime proof, hostile audit, contract validation, and stale-plan cleanup.

## Phase Checklist

- [x] Phase 0 - bootstrap current repo state, identify the closed prior contract, and prove the physical-mouse witness is not acceptable live-slider proof.
- [x] Phase 1 - lock this continuation contract, add registry-driven REDs for canonical `explaino_all`, Color Pipeline, and no-mouse automation.
- [x] Phase 2 - repair canonical `explaino_all` runtime routing so the whole `kExplainoAxisRegistry` axis structure stays active instead of collapsing to a partial owner lane.
- [x] Phase 3 - add in-process set-value UI automation for visible schema Controls-window and Color Pipeline numeric controls, preserving existing click automation.
- [x] Phase 4 - replace physical mouse runtime tests with registry-driven no-mouse published-runtime proof for Explaino-all, owner lanes, and Color Pipeline.
- [x] Phase 5 - hostile-audit the repaired head, rerun focused native/runtime proof, checkpoint, write receipts, push, and clear stale closeout text.

## Explicit User Asks

- [done] Treat `kExplainoAxisRegistry` as the single common-axis authority for canonical `explaino_all`; runtime tests export the C++ registry and generate proofs from that export.
- [done] Fix canonical `explaino_all` so the whole common-axis structure remains active and does not get rewritten into one partial owner lane before runtime sampling.
- [done] Replace OS-mouse live tests with in-process set-value automation that cannot steal the operator mouse.
- [done] Preserve Color Pipeline contracts while changing the shared automation/report harness, including smooth-escape ramp source scale, draft-to-live numeric edits, row enabled/remove click automation, and `balance_void_grade` runtime-backed behavior.
- [done] Preserve additive schema visibility for every registry axis on `explaino_all` plus its registry owner lane.
- [done] Preserve explicit owner selector identity, smooth-escape fast-path behavior, and `SampleFractalPoints(...)` as the shipped legacy projection surface.
- [done] Close only with focused native proof, runtime publish, no-mouse published-runtime proof, hostile audit, checkpoint commit, receipts, push, clean tree, and stale-plan cleanup.

## Proof Ledger

- Bootstrap on 2026-05-18 proved `branch=master`, `HEAD=52cbe5d`, clean tree, `origin/master...HEAD = 0 0`, active locked contract `explaino_all_live_viewer_control_interaction_repair`, and code-quality audit still failing on pre-existing baseline regressions.
- RED proof: `artifacts/logs/explaino_registry_red_family_rules.log` failed because active canonical `explaino_all` registry axes did not stay on the canonical all-axis runtime path.
- Core registry proof: `artifacts/logs/explaino_registry_family_rules_after_core_fix.log` and `artifacts/logs/explaino_registry_no_mouse_cpp_native.log` passed after `ResolveExplainoRuntimeFractalType(explaino_all, params)` and `sample_tier_resolver.cpp` were made registry-driven.
- Harness defect proof: `artifacts/logs/color_pipeline_focused_failfast_crash_proof.log` showed `ui_app/build_tests_vsdevcmd.cmd` previously missed a crashing native executable because negative Windows exit statuses were not treated as failure.
- Color Pipeline proof: `artifacts/logs/color_pipeline_focused_after_implicit_stack_fix.log` passed `test_color_pipeline_core`, `test_color_pipeline_window`, `test_schema_binding`, and `test_escape_time_coloring` after repairing the shipped smooth-escape grading bridge and implicit-stack draft materialization.
- Full native sweep proof: `artifacts/logs/explaino_registry_no_mouse_full_native_after_fixes.log` ran the broad native build and exposed only the stale `test_explaino_zero_axis_equivalence` expectation that assumed `explaino_all` could not compose ripple plus balance-void axes.
- Zero-axis regression proof: `artifacts/logs/explaino_zero_axis_after_all_axis_expectation_fix_2.log` passed `test_explaino_zero_axis_equivalence: 163 passed, 0 failed`.
- Final focused native proof: `artifacts/logs/explaino_registry_no_mouse_focused_native_after_runtime_walk_color_fix_2.log` passed `test_viewer_cli`, `test_ui_schema`, `test_schema_binding`, `test_fractal_family_rules`, `test_sample_tier_resolver`, `test_fractal_sample_result`, `test_fractal_sample_core`, `test_fractal_runtime_validation`, `test_fractal_renderer`, `test_color_pipeline_core`, `test_color_pipeline_window`, `test_escape_time_coloring`, `test_runtime_walk_bootstrap`, and `test_explaino_zero_axis_equivalence`.
- Runtime publish proof: `artifacts/logs/explaino_registry_no_mouse_runtime_publish_after_runtime_walk_color_fix.log` rebuilt and staged `D:\salt-fractal\cuda_newton_fractal_clone\runtime\fractal_ui.exe`.
- Published runtime registry proof: `artifacts/logs/explaino_axis_registry_export_published_runtime.log` exported all 7 axes from the C++ registry with their owner lanes and control ids.
- Final no-mouse published-runtime proof: `artifacts/logs/explaino_registry_no_mouse_runtime_pytest_after_runtime_walk_color_fix.log` passed 6 tests covering Explaino registry set-value controls, canonical `explaino_all`, owner lanes, smooth-escape float32 routing, no physical cursor usage, and Color Pipeline smooth-escape set-value behavior.
- Python syntax proof: `artifacts/logs/explaino_registry_no_mouse_python_compile_final.log` passed `py_compile` for the touched runtime pytest modules and shared runtime harness.

## Hostile Audit

- Status: closed
- Hostile review questions answered:
  Did I actually drive Explaino-all proof from `kExplainoAxisRegistry` instead of duplicating a seven-name subset? Yes; published runtime tests call `--describe-explaino-axis-registry` and generate axis proofs from that export.
  Did I actually keep canonical `explaino_all` on the canonical all-axis runtime path when any registry axis is active? Yes; family-rule native proof and runtime frame-change proof cover individual axes and all-axis composition.
  Did I preserve owner-lane selector identity and owner-axis behavior? Yes; owner lanes remain explicit and the registry export records each owner lane.
  Did I preserve additive schema visibility on `explaino_all` plus registry owner lanes? Yes; `test_ui_schema`, `test_schema_binding`, and published runtime schema proof are green.
  Did I remove first-party physical cursor automation from the runtime proof path? Yes; `tests/runtime_harness.py` no longer contains the OS cursor APIs and the runtime pytest guard passed.
  Did I preserve Color Pipeline smooth-escape ramp numeric live edits, draft-to-live behavior, row click automation, and `balance_void_grade` runtime-backed coverage? Yes; native Color Pipeline rails and the no-mouse Color Pipeline runtime proof are green.
  Did I preserve the smooth-escape fast path for canonical `explaino_all` and explicit owner lanes? Yes; `test_sample_tier_resolver` and published-runtime smooth-escape metadata proof are green.
  Did `SampleFractalPoints(...)` remain the shipped legacy projection surface? Yes; the sample core/result rails pass and no API change was made.
  Did I avoid widening into Meta-Basin, Operator-Itinerary, DSL/program-space, generic color-pipeline expansion, or broad engine redesign? Yes; changes stayed on registry routing, schema/binding automation, Color Pipeline guard fixes, and tests.
  Did I push the repaired head and stop with truthful closeout text? The push and receipt steps are recorded after checkpoint; this plan no longer advertises pre-closeout work.

## Audit Passes

- [x] Pass 1 - RED review found the canonical `explaino_all` runtime lie: active shared axes could resolve to partial owner lanes before the all-axis device branch.
- [x] Pass 2 - hostile Color Pipeline review found two real defects: the native harness missed negative crash exits, and runtime-walk viewer startup reset smooth-escape Color Pipeline state to default Explaino root-basin before the no-mouse report.
- [x] Pass 3 - clean re-read confirmed the repaired state with focused native proof, runtime publish, no-mouse published-runtime proof, and Python syntax proof; no additional real defect found.
- [x] Pass 4 - second clean re-read confirmed plan text, contract scope, and proof logs are aligned; no additional workflow mistake found.

## Audit Findings

- [x] Real finding - `ui_app/build_tests_vsdevcmd.cmd` used `if errorlevel 1` after test executables, so access-violation exits could report false green; fixed with `:run_test` exact zero-exit enforcement.
- [x] Real finding - runtime-walk viewer CLI activation changed `view/params` after `lastFractalType` and `lastPolyKind` were captured, so the first Controls frame reapplied default Explaino color and hid `color_pipeline.source.smooth_escape_ramp.signal.scale.primary`; fixed by resetting those baselines after successful runtime-walk activation.
- [x] Clean re-read - current focused native rail, runtime publish, and no-mouse runtime pytest proved cleanly after both real findings were repaired.
- [x] Clean re-read - stale-plan text was removed from this plan and no closeout-only action remains described as future work.

## Notes

- Scope stayed bounded to the registry-driven Explaino-all repair, no-mouse automation, and Color Pipeline guard surface.
- Physical mouse drag automation remains removed from first-party runtime tests; live UI value changes now flow through visible-control set-value automation and existing edit/commit paths.
- `explaino_all` is intentionally the full common-axis feature lane: every registry axis is active there, while owner lanes remain explicit and authoritative.
