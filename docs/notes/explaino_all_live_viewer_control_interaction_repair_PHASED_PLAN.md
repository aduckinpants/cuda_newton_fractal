# Explaino-All Live Viewer Control Interaction Repair

## Current Phase

Closed - the live Controls-window Explaino-all shared-axis interaction repair is validated, checkpointed, receipted, pushed, and stale-plan clean.

## Phase Checklist

- [x] Phase 1 - open this continuation contract, lock the slice, and capture the current live-viewer contradiction between headless runtime truth and interactive `explaino_all` slider behavior.
- [x] Phase 2 - add the narrowest truthful live Controls-window proving rail for `explaino_all` shared-axis sliders and make it fail on the current head.
- [x] Phase 3 - repair the viewer-path seam so live `explaino_all` slider edits visibly affect the viewport without rebreaking shared-carrier runtime routing, explicit selector identity, or the smooth-escape perf repair.
- [x] Phase 4 - hostile-audit the repaired head, rerun the touched native/runtime proofs, checkpoint, write receipts, and clear stale-plan text.

## Explicit User Asks

- [closed] Explain why `explaino_all` sliders could still look inert in the running viewer after the shared-carrier runtime repair.
- [closed] Repair the live Controls-window interaction seam so `explaino_all` shared-axis sliders actually affect the visible viewport again.
- [closed] Keep the already-restored `explaino_all` shared-carrier runtime routing, explicit selector identity, and smooth-escape fast-path behavior intact.
- [closed] Add viewer-path proof for this exact live bug instead of relying on headless state-load evidence alone.
- [closed] Keep the slice tightly bounded to the live viewer control-interaction seam; no new Explaino redesign, engine rewrite, or unrelated UI polish.

## Proof Ledger

- Bootstrap on 2026-05-18 proved `branch=master`, `HEAD=3bd709f`, clean tree, `origin/master...HEAD = 0 0`, and the active locked contract still pointed at the already-closed `merged_head_explaino_all_legacy_shared_carrier_repair` slice, so a new continuation contract was required before mutation.
- A focused headless published-runtime witness proved the seven shared Explaino axes were already authoritative one-at-a-time on canonical `explaino_all`: `ripple_amplitude`, `splice_offset`, `vortex_strength`, `tension_strength`, `balance_void`, `symmetry_tension`, and `field_curvature` each changed the captured frame hash individually on the current head.
- The remaining user-visible bug was therefore downstream of shared-carrier runtime math: `schema_binding.cpp` still bound the controls correctly, but the running viewer still made the sliders look inert.
- The first live Controls-window RED proved the concrete seam: `tests/test_fractal_runtime_runtime_walk_viewer.py` could not find `fractal_control.ripple_amplitude.primary` or `fractal_control.balance_void.primary` in the UI automation report because the viewer only exported automation rects for the advanced Color Pipeline window.
- The landed repair adds a narrow viewer-path authority seam only: `ui_app/src/schema_binding.h` and `ui_app/src/schema_binding.cpp` can now publish a primary automation id for rendered schema controls, and `ui_app/src/main.cpp` records those rects in the normal Controls window report without changing fractal runtime routing.
- Focused native proof is green in `artifacts/logs/explaino_all_live_viewer_native_focused.log`: the touched schema-binding rail now proves a rendered shared-axis float control publishes `fractal_control.ripple_amplitude.primary`, and the focused helper profile passed `test_schema_binding`, `test_escape_time_coloring`, `test_diagnostics_state_io`, `test_finding_archive_actions`, and `test_runtime_reset`.
- Runtime publish is green in `artifacts/logs/explaino_all_live_viewer_runtime_build.log`; the staged runtime is `D:\salt-fractal\cuda_newton_fractal_clone\runtime\fractal_ui.exe`.
- Focused published-runtime live viewer proof is green in `artifacts/logs/explaino_all_live_viewer_pytest.log`: `2 passed, 8 deselected in 4.71s` for the repaired `explaino_all` Controls-window drag witness covering both `ripple_amplitude` and `balance_void`.
- The repaired live witness now waits for a stable-before/stable-after viewport probe instead of assuming the first 200 ms sample after startup is already settled; this keeps the proof on the actual viewport seam instead of full-window UI churn.
- The slice did not touch shared-carrier runtime routing, explicit selector identity, or the smooth-escape fast-path logic; it only repaired the normal Controls-window automation/report/proof seam around the already-live params.

## Hostile Audit

- Status: complete
- Required posture: assume the live Controls-window edit path, control visibility, param writes, redraw invalidation, runtime publish witness, tests, plan text, and my own summary are wrong until machine proof says otherwise.
- Hostile review questions:
  Did I actually reproduce the live `explaino_all` no-op in the real viewer path instead of hiding behind headless state loads?
  Did I actually repair the live slider-to-viewport seam, or only add automation/reporting without fixing the bug?
  Did I keep canonical `explaino_all` shared-carrier runtime behavior intact while repairing the viewer path?
  Did I keep explicit selector identity intact?
  Did I preserve the smooth-escape fast path and avoid widening into unrelated UI or engine work?

## Audit Passes

- [closed] Pass 1 - add the live Controls-window RED and confirm the current head fails it.
- [closed] Pass 2 - repair the missing normal Controls-window automation/report seam, rebuild the touched native/runtime rails, and confirm the live drag witness reaches the actual viewport seam.
- [closed] Pass 3 - clean re-read the repaired viewer/runtime/plan surfaces after the stable-probe witness fix; no additional real defect found in the touched Explaino live path.

## Audit Findings

- [closed] The first real defect was the actual product seam: the normal schema-driven Controls window never exported UI automation rects, so the live viewer proof could not even reach the shared-axis sliders while the advanced Color Pipeline window still could.
- [closed] The second real defect was in the first proof rail, not in shared-carrier runtime ownership: the initial live viewport witness sampled too early during first-render settling and falsely failed before any drag. The repaired witness now waits for stable-before/stable-after viewport probes and still proves the drag changes the published runtime viewport on `explaino_all`.

## Notes

- Exit criteria: the live Controls-window `explaino_all` shared-axis sliders visibly affect the viewport again under a focused runtime witness, while the already-repaired shared-carrier runtime contract, explicit selector identity, and smooth-escape fast path remain intact.
- Validation target: touched native helper rails, runtime publish, and a focused published-runtime live viewer proof for the repaired Controls-window seam.
