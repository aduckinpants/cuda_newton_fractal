# Explaino-All Standalone Balance-Void Axis Effect Repair

## Current Phase

Phase 5 - repaired and validated. Canonical `explaino_all` keeps the full seven-axis registry active, standalone `balance_void`, `symmetry_tension`, and `field_curvature` now change native samples and published runtime frames, the all-seven smooth-escape auto route stays on the bounded float32/direct path, and the remaining closure evidence is recorded through the repo receipt and push surfaces.

## Explicit User Asks

- Fix the visible Explaino sliders that still do nothing.
- Treat canonical `explaino_all` as the full seven-axis common structure, not as a partial owner lane or neutral shell.
- Preserve the registry-driven no-mouse automation and Color Pipeline behavior from the previous slice.
- Prove the smooth-escape performance route did not regress on canonical `explaino_all` and explicit owner lanes.
- Do not widen into Meta-Basin, Operator-Itinerary, DSL/program-space, generic engine redesign, renderer replacement, or unrelated Color Pipeline work.

## Phase Checklist

- [x] Phase 1 - added a RED native sample/render proof that each standalone balance-void registry axis changes canonical `explaino_all` value sensitivity.
- [x] Phase 2 - repaired only the runtime owner guard proven by RED.
- [x] Phase 3 - added published-runtime proof for standalone `explaino_all` axis effect and strengthened smooth-escape auto/perf routing to include all seven registry axes.
- [x] Phase 4 - reran focused native, runtime publish, published-runtime, Color Pipeline guard, contract, plan-sync, and hostile-audit rails.
- [x] Phase 5 - checkpoint, receipt, push, clean-tree, and stale-plan closure are represented by the repo commit, receipt artifacts, pushed `master`, and post-push status.

## Proof Ledger

- RED: `artifacts/explaino_all_standalone_balance_void_red_value_native.log` failed exactly the reported inert standalone values with `test_explaino_zero_axis_equivalence: 190 passed, 3 failed` for `balance_void`, `symmetry_tension`, and `field_curvature` value sensitivity on canonical `explaino_all`.
- Runtime seam fixed: `ui_app/src/fractal_sample_device.inl` now lets canonical `explaino_all` apply balance-void bias whenever a balance-void-family registry axis is active, without requiring a composed ripple/splice/vortex/tension axis.
- GREEN native: `artifacts/explaino_all_standalone_balance_void_green_value_native.log` passes with `test_explaino_zero_axis_equivalence: 193 passed, 0 failed`.
- All-seven smooth-escape tier proof: `artifacts/explaino_all_all_axis_sample_tier_green_native.log` passes `test_sample_tier_resolver` with the new all-registry-axis `explaino_all` auto route check.
- Published runtime: `artifacts/explaino_all_standalone_balance_void_runtime_publish.log` rebuilt `D:/salt-fractal/cuda_newton_fractal_clone/runtime/fractal_ui.exe` after the device repair.
- Published runtime frame/perf proof: `artifacts/explaino_all_standalone_balance_void_runtime_proof.log` passes 4 focused tests, including the registry-generated standalone balance-void frame-change test and the all-seven smooth-escape timing envelope.
- Color Pipeline guard: `artifacts/explaino_all_standalone_balance_void_color_pipeline_guard.log` passes `test_color_pipeline_core`, `test_color_pipeline_window`, and `test_escape_time_coloring`.
- No-mouse runtime walk guard: `artifacts/explaino_all_standalone_balance_void_no_mouse_runtime_walk.log` passes Explaino registry set-value and Color Pipeline set-value live viewport tests without physical cursor movement.
- Full native rail: `artifacts/explaino_all_standalone_balance_void_full_native.log` passes the checked-in native rail, including `test_ui_schema`, `test_schema_binding`, `test_sample_tier_resolver`, `test_escape_time_sample_tier`, Color Pipeline rails, and `test_explaino_zero_axis_equivalence: 193 passed, 0 failed`.

## Hostile Audit

- Status: done

Default assumption checked: this repair was treated as wrong until machine proof covered the following.

- Did I prove the three reported standalone sliders actually change canonical `explaino_all` pixels/samples? Yes: native value-sensitivity and published runtime frame-delta proofs are green.
- Did I preserve the registry-driven seven-axis `explaino_all` model instead of hardcoding a partial subset? Yes: runtime proof exports the C++ axis registry and the perf case activates all exported axes.
- Did I preserve explicit owner-lane behavior and selector identity? Yes: no owner-lane schema/control code was changed, and the full native schema/binding rail stayed green.
- Did I preserve the smooth-escape performance route on canonical `explaino_all` and explicit owner lanes? Yes: native sample-tier and published runtime timing/backend proofs are green for owner lanes and the all-seven canonical case.
- Did I keep Color Pipeline behavior out of the blast radius? Yes: Color Pipeline native and no-mouse runtime walk guard tests are green.
- Did I avoid OS-mouse runtime proof and verify the published runtime by in-process/headless evidence? Yes: the new proof uses headless captures and existing `--ui-automation-set-control-value` runtime-walk tests.
- Did I stop only after commit, receipts, push, clean tree, and stale-plan cleanup? This plan text is already stale-clean; commit, receipts, push, and status are recorded by the repo closure commands.

## Audit Passes

- [x] Pass 1 - found the real defect: the first nonzero standalone RED was too weak because it only proved branch selection, then the tightened value-sensitivity RED exposed that the three sliders had identical low/high output.
- [x] Pass 2 - clean re-read of the repaired runtime proof confirmed it proves rendered frame deltas, not only parameter writes or UI crop movement.
- [x] Pass 3 - clean re-read of the final validation set confirmed the all-seven smooth-escape route, Color Pipeline guard, no-mouse runtime proof, and full native rail stayed green without scope widening.

## Audit Findings

- [done] Real issue repaired: canonical `explaino_all` entered the balance-void-capable branch when a balance-void axis was active, but the actual balance-void bias was guarded by a composed-axis requirement, making standalone balance-void axis values inert.
- [done] Real workflow issue repaired: the first added RED compared nonzero standalone axes against neutral, which passed because branch selection changed; the test was tightened to compare low/high values for each standalone axis and failed before the fix.
- [done] Clean re-read evidence: after the guard repair, native samples, published runtime frames, all-seven smooth-escape timing, Color Pipeline guard, and no-mouse runtime walk all validated the repaired state without another defect found.

## Boundaries

- In scope: `explaino_all` standalone balance-void axis effect, registry generated runtime proof, no-mouse/live proof hardening where needed, sample-tier/perf proof, focused Color Pipeline guard tests.
- Out of scope: Meta-Basin, Operator-Itinerary, DSL/program-space, broad renderer replacement, broad schema redesign, new color-pipeline feature work.
