# Advanced Color Library Foundation Phase 8P - Explaino Dual-Seed Multi-Seed Classification And Ownership

## Current Phase

Complete - the dual-seed / multi-seed classification slice is closed on this head: `explaino_seed_b` and `explaino_mix` now have one explicit checked-in dual-seed ownership registry, published-runtime proof shows the pair stays outside canonical Explaino-all, and the structural-modifier plus resolved-coupling buckets remained out of scope.

## Phase Checklist

- [x] Phase 1 - replace the closed phase8n lock with a new bounded deferred-class plan and contract, inspect the owner seams for `explaino_seed_b` and `explaino_mix`, and land RED witnesses that prove the repo still lacks one explicit fail-closed ownership answer for the dual-seed bucket
- [x] Phase 2 - classify `explaino_seed_b` and `explaino_mix` explicitly as canonicalizable now, legacy-only, or a different ownership model; promote only what runtime/default/state proof supports, and fence every non-promoted answer explicitly
- [x] Phase 3 - rerun the narrowest truthful native plus published-runtime witnesses, hostile-audit the repaired state, checkpoint, write machine receipts, reread the committed head for stale closeout text, and keep phase8j/8k/8l/8m/8n plus structural-modifier scope truth intact

## Explicit User Asks

- [done] Open and execute one bounded dual-seed / multi-seed classification plus ownership slice for `explaino_seed_b` and `explaino_mix` only.
- [done] Prove which current legacy selectors own `explaino_seed_b` and `explaino_mix`, whether each parameter has real runtime effect, and whether zero or default behavior collapses to canonical Explaino behavior.
- [done] Split the bucket where the semantics diverge: both parameters stay in a different dual-seed ownership model, but only `explaino_mix` has zero-collapse back to canonical Explaino while `explaino_seed_b` remains a secondary-surface carrier with no standalone neutral collapse.
- [done] Keep structural or root-pack modifiers (`phoenix_p_real`, `explaino_cluster_radius`), resolved couplings, generic advanced-color core work, manual archive archaeology, and historical archive compatibility out of scope unless one of them becomes a real classification blocker.
- [done] Preserve phase8j canonical identity plus axis registry, phase8k sidecar support, phase8l legacy variant projection plus preset truth, phase8m guardrails, and phase8n deferred coupling answers.
- [done] Close only with hostile review, checkpoint commit, validation receipt, contract proof receipt, clean tree, and committed-head stale-plan reread.

## Presumption Loop

The controlling risk for phase8p was false canonicalization by analogy. The final answer on this head is a bounded different-ownership model, not Explaino-all promotion: both parameters remain owned by the `explaino_dual` carrier, `explaino_mix` collapses to canonical Explaino at zero, and `explaino_seed_b` remains a secondary-seed surface whose effect only becomes runtime-real when the dual-seed blend gate is active.

## Presumption Evidence

- `ui/fractal_binding_surface_v1.ui_schema.json` still exposes `explaino_seed_b` and `explaino_mix` only when `fractal.view.fractal_type == explaino_dual`, which proves the current UI owner seam is still legacy-selector-specific.
- `ui_app/src/fractal_derived_fields.cpp` uses `explaino_seed_b` and `explaino_mix` only for `FractalType::explaino_dual`, where they change generated Explaino roots and quartic coefficients instead of acting as editor-only dead state.
- `ui_app/src/fractal_sample_device.inl` and `ui_app/src/fractal_probe_runner.cpp` consume those derived coefficients at runtime, so dual-seed semantics are runtime-real even though the canonical Explaino-all registry does not currently classify them.
- `ui_app/src/fractal_derived_fields.cpp` plus `ui_app/tests/test_fractal_derived_fields.cpp` already prove `explaino_mix = 0` collapses the dual-seed shape back to the primary Explaino seed surface, but the current repo has not yet recorded whether that makes the bucket canonicalizable or merely part of a separate ownership model.
- `ui/fractal_binding_surface_v1.ui_schema.json` still fences `explaino_cluster_radius` to `explaino_mult,explaino_rational`, so the structural modifier bucket remains a separate owner seam unless live proof shows a blocker.

## Proof Ledger

- Bootstrap on 2026-05-15 proved branch=`feature/advanced-color-pipeline-draft-editor-reframe`, `HEAD=6313d14`, clean tree, and an active locked contract on the already closed phase8n deferred-couplings slice.
- `py -3.14 tools/viewer_host_repo_status.py` proved `staged=none | unstaged=none | untracked=none`, so phase8p starts as a fresh session rather than dirty carryover.
- Mandatory authority reread re-proved that phase8j, phase8k, phase8l, phase8m, phase8n, and the phase8o staging packet are closed, and that the next bounded deferred class is dual-seed / multi-seed rather than resolved couplings or structural modifiers.
- Current code search proved the present selector owner is `explaino_dual` only: `ui/fractal_binding_surface_v1.ui_schema.json` gates both `explaino_seed_b` and `explaino_mix` with `visible_if == explaino_dual`, while `ui_app/src/fractal_family_rules.h` still lacks a parallel checked-in ownership registry for that bucket.
- Current code search also proved the structural-modifier seam is separate: `explaino_cluster_radius` remains fenced to `explaino_mult` and `explaino_rational`, while `phoenix_p_real` remains the phoenix-step carrier seam for other legacy variants.
- The first RED landed truthfully: `artifacts/phase8p_red_family_rules.log` captured the focused compile failure on missing `ExplainoDualSeedDescriptor`, `FindExplainoDualSeedDescriptor`, and `FindExplainoDualSeedDescriptorByBindingPath`, proving the repo still lacked one explicit checked-in dual-seed ownership surface before implementation.
- `ui_app/src/fractal_family_rules.h` now owns one explicit `kExplainoDualSeedRegistry` that classifies `explaino_seed_b` as a secondary-seed surface and `explaino_mix` as a blend gate, both under a different ownership model than canonical Explaino-all.
- Focused native witnesses are green from `D:/salt-fractal/cuda_newton_fractal_clone/build_tests`: `test_fractal_family_rules.exe`, `test_ui_schema.exe`, `test_fractal_derived_fields.exe`, `test_runtime_reset.exe`, and `test_schema_binding.exe`.
- The first hostile-audit pass found a real missing fence regression in my initial answer: the new registry had positive-entry proof but no explicit negative proof that structural modifiers and resolved couplings stayed out of the dual-seed bucket.
- After adding that regression in `ui_app/tests/test_fractal_family_rules.cpp`, the focused family-rules witness rebuilt and passed again.
- `py -3.14 tools/viewer_host_run_logged_command.py --label "phase8p-runtime-publish" --log artifacts/phase8p_runtime_publish.log -- ui_app/build_vsdevcmd.cmd` published the current runtime successfully to `D:/salt-fractal/cuda_newton_fractal_clone/runtime/fractal_ui.exe`.
- `py -3.14 tools/viewer_host_run_logged_command.py --label "phase8p-runtime-pytest" --log artifacts/phase8p_runtime_pytest.log -- py -3.14 -m pytest tests/test_fractal_runtime_explaino_escape_variants.py -q -k "test_explaino_all_neutral_defaults_match_explaino_published_runtime or test_explaino_all_registry_axes_round_trip_and_change_published_runtime_frame or test_explaino_balance_void_neutral_defaults_match_explaino_published_runtime or test_explaino_dual_seed_runtime_behavior_stays_outside_canonical_explaino_all or test_explaino_legacy_couplings_round_trip_and_neutral_collapse"` passed `8 passed, 30 deselected`, proving canonical Explaino-all preserved slice-1/2/3 behavior, phase8n stayed green, and the new dual-seed answer stays outside canonical Explaino-all.
- `test_explaino_dual_seed_runtime_behavior_stays_outside_canonical_explaino_all` now proves the full ownership answer: `explaino_dual` defaults stay outside the canonical Explaino frame, `explaino_mix = 0` collapses back to canonical Explaino even when `explaino_seed_b` changes, `explaino_seed_b` becomes runtime-real when `explaino_mix = 1`, and `explaino_all` ignores both deferred dual-seed params entirely.

## Hostile Audit

- Status: complete
- Required posture: assume the bucket is being over-promoted, under-classified, silently widened into structural modifiers, or left as hidden legacy-selector authority until deterministic selector, runtime, reset, diagnostics, and published-runtime proof disproves that suspicion.
- Current hostile result: one real missing regression was found and repaired, and the repaired state survived a clean native plus published-runtime re-read without exposing another real defect.

## Audit Passes

- [x] Pass 1 - land REDs that prove the repo still lacks one explicit fail-closed ownership authority for `explaino_seed_b` and `explaino_mix`, and that at least one of them still survives only through legacy-selector ownership.
- [x] Pass 2 - hostile re-read the first GREEN for false canonicalization, fake runtime proof, missing zero/default-collapse evidence, or silent widening into structural modifiers or resolved couplings.
- [x] Pass 3 - clean re-read the repaired state with the final narrow native plus published-runtime witnesses and confirm the committed head does not still contain stale closeout text.

## Audit Findings

- [x] Real missing regression fixed: the first dual-seed ownership answer had positive-entry proof only, so hostile audit added explicit negative assertions that structural modifiers and resolved couplings remain outside the dual-seed registry.
- [x] Runtime answer proved truthful: `explaino_mix` collapses dual-seed behavior back to canonical Explaino at zero, but `explaino_seed_b` has no standalone neutral/default collapse and remains runtime-real only under the dual-seed carrier.
- [x] Clean re-read of the repaired state reran the focused native and published-runtime witnesses and did not expose another real issue after the out-of-scope fence regression landed.

## Notes

- Required owner seams for this slice:
  - `docs/contracts/advanced_color_library_foundation_phase8p_explaino_dual_seed_multiseed_classification_ownership.contract.json`
  - `docs/notes/advanced_color_library_foundation_phase8p_explaino_dual_seed_multiseed_classification_ownership_PHASED_PLAN.md`
  - `ui/fractal_binding_surface_v1.ui_schema.json`
  - `ui_app/src/fractal_types.h`
  - `ui_app/src/fractal_derived_fields.cpp`
  - `ui_app/src/schema_binding.cpp`
  - `ui_app/src/enum_id_utils.h`
  - `ui_app/src/runtime_reset.cpp`
  - `ui_app/src/diagnostics_capture.cpp`
  - `ui_app/src/diagnostics_state_io.cpp`
  - `ui_app/src/fractal_family_rules.h`
  - `ui_app/src/fractal_sample_device.inl`
  - `ui_app/src/fractal_probe_runner.cpp`
  - `ui_app/src/runtime_walk_headless.cpp`
  - `ui_app/tests/test_fractal_family_rules.cpp`
  - `ui_app/tests/test_fractal_derived_fields.cpp`
  - `ui_app/tests/test_runtime_reset.cpp`
  - `ui_app/tests/test_schema_binding.cpp`
  - `ui_app/tests/test_ui_schema.cpp`
  - `ui_app/tests/test_fractal_probe.cpp`
  - `tests/test_fractal_runtime_explaino_escape_variants.py`
- Final ownership answer on this head:
  - `explaino_seed_b` stays a different dual-seed ownership model as the secondary-seed surface carried by `explaino_dual`; it is runtime-real under the carrier but has no standalone zero/default collapse to canonical Explaino.
  - `explaino_mix` stays a different dual-seed ownership model as the dual-seed blend gate carried by `explaino_dual`; it is runtime-real under the carrier and collapses back to canonical Explaino at `0.0`.
  - Neither parameter was promoted into the canonical Explaino-all owner surface in this slice.
- Structural modifiers (`phoenix_p_real`, `explaino_cluster_radius`) and resolved couplings (`momentum_beta`, `joy_coupling`, `fold_coupling`, `bell_coupling`) stayed blocked from scope and are now protected by explicit negative registry assertions.

## Resume Point

Closed. Any future follow-up must open a new bounded slice if it wants to promote dual-seed behavior further or to take on the separate structural-modifier bucket; phase8p already settled the current ownership answer for `explaino_seed_b` and `explaino_mix`.

## Action Hostile Review

- Action ID: action-20260515-explaino-dual-seed-multiseed-classification
- Suspected Failure Mode: the slice exposes more controls or centralizes labels while hidden legacy `explaino_dual` runtime/default/state authority still owns the real behavior, or the work silently widens into structural modifiers.
- Correct Owner/Action: classify `explaino_seed_b` and `explaino_mix` explicitly through one checked-in ownership surface, preserve any non-promoted answer as an explicit fence, and keep structural modifiers plus resolved couplings out of scope unless they become a proven blocker.
- Proof Surface: checked-in REDs, focused native and published-runtime witnesses, phased-plan sync, hostile-audit validation, checkpoint commit, validation receipt, contract proof receipt, and committed-head stale-plan reread.
- Blocked Action: no automatic dual-seed promotion, no structural or root-pack modifier promotion, no resolved-coupling reopening, no generic advanced-color core reopen, and no historical archive widening in this slice.
