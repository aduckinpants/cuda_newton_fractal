# Fractal Sample Evidence Widening - Projection And Flow Witness

## Current Phase

Complete - the bounded projection-and-flow consumer is landed on the sidecar lens/window path, the restart-facing docs are truth-repaired, the hostile reread found and repaired two real proof defects, the focused validators and touched-seam proofs are green, and this plan is the final closed-state surface for the checkpoint head. Machine receipts for this head are written after the commit rather than into the plan text itself.

## Phase Checklist

- [x] Phase 1 - open this checked-in projection-and-flow plan and contract, replace the closed slice-C lock, and re-prove the RED head still has no real bounded projection-and-flow consumer while the sidecar lens/window path remains aggregate-only.
- [x] Phase 2 - land one real bounded projection-and-flow consumer in the sidecar lens/window lane using only justified widened evidence while preserving `SampleFractalPoints(...)`, `FractalSampleResult`, and current legacy caller behavior.
- [x] Phase 3 - run the narrow validation ladder, complete hostile audit, checkpoint, write receipts, clear stale closeout text, and leave the repo clean.

## Explicit User Asks

- [done] Open and execute only the bounded projection-and-flow widened-consumer lane that follows the closed slice-C paired-state/counterfactual proof.
- [done] Prefer projection-and-flow over `ExplainO-BalanceVoid` unless live repo proof forces a narrower truthful alternative.
- [done] Preserve `FractalSampleResult` and `SampleFractalPoints(...)` as the shipped legacy projection surfaces.
- [done] Do not widen into `ExplainO-BalanceVoid`, generic engine refactor, renderer replacement, runtime-walk redesign, meta-basin, operator-itinerary, DSL/program-space, advanced-color reopen, or historical explaino archaeology.
- [done] Close only with commit, validation receipt, contract proof receipt, clean tree, and a committed-head stale-plan reread.

## Proof Ledger

- Bootstrap re-proved branch=`feature/fractal-sample-evidence-widening`, `HEAD=d39d80c`, clean tree, fresh session, and an active locked contract still pointing at the already closed slice-C contract `fractal_sample_evidence_widening_slice_c_paired_state_counterfactual_witness`.
- Current single-orbit authority is still the shipped legacy surface in `ui_app/src/fractal_sample_result.h`, `ui_app/src/fractal_types.h`, `ui_app/src/fractal_sample_core.cu`, `ui_app/src/fractal_renderer.cu`, `ui_app/src/explaino_sidecar_cuda_sample_host.cpp`, and `ui_app/src/runtime_walk_field_slime.cpp`; `SampleFractalPoints(...)` already projects from the widened host seam back to `FractalSampleResult`.
- Current widened evidence authority is still bounded to `sample_coord + legacy_result` in `ui_app/src/fractal_sample_result.h`, and `ui_app/src/fractal_sample_core.cu` still emits that payload without any richer orbit trace or renderer-specific contract growth.
- `ui_app/src/explaino_sidecar_measurement.cpp` is the first real widened consumer from slice C, but it stops at paired `minus_counterfactual` / `plus_counterfactual` witness summaries inside `SidecarMeasurementRow`; no downstream projection surface reads those witness metrics yet.
- `ui_app/src/explaino_sidecar_lens.cpp` plus `ui_app/src/explaino_sidecar_window.cpp` are the narrowest truthful next consumer seams: they already form a live projection-and-guidance surface for the sidecar window, but on the RED head they still derive guidance and active-zone flow from aggregate deltas only rather than widened evidence.
- `ui_app/src/runtime_walk_field_slime.cpp` is not the narrowest truthful next consumer on this head: it already owns its requested coordinates directly and still consumes only legacy `FractalSampleResult` outputs, so forcing widened evidence there would be broader than necessary.
- Current widened payload appears sufficient for the chosen slice: projection-and-flow can be justified from the existing paired evidence coordinates plus nested legacy results, so no new evidence field is proven necessary before the RED lands.
- Minimal blocker-clearing preflight is required before feature edits: `viewer_host_begin_work_slice.py` and `viewer_host_prepare_slice.py` both require the new plan/contract to already exist, while the closed slice-C contract does not scope those new files. This checked-in plan/contract pair is the smallest truthful bridge needed to replace the lock and continue immediately into slice work.
- Checked-in RED landed in the focused lens compile: `ui_app/tests/test_explaino_sidecar_lens.cpp` first demanded `SidecarLensProjectionRow::projection_flow_bias`, and the current head failed with `error C2039: 'projection_flow_bias': is not a member of 'SidecarLensProjectionRow'`, which proved the current repo still had no bounded projection-and-flow consumer surface.
- First GREEN now lives in `ui_app/src/explaino_sidecar_lens.h` and `ui_app/src/explaino_sidecar_lens.cpp`: the sidecar lens projection now computes bounded `projection_flow_bias` / `projection_flow_confidence` from the existing paired counterfactual witness and uses that signal to resolve live lens guidance plus active-zone asymmetry without changing `SampleFractalPoints(...)`, `FractalSampleResult`, or the widened evidence payload.
- The live downstream consumer proof is green in `ui_app/tests/test_explaino_sidecar_window.cpp`: a widened-evidence-only fake host now drives `BuildExplainoSidecarWindowState(...)` through the actual sidecar measurement -> lens -> window path, proves the legacy `Sample(...)` hook stays unused when widened evidence is available, and proves the live lens row turns toward the stronger minus-direction zoom flow witness.
- Focused unit proof is green in `ui_app/tests/test_explaino_sidecar_lens.cpp`: a tie-shaped aggregate measurement row with stronger plus-direction sample-coordinate displacement now yields positive `projection_flow_bias`, plus guidance, and plus-weighted active-zone asymmetry, which disproves the old aggregate-only lens behavior.
- Adjacent compatibility proofs are green on the current head: `ui_app/tests/test_explaino_sidecar_measurement.cpp` still proves the existing widened payload is the only evidence source in play, and `ui_app/tests/test_explaino_sidecar_cuda_sample_host.cpp` still proves `SampleFractalPoints(...)` remains the shipped legacy projection surface beside widened evidence support.
- Restart-facing truth repair landed in `docs/notes/fractal_sample_evidence_widening_launch_anchor.md` and `docs/notes/fractal_sample_evidence_widening_staging_PHASED_PLAN.md`: the launch packet now names the projection-and-flow slice as landed instead of still deferred and points fresh restarts at this active plan.
- This slice stays bounded without runtime publish or broader viewer proof: the chosen consumer is a headless sidecar window-state build, so focused contract/unit/window tests discriminate the behavior without needing a published runtime lane.

## Hostile Audit

- Status: complete
- Required posture: assume the projection-and-flow consumer is fake, only renames helpers, silently reuses the slice-C paired witness without adding a real downstream consumer, replaces `SampleFractalPoints(...)`, widens into `ExplainO-BalanceVoid`, or stops with stale closeout text until proof disproves each failure mode.
- Current hostile result: two real proof defects were found and repaired during the slice, and the clean reread of the repaired state did not find an additional real defect.

## Audit Passes

- [x] Pass 1 - prove the RED head still has no real bounded projection-and-flow consumer of widened sample evidence.
- [x] Pass 2 - land the bounded projection-and-flow consumer and prove it reads widened evidence meaningfully rather than only aggregate deltas.
- [x] Pass 3 - hostile reread the landed diff for scope drift, speculative field growth, legacy-path replacement, and stale restart-facing docs.
- [x] Pass 4 - rerun the repaired touched-seam proofs, re-read the repaired state, and confirm no additional real defect before checkpoint closure.

## Audit Findings

- [x] Real scope defect fixed: the first GREEN draft tried to mix residual, iteration, and flip deltas into the projection-flow score without proof that the projection lane needed them. The repaired lens now uses only widened sample-coordinate displacement, which is the narrowest justified flow signal for this slice.
- [x] Real proof-fixture defect fixed: the first zoom-only window witness swept too broad a zoom range and failed to isolate the intended near-baseline directional proof. The repaired fixture now constrains the zoom surface to `9.0..11.0`, which keeps the live minus/plus flow witness truthful and discriminating.
- [x] Clean re-read evidence: the repo now has one second real bounded projection-and-flow consumer in the sidecar lens/window lane, no new evidence fields were added beyond `sample_coord + legacy_result`, `SampleFractalPoints(...)` and `FractalSampleResult` remain the shipped legacy projection surfaces, and `ExplainO-BalanceVoid` plus broader engine work remain deferred.
