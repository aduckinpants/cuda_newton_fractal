# Explaino Structural / Root-Pack Launch Anchor

Use this file as the checked-in authority surface for the structural / root-pack deferred-class packet covering `phoenix_p_real` and `explaino_cluster_radius`.

Purpose:

- keep future prompts short without making them underspecified
- force kickoff through checked-in repo authority instead of chat reconstruction
- preserve the bounded structural / root-pack scope fence even if the exact kickoff wording changes later

## Non-Negotiable Identity Rules

- This lane is not a generic `Explaino-all` cleanup pass. It is one bounded deferred-class classification/promotion lane for `phoenix_p_real` and `explaino_cluster_radius` only.
- The current canonical seven-axis `Explaino-all` registry remains shipped and intact, and the closed dual-seed / multi-seed answer remains separate, unless this lane proves a truthful extension path.
- Structural / root-pack semantics are not neutral-at-zero deformation axes by assumption. They must be proven.
- If structural or root-pack semantics cannot truthfully project through canonical `Explaino-all` state, they must remain legacy-only or move to a different ownership model.
- Do not force `phoenix_p_real` and `explaino_cluster_radius` into one shared answer by analogy. Split the bucket if the live selector/runtime/default seams differ.

## Scope Fence

Future structural / root-pack slices may:

- classify `phoenix_p_real` and `explaino_cluster_radius`
- prove whether either or both can project through canonical `Explaino-all` ownership
- preserve legacy-only or different-ownership answers when the proof does not support promotion
- add bounded enforcement follow-up only if a truthful promotion lands first

Future structural / root-pack slices must not:

- reopen shipped generic advanced-color core work
- reopen historical `234919_563__explaino_inertial` compatibility work unless explicitly reclassified
- reopen the closed dual-seed / multi-seed bucket
- reopen the resolved couplings bucket
- treat this lane as a reason to skip proof, stale-plan gates, or slice contracts

## Staged Slice Order

Follow this order unless live repo evidence proves a narrower truthful split is better:

1. classification + owner-seam map
2. bounded promotion only if proof supports it
3. enforcement follow-up only if a promotion actually lands
4. optional cleanup only if the first two slices leave truthful residual debt

Do not start with enforcement before ownership/classification truth exists.
Do not start with cleanup before a promoted path or explicit legacy fence exists.

Current closed result:

- phase8r closed Slice A and proved the shared bucket does not stay unified
- phase8s closed the bounded `phoenix_p_real` follow-up as a dedicated shared phoenix-step memory-term carrier fence outside canonical `explaino_all`
- `phoenix_p_real` no longer belongs on the shared structural/root-pack carrier map on the current head
- phase8t closed `explaino_cluster_radius` as a dedicated split-selector surface with divergent `explaino_mult` versus `explaino_rational` runtime roles
- `0.0` for `phoenix_p_real` remains only a carrier-neutral memory-term state, not canonical Explaino collapse
- any future structural/root-pack work on this head should choose one explicit split lane instead of reopening one shared classification slice, replaying `phoenix_p_real`, or reopening a unified `explaino_cluster_radius` lane

## Group Under Review

The historical deferred packet for this anchor is:

- `phoenix_p_real`
- `explaino_cluster_radius`

Current split follow-up options on this head:

- `explaino_mult` root-shape work for `explaino_cluster_radius`
- `explaino_rational` Laurent-term work for `explaino_cluster_radius`

The key questions this lane must answer are:

- `phase8s` already answered `phoenix_p_real`: it stays outside canonical `explaino_all` as a dedicated shared phoenix-step memory-term carrier
- `phase8t` already answered the shared `explaino_cluster_radius` question: it stays outside canonical `explaino_all` as a dedicated split-selector surface and cannot truthfully continue as one unified later lane
- whether one later split lane should target only `explaino_mult` root-shape behavior or only `explaino_rational` Laurent-term behavior
- whether any later split lane should stop at non-promotion truth or needs a narrower owner refinement on top of the phase8t fence

## Explicit Out-Of-Scope Buckets

Do not widen this lane into:

- dual-seed / multi-seed structure
  - `explaino_seed_b`
  - `explaino_mix`
- resolved legacy coupling ownership
  - `momentum_beta`
  - `joy_coupling`
  - `fold_coupling`
  - `bell_coupling`

If one of those groups proves to be a real blocker, stop and report it instead of smuggling it into this lane.

## Required Owner Seams

Every future structural / root-pack kickoff must inspect and name the current ownership seams before mutation:

- `ui/fractal_binding_surface_v1.ui_schema.json`
- `ui_app/src/fractal_types.h`
- `ui_app/src/fractal_derived_fields.cpp`
- `ui_app/src/schema_binding.cpp`
- `ui_app/src/runtime_reset.cpp`
- `ui_app/src/diagnostics_capture.cpp`
- `ui_app/src/diagnostics_state_io.cpp`
- `ui_app/src/fractal_family_rules.h`
- `ui_app/src/fractal_runtime_validation.h`
- `ui_app/src/fractal_sample_device.inl`
- `ui_app/src/fractal_probe_runner.cpp`
- `ui_app/src/runtime_walk_headless.cpp`
- the current narrowest truthful structural/Explaino runtime/viewer witnesses under `ui_app/tests/` and `tests/`

Kickoff must explicitly report:

- which current legacy selectors own `phoenix_p_real` and `explaino_cluster_radius`
- whether `phoenix_p_real` still acts as a shared phoenix-step memory-term carrier outside canonical Explaino
- whether `explaino_cluster_radius` still acts as root-pack/root-shape runtime authority rather than a neutral Explaino deformation axis
- whether each parameter has real runtime effect
- whether default or zero behavior is compatible with canonical Explaino behavior for each parameter
- whether the lane can stay bounded without reopening dual-seed / multi-seed or resolved couplings
- what the provisional ownership models are before RED turns into proof

## Mandatory Kickoff Floor

Every future structural / root-pack prompt should still require, at minimum:

- mandatory bootstrap reads of the current restart authority and most recent closed Explaino-family slices through phase8p
- `py -3.14 tools/viewer_host_session_bootstrap.py --audit --tail-handoff 8`
- `py -3.14 tools/viewer_host_repo_status.py`
- a fresh slice plan/contract before mutation
- an explicit first RED/proving step
- a hostile review posture
- the post-closeout stale-plan gate on the committed head

The prompt may stay short as long as it points here and the main Explaino-all launch anchor while still forcing the session to report repo state, active lock, chosen slice, first RED, and expected owner seams.

## Per-Slice Success Rules

### Slice A - Classification + Owner-Seam Map

Must prove:

- `phoenix_p_real` and `explaino_cluster_radius` have explicit ownership answers
- the answer is based on selector/runtime/default/state proof, not analogy
- any required split between the two parameters is explicit

Status:

- closed in `phase8r`

### Split Follow-Up - Phoenix P Real Ownership Fence

Must prove:

- `phoenix_p_real` has one explicit owner surface outside canonical `explaino_all`
- default/reset/runtime/state truth stays attached to the shared phoenix-step carrier surface
- carrier-neutral `0.0` is not misreported as canonical Explaino collapse
- no separate enforcement follow-up is required on the current head

Status:

- closed in `phase8s`

### Later Cluster Follow-Up Only If Explicitly Chosen

Must prove:

- any `explaino_cluster_radius` follow-up stays bounded to that root-pack carrier lane
- canonical or different-model default/neutral behavior remains truthful
- legacy selectors do not preserve a second hidden implementation authority

### Enforcement Only If Needed Later

Must prove:

- a future structural / root-pack parameter cannot drift past the chosen ownership model without fail-closed test coverage
- canonical or fenced legacy behavior remains enforced after promotion or explicit non-promotion

## Post-Closeout Gate

Every future structural / root-pack slice must re-read its active phased plan on the committed head and fail closed if the plan still contains stale pre-closeout language or untruthful open asks.

## Start From

- [advanced_color_library_foundation_explaino_all_launch_anchor.md](/C:/code/cuda_newton_fractal_clone/docs/notes/advanced_color_library_foundation_explaino_all_launch_anchor.md)
- [advanced_color_library_foundation_phase8s_explaino_phoenix_p_real_followup_ownership_fence_PHASED_PLAN.md](/C:/code/cuda_newton_fractal_clone/docs/notes/advanced_color_library_foundation_phase8s_explaino_phoenix_p_real_followup_ownership_fence_PHASED_PLAN.md)
- [advanced_color_library_foundation_phase8r_explaino_structural_root_pack_classification_owner_map_PHASED_PLAN.md](/C:/code/cuda_newton_fractal_clone/docs/notes/advanced_color_library_foundation_phase8r_explaino_structural_root_pack_classification_owner_map_PHASED_PLAN.md)
- [advanced_color_library_foundation_phase8q_explaino_structural_root_pack_staging_PHASED_PLAN.md](/C:/code/cuda_newton_fractal_clone/docs/notes/advanced_color_library_foundation_phase8q_explaino_structural_root_pack_staging_PHASED_PLAN.md)
- [advanced_color_library_foundation_oracle_and_inventory.md](/C:/code/cuda_newton_fractal_clone/docs/notes/advanced_color_library_foundation_oracle_and_inventory.md)
- [advanced_color_feature_restart_inventory.md](/C:/code/cuda_newton_fractal_clone/docs/notes/advanced_color_feature_restart_inventory.md)
- [advanced_color_library_foundation_PHASED_PLAN.md](/C:/code/cuda_newton_fractal_clone/docs/notes/advanced_color_library_foundation_PHASED_PLAN.md)
