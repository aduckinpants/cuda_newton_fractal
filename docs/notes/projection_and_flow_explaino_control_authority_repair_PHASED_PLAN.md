# Projection-and-Flow Fractal - Explaino Control Authority Repair

## Current Phase

Closed on the committed head - the Explaino Projection-and-Flow control-authority repair is checkpointed, hostile-audited, stale-plan synced, and bounded away from color-pipeline expansion, BalanceVoid, meta-basin, Operator-Itinerary, DSL/program-space, renderer replacement, and legacy sample-surface mutation.

## Phase Checklist

- [x] Phase 1 - replace the closed Explaino Projection-and-Flow variant lock, prove the current dead/weak control-authority seams, and keep the bridge preflight bounded to opening this slice.
- [x] Phase 2 - land one bounded Explaino Projection-and-Flow control-authority repair that makes `root spread` and `phase strength` materially real while strengthening one-at-a-time seed / seed-fraction authority without reopening color-pipeline or broad engine work.
- [x] Phase 3 - prove the repaired state through focused derived-field, kernel/native, and published-runtime one-at-a-time control-sensitivity witnesses while preserving `SampleFractalPoints(...)` / `FractalSampleResult`.
- [x] Phase 4 - run hostile audit, checkpoint, write receipts, restore any temporary bridge scope, clear stale closeout text, and leave the repo clean on the committed head.

## Explicit User Asks

- [done] Open and execute only one bounded Explaino Projection-and-Flow control-authority repair slice.
- [done] Make the surfaced `root spread`, `phase strength`, `seed`, and `seed fraction` controls on `explaino_projection_and_flow` materially authoritative instead of hiding or de-scoping them.
- [done] Keep this bounded away from color-pipeline expansion, ExplainO-BalanceVoid, meta-basin, Operator-Itinerary, DSL/program-space, renderer replacement, and broad engine refactor work unless one tiny blocker seam proves otherwise.
- [done] Preserve `FractalSampleResult` and `SampleFractalPoints(...)` as the shipped legacy projection surfaces.
- [done] Close only with focused native proof, runtime publish, one-at-a-time published-runtime sensitivity proof, hostile audit, checkpoint commit, machine receipts, clean tree, and the committed-head stale-plan gate.

## Proof Ledger

- Bootstrap proved branch=`feature/fractal-sample-evidence-widening`, `HEAD=35c3773`, clean tree, fresh session, and an active locked contract still pointing at the already closed Explaino Projection-and-Flow variant contract `projection_and_flow_explaino_pattern_variant`.
- Minimal blocker-clearing preflight is required before feature edits because the closed Explaino Projection-and-Flow variant contract still owns the active lock; the bridge must stay limited to opening this new checked-in plan/contract pair and later restoring any temporary scope widening.
- Current UI/runtime authority already surfaces the controls on the public carrier: `ui/fractal_binding_surface_v1.ui_schema.json`, `ui_app/src/schema_binding.cpp`, `ui_app/src/diagnostics_capture.cpp`, and `ui_app/src/diagnostics_state_io.cpp` expose `explaino_seed`, `explaino_seed_drift`, `explaino_root_spread`, `explaino_phase_strength`, `explaino_warp_strength`, and `explaino_damping` on `explaino_projection_and_flow`.
- Current derived-field seam proves `root spread` and `phase strength` are dead on the carrier: `ui_app/src/fractal_family_rules.h` makes `UsesExplainoCustomPolynomialAuthority(explaino_projection_and_flow)` false, and `ui_app/src/fractal_derived_fields.cpp` therefore clears `explaino_root_count`, clears `poly_coeffs_b`, and re-syncs the shared Projection-and-Flow preset instead of consuming `explaino_root_spread` or `view.explaino_phase_strength`.
- Current runtime carrier seam proves `seed` / `seed fraction` are only a one-shot start perturbation on this lane: `ui_app/src/fractal_sample_device.inl` uses the combined Explaino seed only through `LogisticAreaUToSeed(...)` at `explaino_warp_start(...)`, then continues with shared Projection-and-Flow recurrence plus Explaino damping; `root spread` and `phase strength` do not appear on the carrier path at all.
- Current runtime proof is bundled rather than isolated: `tests/test_fractal_runtime_explaino_escape_variants.py` proves the carrier exists and differs from standalone when multiple controls change together, but it does not yet prove one-at-a-time sensitivity for `seed`, `seed fraction`, `root spread`, or `phase strength`.
- The bounded owner seams for this repair are expected to be `ui_app/src/fractal_derived_fields.cpp`, `ui_app/src/fractal_family_rules.h`, `ui_app/src/fractal_runtime_validation.h`, `ui_app/src/fractal_sample_device.inl`, `ui_app/src/schema_binding.cpp`, `ui_app/src/diagnostics_capture.cpp`, `ui_app/src/diagnostics_state_io.cpp`, `ui_app/tests/test_fractal_derived_fields.cpp`, `ui_app/tests/test_fractal_family_rules.cpp`, `ui_app/tests/test_fractal_runtime_validation.cpp`, `ui_app/tests/test_fractal_sample_kernel.cu`, `ui_app/tests/test_schema_binding.cpp`, `ui_app/tests/test_diagnostics_capture.cpp`, `ui_app/tests/test_diagnostics_state_io.cpp`, and `tests/test_fractal_runtime_explaino_escape_variants.py`.
- `SampleFractalPoints(...)` can remain untouched in signature and owner role if this repair keeps the Explaino authority inside derived-field/runtime parameter seams and lets the existing shipped sample surface carry the changed runtime semantics without API replacement.
- Implemented authority seam: `ui_app/src/fractal_derived_fields.cpp` now maps `explaino_projection_and_flow` into a custom Explaino polynomial keyed by `projection_and_flow_root_family`; quartic family keeps the full conjugate-pair Explaino root pack, cubic family collapses the second lobe into a bounded real anchor so `root spread`, `phase strength`, `seed`, and `seed drift` all materially perturb the carrier polynomial while standalone `projection_and_flow` keeps the shipped preset path.
- Shared-vs-Explaino control ownership is now explicit and proved: shared `projection_and_flow_root_family`, `projection_and_flow_target_radius`, and `projection_and_flow_pressure_threshold` still govern the Projection-and-Flow lane, while Explaino-owned `explaino_root_spread`, `view.explaino_phase_strength`, `explaino_seed`, `view.explaino_seed_drift`, `explaino_warp_strength`, and `explaino_damping` stay public and now materially alter either the derived carrier polynomial or the runtime start/damping path on `explaino_projection_and_flow`.
- Legacy projection ownership stays intact: `SampleFractalPoints(...)` and `FractalSampleResult` were not changed; the repaired lane continues to flow through the existing sample/result seam, with Explaino-specific control authority injected via derived `params.poly_coeffs` and existing runtime inputs instead of API widening.
- Focused native proof is green for the repair checkpoint via `ui_app/build_tests_vsdevcmd.cmd` (`artifacts/logs/projection_flow_control_authority_native_rerun8.log`), including the new derived-field, family-rule, diagnostics, validation, and kernel witnesses for the Explaino Projection-and-Flow carrier.
- Focused runtime publish is green for the repair checkpoint via `ui_app/build_vsdevcmd.cmd` (`artifacts/logs/projection_flow_control_authority_runtime_build_rerun2.log`).
- Focused published-runtime proof is green for the repair checkpoint via `py -3.14 -m pytest tests/test_fractal_runtime_explaino_escape_variants.py -k projection_and_flow -q` (`artifacts/logs/projection_flow_control_authority_runtime_pytest_rerun4.log`), including one-at-a-time headless frame-sensitivity witnesses for `root spread`, `phase strength`, `seed`, `seed drift`, and `warp strength`.
- Contract validation, phased-plan sync, and hostile-audit validation are green on this closeout lane via `tools/viewer_host_validate_slice_contract.py`, `tools/viewer_host_assert_phased_plan_sync.py`, and `tools/viewer_host_validate_hostile_audit.py`.
- The temporary bridge scope on the closed prior contract has been restored, checkpoint commit `0bab8f0` captured the bounded repair, and this phased plan now reflects the closed post-audit state with no remaining slice-owned work.

## Hostile Audit

- Status: complete
- Required posture: assume the first fix will still leave `root spread` or `phase strength` inert, will only strengthen bundled state differences instead of isolated controls, will accidentally hide or de-scope broken controls, or will widen into color-pipeline or generic engine work unless focused proof disproves each failure mode.
- Hostile review questions:
  Did I actually make `root spread` authoritative, or is it still a dead persisted slider?
  Did I actually make `phase strength` authoritative, or is it still dead on this carrier?
  Did I prove `seed` and `seed fraction` with isolated one-at-a-time runtime sensitivity, or only with bundled state changes?
  Did I preserve `SampleFractalPoints(...)` as the shipped legacy projection path?
  Did I silently widen into color-pipeline work or a generic engine rewrite?
  Did I stop with stale closeout text again?

## Audit Passes

- [x] Pass 1 - lock the RED with focused derived-field, kernel/native, and published-runtime witnesses proving the current carrier still has dead `root spread` / `phase strength` authority and only bundled seed/drift proof.
- [x] Pass 2 - hostile reread the repaired control-authority seam for boundedness, preserved legacy sampling, and one-at-a-time sensitivity instead of persistence-only proof.
- [x] Pass 3 - rerun the repaired native/runtime witnesses, restore the temporary bridge scope, and re-read the repaired state until no additional slice-owned defect or workflow lie remains.

## Audit Findings

- [x] Hostile audit found a real liar seam in `ui_app/src/fractal_runtime_validation.h`: after the first repair, `explaino_projection_and_flow` still accepted a quartic root-family claim backed by a cubic custom polynomial (or the inverse) as long as `explaino_root_count` matched. Added a regression in `ui_app/tests/test_fractal_runtime_validation.cpp`, tightened validation to require family/degree agreement, and reran native plus published-runtime proof on the repaired state.
- [x] Hostile audit found stale proof text in the diagnostics/runtime witnesses: `ui_app/tests/test_diagnostics_capture.cpp`, `ui_app/tests/test_diagnostics_state_io.cpp`, and the Projection-and-Flow runtime pytest section still encoded the old preset-persistence story. Updated those witnesses so they now assert explicit custom-poly identity plus one-at-a-time runtime frame sensitivity.

## Notes

- Required first REDs:
  1. prove `explaino_root_spread` is surfaced but does not materially affect `explaino_projection_and_flow`
  2. prove `explaino_phase_strength` is surfaced but does not materially affect `explaino_projection_and_flow`
  3. prove the current runtime proof for `explaino_seed` / `explaino_seed_drift` is bundled and does not isolate one-at-a-time sensitivity
  4. prove `SampleFractalPoints(...)` and current legacy callers remain bound to legacy semantics on the RED head
  5. prove color-pipeline integration, BalanceVoid, meta-basin, Operator-Itinerary, and DSL/program-space remain out of scope for this slice
