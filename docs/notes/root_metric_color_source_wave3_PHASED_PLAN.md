# Root-Metric Color Source Wave 3 Phased Plan

## Explicit User Asks

- Repeat the plan-to-goal workflow and work the next wave from the intake packet.
- Start from the closed Wave 2 root-field consumer work.
- Merge Wave 2 upstream before opening the next wave.
- Treat the side-folder packet as `import_candidate`, not repo authority.
- Choose exactly one Wave 3 medium enabler and prove exactly one consumer.
- Stop before Wave 4 or broader idea work.

## Current Phase

Phase 4 - hardening and close.

## Phase Checklist

- [x] Phase 0: merge `codex/root-field-consumer-wave2` into `master`, push, and rearward-review merged `master`.
- [x] Phase 0: create `codex/root-metric-color-source-wave3` from merged `master`.
- [x] Phase 0: validate this plan/contract and lock the active slice.
- [x] Phase 0: record the Wave 3 import-candidate selection and no-new-source audit.
- [x] Phase 1: add RED/native metadata and coloring tests for the selected root-metric source.
- [x] Phase 2: implement the selected source and minimal runtime plumbing.
- [x] Phase 3: prove exactly one consumer: `explaino_mandelbrot_root_trap` with `root_phase`.
- [ ] Phase 4: hostile audit, classify the enabler, sync docs/status, validate, receipt, rearward-review, push, and stop before Wave 4.

## Scope

In scope:

- Wave 3 medium enabler: root-metric Color Pipeline sources.
- Exactly one new source in this slice: `root_phase`.
- One consumer proof: `explaino_mandelbrot_root_trap` using `root_phase` as a Color Pipeline source.
- Native metadata/coloring/state tests and one no-mouse runtime proof.
- Capture/replay proof for the selected consumer if promoted.

Out of scope:

- Additional root-metric sources such as nearest-second-root gap, pair gap, centroid drift, symmetry error, velocity, cluster id, or adjacency class.
- Wave 4 low-hanging idea pass.
- New SDF operators, SDF orbit-trap lane, SDF force iteration, or new SDF-native families.
- Sample evidence widening, root-adjacency compact state, charts, seed hunting, slime/GA work, or perturbation zoom.
- Color Pipeline graph UI or Salticid adapter/removal work.
- Physical mouse automation.

## Phase 0 - Live Revalidation And Selection

### Intent

Import only the part of the side-folder Wave 3 packet that survives current repo state.

### Packet Input

The packet's Wave 3 rule is:

```text
Choose exactly one medium enabler.
Prove exactly one consumer.
Stop before it becomes architecture theater.
```

Candidate order in the packet:

1. Root-metric Color Pipeline sources if the no-new-source audit fails.
2. Sample evidence cleanup only if a concrete consumer proves the need.
3. Root-adjacency compact state if debug views are already strong.
4. SDF follow-on only after Root SDF repair closes clean.

### Selection Result

- Wave 2 is merged to `master` at `3f2f149`.
- The repo already has `root_proximity` and categorical `root_index`.
- No current source exposes root phase, nearest-second-root gap, pair gap, centroid drift, symmetry error, or root velocity.
- The no-new-source audit therefore fails for root phase/gap style views, but not for proximity-only trap coloring.
- Selected enabler: one new root-metric Color Pipeline source, `root_phase`.
- Selected consumer: `explaino_mandelbrot_root_trap`, because Wave 2 already proved exact Mandelbrot recurrence collapse and root-field trap/color consumption.

### Exit Criteria

- Plan and contract validate.
- Active slice is locked with `viewer_host_begin_work_slice.py`.
- No product code mutation before slice lock.

## Phase 1 - RED Tests

### Intent

Prove the missing source and consumer coverage before implementation.

Required REDs:

- Color Pipeline function catalog does not expose `root_phase`.
- Function-id parser does not resolve `root_phase`.
- `ColorPipelineSourceSignalKindForSignal(...)` cannot classify `root_phase` as phase-like.
- Escape-time coloring cannot produce a root-phase signal distinct from `root_proximity`.
- Runtime no-mouse source row setup cannot select `root_phase` on `explaino_mandelbrot_root_trap`.

## Phase 2 - Root Phase Source

### Intent

Add exactly one root-metric source without dumping the rest of the root-metric backlog into this slice.

Definition:

- `root_phase` samples the phase angle from the active sample point to the nearest authoritative root.
- The normalized signal is phase-like in `[0, 1)`.
- The source supports:
  - `signal.phase_offset`
  - `signal.wrap_cycles`
  - `signal.blend_weight`
- Existing root authority resolution is reused.
- Unsupported lanes fail closed to a structured/no-root signal path already used by root metrics; no fake roots.

Non-goals:

- No root-pair adjacency.
- No second-nearest root gap.
- No root velocity or `h(t)` metadata.
- No custom graph UI.

## Phase 3 - One Consumer Proof

### Intent

Prove the enabler on one real visible lane, not just in helper code.

Consumer:

- `explaino_mandelbrot_root_trap`.

Required proof:

- Selector identity remains `explaino_mandelbrot_root_trap`.
- Selecting `root_phase` as the Source row is accepted.
- Changing `signal.phase_offset` or `signal.wrap_cycles` changes the frame hash.
- `root_phase` and `root_proximity` produce different bounded samples on the same state.
- Capture Finding, load state, and replay preserve the selected source row and frame hash.

## Phase 4 - Hardening And Close

### Classification

Close the enabler as exactly one:

- `ENABLER_ACCEPTED`
- `ENABLER_ACCEPTED_DEBUG_ONLY`
- `ENABLER_DEFERRED`
- `ENABLER_ABORTED`

Expected classification if proofs pass:

- `ENABLER_ACCEPTED`

### Exit Criteria

- Hostile audit records a real finding or enough clean re-read passes after repair.
- Focused native rails pass.
- Runtime publish and published-runtime proof pass.
- Contract validation, plan sync, code quality, hostile-audit validation, and diff check pass.
- Validation and contract proof receipts are written.
- Rearward review is `ok`.
- Branch is pushed and tree is clean.
- Stop before Wave 4 low-hanging idea pass.

## Hostile Audit

- Status: complete
- Classification: `ENABLER_ACCEPTED`.
- Replan boundary: stop before Wave 4 after this branch is checkpointed and pushed.

Audit questions:

- Did I add only one root-metric source?
- Did `root_phase` reuse existing root authority instead of inventing roots?
- Did unsupported lanes fail closed without fake support?
- Did I prove exactly one consumer and avoid Wave 4 idea sprawl?
- Did I preserve existing `root_proximity`, `root_index`, Mandelbrot, Magnet, Root SDF, SDF pack, and Color Pipeline behavior?
- Did I stop before broad graph UI, SDF ops, sample evidence widening, or root-adjacency infrastructure?

## Audit Passes

- [x] Pass 1 - found the active slice contract omitted `docs/ui_salt`, which would have made the materialized metadata update an out-of-scope mutation. Contract was revised and relocked before metadata mutation.
- [x] Pass 2 - found stale materialized/runtime expected counts after adding `root_phase` and its companion suggestion. Counts and audits were repaired, then rerun.
- [x] Pass 3 - clean re-read of the repaired state: re-read touched authority seams for scope creep; only `root_phase` was added, with no extra root metrics, no Wave 4 work, no graph UI, and no SDF-op expansion.

## Audit Findings

- [x] Finding 1: contract scope originally missed `docs/ui_salt`; repaired by adding `docs/ui_salt` to `allowed_mutation_scope` and rerunning `viewer_host_revise_contract.py` for `global_active_contract`.
- [x] Finding 2: materialized/runtime count expectations were stale after adding `root_phase`; repaired schema/materializer/runtime expected counts, including active companion suggestions.
- [x] Finding 3: runtime proof initially covered only native/helper behavior; added no-mouse published-runtime proof for `explaino_mandelbrot_root_trap` selecting `root_phase` and changing `signal.phase_offset`. Confirmed the repaired state: the proof is now viewer-facing and no broader work was introduced.

## Proof Ledger

| Item | Evidence |
| --- | --- |
| Wave 2 merge | `git merge --ff-only codex/root-field-consumer-wave2` fast-forwarded `master` to `3f2f149`; `git push origin master` pushed it. |
| Merged-head rearward review | `py -3.14 tools/viewer_host_rearward_review.py` returned `status=ok` for `3f2f149`. |
| Active slice lock | `py -3.14 tools/viewer_host_begin_work_slice.py` opened `ck:a1b4de1b`; `viewer_host_revise_contract.py` relocked the revised contract after adding `docs/ui_salt` scope. |
| RED native proof | `artifacts/root_metric_color_source_wave3_red_native.log` failed as expected before `root_phase` existed. |
| Focused native proof | `cmd /c ui_app\build_tests_vsdevcmd.cmd test_color_pipeline_core test_escape_time_coloring > artifacts\root_metric_color_source_wave3_native_2.log 2>&1` passed. |
| Focused native public/state proof | `cmd /c ui_app\build_tests_vsdevcmd.cmd test_schema_binding test_fractal_family_rules test_fractal_types test_sample_tier_resolver test_diagnostics_state_io > artifacts\root_metric_color_source_wave3_native_4.log 2>&1` passed. |
| UI-Salt materializer/runtime contract proof | `py -3.14 -m pytest tests\test_ui_salt_materializer.py tests\test_fractal_runtime_ui_salt_contract.py -q > artifacts\root_metric_color_source_wave3_py_contract_runtime_2.log 2>&1` passed: `41 passed`. |
| Runtime publish | `cmd /c ui_app\build_vsdevcmd.cmd > artifacts\root_metric_color_source_wave3_publish.log 2>&1` passed and staged `D:\salt-fractal\cuda_newton_fractal_clone\runtime\fractal_ui.exe`. |
| Published runtime root-field proof | `py -3.14 -m pytest tests\test_fractal_runtime_root_field_consumers.py -q > artifacts\root_metric_color_source_wave3_runtime_root_field_consumers.log 2>&1` passed: `5 passed`. |
