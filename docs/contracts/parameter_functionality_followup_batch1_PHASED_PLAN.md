# Parameter Functionality Follow-Up Batch 1

## Explicit User Asks

- Implement the parameter functionality follow-up plan as bounded slices with proof, receipts, push, and clean tree.
- Start with weak epsilon witness classification before new controls.
- Add low-hanging, default-preserving formula controls before larger authority projects.
- Preserve no-mouse persistent runtime proof and avoid OS cursor or repeated viewer relaunch loops.
- Do not add owner-specific controls to `explaino_all`.
- Keep Color Pipeline, capture finding, FPS pacing, equation-pack viewport integration, perturbation zoom, generated editors, and Explaino Julia/Collatz authority work out of this first batch.

## Current Phase

Phase 1 is closed for checkpoint: `nova::epsilon` has a tuned live witness, `explaino_y::epsilon` remains honestly classified as unresolved/weak, and the first two low-risk controls are implemented with focused native, full native, and persistent no-mouse runtime proof.

## Phase Checklist

- [x] Bootstrap and confirm repo status.
- [x] Open this checked-in plan and contract.
- [x] Lock the active slice to this contract.
- [x] RED: add failing native/schema/runtime rails for `spider_feedback` and rational-escape denominator power.
- [x] RED/classification: add tuned witness checks for `nova::epsilon` and `explaino_y::epsilon` without claiming failure from the old default witness.
- [x] Implement `spider_feedback` with default `0.5`, owner-lane visibility, binding, validation, state IO, sample/runtime authority, and default parity.
- [x] Implement `explaino_rational_escape_denominator_power` with default `3`, owner-lane visibility, binding, validation, state IO, sample/runtime authority, and default parity.
- [x] Validate focused native/schema/sample/runtime rails.
- [x] Run hostile audit and repair real findings.
- [x] Checkpoint, write receipts, push, and verify clean tree.

## Proof Ledger

- Starting head: `4403589` on `master`.
- Prior audit authority: `artifacts/audit/explaino_lambda_phase_strength_parameter_audit.md`.
- Weak witness inputs: `nova::epsilon` and `explaino_y::epsilon` did not change under the broad 320x240 default witness, but runtime code reads epsilon, so they are not currently classified as dead.
- Planned RED for `spider_feedback`: schema/native/runtime tests fail until the control exists and changes Spider output while preserving default `0.5` parity.
- Planned RED for rational escape denominator power: schema/native/runtime tests fail until the control exists and changes `explaino_rational_escape` output while preserving default `3` parity.
- RED evidence: `parameter_functionality_batch1_red_schema` failed on missing `spider_feedback`; `parameter_functionality_batch1_red_sample` failed because `KernelParams` lacked both new fields.
- Implementation evidence: schema owner/native rails passed in `artifacts/validation/parameter_functionality_batch1_schema_owner_impl2.json`; validation/UI rail passed in `artifacts/validation/parameter_functionality_batch1_validation_ui_impl3.json`; sample rail passed in `artifacts/validation/parameter_functionality_batch1_sample_impl.json`.
- Runtime publish: `artifacts/validation/parameter_functionality_batch1_runtime_publish.json`.
- Persistent no-mouse runtime proof: `py -3.14 -m pytest tests/test_fractal_runtime_parameter_functionality.py -q --junitxml artifacts/pytest/parameter_functionality_batch1_runtime.junit.xml`; one viewer process proves Spider feedback, rational denominator power, and tuned Nova epsilon frame-hash sensitivity.
- Sample tier guard: `artifacts/validation/parameter_functionality_batch1_sample_tier.json`.
- Contract/code-quality/full-native proof: `artifacts/validation/parameter_functionality_followup_batch1_contract.json`, `artifacts/validation/parameter_functionality_batch1_code_quality.json`, and `artifacts/validation/parameter_functionality_batch1_full_native.json`.
- Weak epsilon classification: `artifacts/audit/parameter_functionality_batch1/epsilon_witness_classification.json`; `nova::epsilon` is proved live-sensitive on a tuned near-threshold view, while `explaino_y::epsilon` remains unresolved/weak after an 800-request bounded search and is not claimed dead or fixed.

## Hostile Audit

- Status: complete
- Did I prove or honestly classify the weak epsilon rows instead of hiding them? Nova proved; Explaino-Y documented unresolved/weak, not hidden.
- Did I keep `spider_feedback` owner-lane only and default-compatible? Current native/runtime proof says yes.
- Did I keep rational-escape denominator power owner-lane only and default-compatible? Current native/runtime proof says yes.
- Did I avoid `explaino_all`, Color Pipeline, capture, FPS pacing, equation-pack, perturbation, generated-editor, and larger Explaino authority drift? Yes; final diff only touches the bounded schema/runtime/test/state surfaces plus this contract/plan/handoff.
- Did I close with receipts, push, clean tree, and no stale plan text? Yes for the committed slice contract.

## Audit Passes

- [done] Pass 1: schema/binding audit found the native fixed-formula classifier rejected the requested Spider owner control; repaired with a narrow `spider_feedback` exception.
- [done] Pass 2: safe-mode/UI audit found the safe-mode schema did not expose the new controls and had stale expected panel count; repaired by adding safe-mode builders and tests.
- [done] Pass 3: scope audit found `spider_feedback` was a bound float control but not available to the schema-driven animation target list; repaired by adding the owner-lane animation target option. Diff audit found no `explaino_all` ownership exposure and no new OS mouse automation.
- [done] Clean re-read confirmed the repaired state after pass 3: no additional real defect found in owner visibility, validation, state IO, runtime math, no-mouse proof, or deferred-scope boundaries.

## Audit Findings

- [x] Real finding 1: `test_schema_binding` fixed-formula classifier treated Spider as a no-control lane and failed after `spider_feedback` was added. Repaired by allowing only `spider_feedback` on the Spider lane.
- [x] Real finding 2: safe-mode schema omitted the two new controls and the UI schema expected the old safe-mode fractal panel size. Repaired by adding safe-mode builders and updating native checks.
- [x] Real finding 3: `spider_feedback` was a new bound float parameter but was missing from the schema-driven animation target dropdown. Repaired by adding the owner-lane animation option; integer rational denominator power remains non-animated.
- [x] Clean re-read confirmed no additional real issue found after the third repair and the exact contract rails plus full native helper suite passed.

## Deferred To Later Contracts

- `collatz_transition_strength`.
- Burning Ship, Celtic Mandelbrot, and Perpendicular Burning Ship variant controls.
- Explaino Julia direct constants with explicit seeded/custom authority.
- Explaino Collatz direct variants.
- Generated/internal editors.
- Equation-pack viewport integration.
- Perturbation zoom.
